---
title: M-R12 execution evidence
type: execution
date: 2026-09-03
owner: autoforge
status: settled
touches: [".gitignore"]
---

- R12: added `.autoforge/` to `.gitignore:47` (curated default per AD-18, ponytail: 1 line, no new deps)
- Verified `git check-ignore -v .autoforge/discovery/report.md` → `.gitignore:47:.autoforge/` (exit 0)
- Existing tracked `.autoforge/` files remain tracked until `git rm --cached` (if curated subset desired); future ephemeral staging now ignored per policy
- No state/vault-notes.json edits; staging via explicit `git add .gitignore` + `git add -f .autoforge/execution/M-R12.md`
