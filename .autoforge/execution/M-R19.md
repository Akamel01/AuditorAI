---
plan: M-R19
title: Wayfinder plumbing traceability — CLI script and board parity
date: 2026-09-03
---

What changed:
- Added CLI script: scripts/wayfinder-tickets.ts that compiles a ticket index using indexWayfinderTickets() and emits JSON. Also writes a local markdown canonical artifact at .autoforge/explanation/M-R19-ticket-index.md.
- Updated src/wayfinder/tickets.ts to expose indexWayfinderTickets() with a small, local-canonic data load (from .autoforge/wayfinder/index.json if present) and computed counts (total/frontier/ready/hitl).
- Updated src/app/dev/mission-control/_components/ticket-board.tsx to export getTicketCounts() (parity helper) by reusing indexWayfinderTickets(). It imports from ../../../wayfinder/tickets.

Notes:
- No GitHub sync is performed; all changes are local and report-only where appropriate.
- Uses existing frontmatter/yaml-like structure for plan notes when available; this file demonstrates the canonical index path and counts.
