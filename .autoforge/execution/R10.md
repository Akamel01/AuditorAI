# R10: KV-first dedupe index persistence with file seed fallback (EROFS)

- Objective: Implement R10 state/dedupe-index.json KV-truth with file fallback, including EROFS warning when seed unavailable.
- Scope: touch src/discovery/dedupe-persist.ts and src/lib/persistence/keys.ts. Ensure single-writer semantics for persistence writes. Mirror behavior after R8 and in wave B serial context.
- Approach (lazy, minimal, reusable):
  - KV-first load/write using host DataStore if available; otherwise fall back to state/dedupe-index.json seed.
  - Load path now attempts to read from KV synchronously (best effort) before reading the seed file.
  - Writes are attempted to KV first; if ROFS/ENOENT, fall back to file and log a WARN. Serialized writes to avoid race conditions via an in-module queue.
  - Added simple KV keys exposure in keys.ts: DEDUPE_INDEX_PATH and dedupeKvKey() for reuse.
- Files touched:
  - src/discovery/dedupe-persist.ts: KV-first load path, file seed fallback, WARNs, and serialized writes.
  - src/lib/persistence/keys.ts: export DEDUPE_INDEX_PATH and dedupeKvKey() for reuse.
- Evidence plan:
  - R10 is implemented with KV-first load, file seed fallback, and serialized write path.
  - The code emits WARN messages when seed is missing due to ROFS/ENOENT.
  - One runnable check noted; vault-sync --check respected by test harness.
- Notes: ponytail ceiling comment added where relevant to document EROFS ceiling.
