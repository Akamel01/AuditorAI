# Review: H12 M-H12-1 (e2e + ticket-data fix)

**Verdict: APPROVED_WITH_NOTES**

Method: read-only review per brief (no test runs, no edits, no commits). Sources: `git diff HEAD`, workdir reads of the 5 listed files + `.autoforge/execution/H12-M1.md` + `src/wayfinder/ticket-types.ts` + `src/wayfinder/tickets.ts:83-89`. Gate-log claims (6/6 green) taken as testimony, not independently re-run per budget. M2 files explicitly out of scope (separate APPROVED_WITH_NOTES stands).

## (1) Helper locator → getByRole heading exact — PASS

- `tests/e2e/harvest-buttons.spec.ts:19`: `getByText("MISSION CONTROL")` → `getByRole("heading", { name: "Mission Control", exact: true })`.
- Strict-mode kill cited in `workflow/wayfinder/maps/ai-harvest-stream/tickets/H12-ci-green.md:21` (4-element match) is plausibly fixed: role+exact heading is strictly narrower than text match, so rot-proof direction is correct.
- Caveat (non-blocking): DOM heading text not re-verified within read-only scope; trust rests on gate log `.autoforge/execution/H12-M1.md:15-18` (3/3 harvest pass). No contrary evidence in diff.

## (2) Ticket fixes H10/H12 — PASS with cleanup note

- Validity: `src/wayfinder/ticket-types.ts:4-10` allows `open|claimed|blocked|closed|resolved|out-of-scope`; `src/wayfinder/tickets.ts:83-89` `parseStatus` throws on anything else (so `in_progress` would indeed 500 the index). Current workdir `H10-verify-continuous.md:6` = `status: open` (valid), `H12-ci-green.md:6-7` = `status: open` + `assignee: harvest-verifier` (valid).
- Minimality: H10 tracked diff is **trailing-whitespace-only** (`status: open` → `status: open `, confirmed via byte repr `b'status: open '` vs HEAD `b'status: open'`). Harmless (`parseScalar` in `tickets.ts:72-77` trims) but noise — should be reverted to bare `open`. No semantic `in_progress→open` transition is visible in `git diff HEAD`; HEAD already `open`, so the orchestrator's "invalid in_progress" cause cannot be confirmed from HEAD history (may predate HEAD or refer to uncommitted state). Current state is index-safe regardless.
- NOTHING else changed in H10 (single-line diff); H12 is untracked-new (expected — `git ls-files` confirms absent at HEAD) with otherwise intact front-matter.
- Closed tickets untouched: `H7-agent-reach-provider.md:6`, `H8-continuous-loop.md:6`, `H9-start-continuous-ui.md:6`, `H11-reach-keys-ui.md:6` all still `status: closed`; `git diff HEAD -- tickets/` shows only the H10 line.

## (3) flow.spec `.first()` additions — PASS (acceptable interim) — RULING explicit

- Locations: `tests/e2e/flow.spec.ts:29-30` (`a:hasText firstLink`), `:34` (`li a.first().click()`), `:121-122` (Attachments pair). All narrow previously strict-mode-failing ambiguous locators.
- **Ruling: ACCEPTABLE for CI-green, technically MASKING local test-pollution — not blocking, follow-up recommended.**
- Reasoning: duplicates arise from fixed project names (`Playwright Corridor`, `Playwright Attachments`) + fixed workspace keys (`flow.spec.ts:6,111`) accumulating same-name rows across repeated local runs against persistent state. In CI single-run (fresh server/state) n=1, so `.first()` ≡ identity — it does not weaken the CI gate, and all downstream assertions (stage text, finding-card count, adjudication, report, thumbnails) still execute. The masking risk (clicking a stale same-name project and passing on stale data) is real but low-severity: stale rows share name+stage+workspace so assertions would still exercise the same product path, not hide a product bug — it hides a test-isolation bug. The strictly stronger fix (unique names, e.g. `Playwright Corridor ${Date.now()}` asserting single match) is same-size and eliminates even that, but requiring it now would demand a re-run outside reviewer budget for zero CI-signal gain. Recommend as non-blocking follow-up; do not gate M1 on it.
- Scope note: H12 ticket acceptance (`H12-ci-green.md:34`) says "flow.spec unaffected" — this touch deviates from the ticket's "sibling locators in same file" scope, but orchestrator explicitly amended scope to include same-disease locators, so authorized creep. Flagged for traceability.

## (4) ci.yml ADMIN_KEY placement — PASS

- `.github/workflows/ci.yml:83-84`: `env: ADMIN_KEY: test-admin-key-0123456789abcdef` scoped under `jobs.e2e` only. Value matches spec default `tests/e2e/harvest-buttons.spec.ts:3`. No other jobs touched (`git diff HEAD -- .github/workflows/ci.yml` = 2 added lines only; state/evidence/vault/eval-gate/quality hunks absent).

## (5) No app-source edits in M1 diff — PASS (with workdir-pollution note)

- `git diff HEAD -- src/` is empty. M1 tracked diff = 3 files only (ci.yml + 2 specs) + H10 one-liner + untracked H12.
- Note: workdir `git diff HEAD --stat` shows non-M1 modifies (`discovery-harvest.yml`, `ops-loop-evidence.json` twins, `state/*.json`) attributable to M2/parallel lanes, not M1 — out of verdict scope but merge hygiene should stage only M1 paths per AGENTS.md.

## (6) H12 ticket own status/assignee — PASS

- `H12-ci-green.md:6-7`: `status: open` ∈ KNOWN_STATUS (`tickets.ts:25-32`), `assignee: harvest-verifier` non-empty → correctly non-frontier claimed work item; `blocked_by: []` / `blocks: [H10]` intact (`:8-9`).

## Notes / follow-ups (non-blocking)

1. Revert H10 trailing space (`H10:6` → bare `status: open`).
2. Consider unique-name e2e projects (timestamp suffix) as hardening over `.first()`.
3. Gate evidence is testimonial only (`.autoforge/execution/H12-M1.md:11-24` claims 3 flow + 3 harvest + lint/typecheck + mock exit 0); reviewer did not re-run per budget — validator/integration may spot-check `npx playwright test` + `node scripts/harvest-verify.mjs --mock`.
4. Do NOT judge M2 paths in this review (untracked writer, evidence-anchor files) — covered separately.

## Summary

- 6/6 items PASS (2 with notes). Worst: H10 whitespace noise (cosmetic); `.first()` masking risk (low, CI-neutral).
- Unblocks validator + integration on M1 files only; stage M1 paths explicitly, exclude M2/parallel dirt.
