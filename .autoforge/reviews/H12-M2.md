# R-H12-2 — Review: H12 M-H12-2 (gate evidence module)

**Verdict: APPROVED_WITH_NOTES**

Scope: M2 only. M1 files (`ci.yml`, `harvest-buttons.spec.ts`) intentionally not read/judged per brief (separate reviewer). Sources: `git diff HEAD`, working-tree reads of the 4 M2 globs, `plan-H12.md` M-H12-2 §, `execution/H12-M2.md`, `architecture/H12.md` §2. No test runs, no edits (this file excepted).

## (1) Writer scope + hygiene — PASS_WITH_NOTE

- **Only `commit`+`generatedAt` patched — PASS.** `git diff HEAD --stat` shows each JSON `4 ++--` (2 del / 2 add, single hunk `@@ -1,6 +1,6 @@`). Both diffs change only lines 2–3 (`generatedAt`, `commit`); `vercelDeployment` (line 4) onward untouched. No other hunks.
  - Evidence: `.autoforge/validation/ops-loop-evidence.json:2-3` new `2026-09-09T07:46:25.343Z` / `da88aa2…`; `stages/07_validate/output/ops-loop-evidence.json:2-3` identical new values; old values `2026-09-08T01:56:06.620Z` / `a1219915…` replaced in both.
  - Passthrough mechanism: `scripts/refresh-evidence-head.mjs:14-17` (`Object.assign({}, data, {commit, generatedAt})`) — all other keys preserved by construction; `mjs:20-22` serializes same `updated` object to both paths with `JSON.stringify(updated, null, 2) + "\n"`.
- **Stdlib only — PASS.** Imports are `mjs:2-4` (`node:fs`, `node:path`, `node:child_process`) — no deps. Note: plan (`plan-H12.md:44`) prescribed `node:fs/promises`; worker used sync `node:fs` (`readFileSync`/`writeFileSync`). Still stdlib, functionally equivalent for a ~27-line zero-arg script; no new dependency. Non-blocking.
- **Non-zero exit on git failure — PASS.** `mjs:13` `execSync('git rev-parse HEAD')` throws on git failure → `mjs:24-27` catch logs + `process.exit(1)`. JSON-parse failure takes the same path.
- **chmod +x — FAIL (note, non-blocking).** `mjs:1` has `#!/usr/bin/env node` shebang but mode is `644` (`ls -l scripts/refresh-evidence-head.mjs`, `stat` `644`), not executable. No runtime effect (workflow invokes via `node scripts/...`), but contract explicitly required `+x`. **Fix at merge (orchestrator-side): `chmod +x scripts/refresh-evidence-head.mjs`.**

## (2) Workflow — PASS

- **ONE new step before harvest — PASS.** Exactly one added step: `discovery-harvest.yml:35-36` (`- name: Refresh evidence head` / `run: node scripts/refresh-evidence-head.mjs`), placed after `npm ci` (`:33`) and before `run discovery harvest` (`:38`). Matches review brief ("ONE new step before harvest"). No other step additions/removals.
- **Bot-commit covers both JSONs — PASS.** `discovery-harvest.yml:64-65` extends `git add` with `.autoforge/validation/ops-loop-evidence.json stages/07_validate/output/ops-loop-evidence.json` (continuation of the ledger line). Full workflow diff is 2 hunks only (new step + extended `git add`); `stat` confirms `discovery-harvest.yml: 6 +++++-` (no other workflow changes).
- **`[skip ci]` + PR guard intact — PASS.** `discovery-harvest.yml:56` `if: github.event_name != 'pull_request'` retained verbatim; `:67` commit message retains `[skip ci]` verbatim. `fetch-depth: 0` (`:26`), `schedule.cron 17 * * * *` (`:5`), `workflow_dispatch` (`:6`) untouched.
- **YAML valid (read-only) — PASS.** Visual parse: 2-space mapping indent, 6-space step list indent, quoted cron, `|` block scalar at `:43` preserved, line-continuation `\` at `:64` well-formed. No execution performed per budget. (Pre-commit YAML hook remains the merge-gate authority.)
- Placement note (non-blocking): architect `H12.md:106` / plan `plan-H12.md:45` sketch the step *after* `run discovery harvest`; worker placed it *before*, matching this review's brief. Functionally equivalent for the anchor (HEAD is unchanged by the uncommitted harvest writes either way; the bot commit then covers ledger+anchor atomically). No action.

## (3) Honesty / Gate-meaningfulness — PASS

- **No fabricated evidence — PASS.** Writer invents no `t1`/`t2`/`vaultCheck`/health bodies (rejected alt `H12.md:136` respected); only re-anchors truthful `commit = rev-parse HEAD` (`mjs:13`) and `generatedAt = now` (`mjs:16`). JSON diffs confirm bodies untouched.
- **Scheduled-run anchor keeps Gate meaningful — PASS.** Refresh lives inside the hourly harvest job (cadence = harvest cadence, cron `17 * * * *`), not per-push; bot push carries `[skip ci]` so it cannot loop CI, and Gate goes green on the next human push via ancestor+freshness path (architect `H12.md:111-113` intent preserved; no checker/threshold edits in M2 scope).

## (4) Twin identical — PASS

- `diff -q .autoforge/validation/ops-loop-evidence.json stages/07_validate/output/ops-loop-evidence.json` → `TWIN-IDENTICAL` in working tree; both diffs show the same new `generatedAt`/`commit` pair. Twin-atomicity mechanism deviates cosmetically from spec (`H12.md:95-96` prescribed `fs.copyFile` canonical→twin; worker does two `writeFileSync` of the same string in the same process, `mjs:20-22`) — byte-identical result proven, so non-blocking. No separate-step/workflow split.

## (5) Touches / strays — PASS

- M2 globs per `plan-H12.md:42` are exactly the 4 files; `git status --porcelain` for those paths shows exactly `M .autoforge/validation/ops-loop-evidence.json`, `M stages/07_validate/output/ops-loop-evidence.json`, `M .github/workflows/discovery-harvest.yml`, `?? scripts/refresh-evidence-head.mjs` — no more, no fewer.
- Other working-tree entries (`M .github/workflows/ci.yml`, `M tests/e2e/harvest-buttons.spec.ts`) belong to the M1 lane (`plan-H12.md:8`); untracked `state/harvest-verify/*`, `test-results/*`, `workflow/.../H12-ci-green.md`, `.autoforge/{architecture,execution,plans}/H12*` are not writer outputs (writer writes only the two JSONs) and are not attributable to M2. No M2 stray outside its 4 globs.

## Standards / Spec (code-review lens, summary)

- Standards: stdlib-only, zero-arg, single-seam adapter at the anchor pair; no speculative generality (no args parsing, no thresholds); two `writeFileSync` calls are Duplicated-Code-shaped but share one `updated` string in one process — acceptable, `copyFile` would be marginally cleaner. No baseline smell above judgement-call level.
- Spec: all `plan-H12.md:43-46` steps present (writer + workflow step + extended add + immediate local refresh evidenced by the two JSON working-tree updates); no scope creep (no checker edits, no new commit/push step, no threshold change); no M1 overlap.

## Required actions (notes, non-blocking for V-H12)

1. `chmod +x scripts/refresh-evidence-head.mjs` at merge (orchestrator-side; worker is barred from git ops anyway). Re-verify with `stat`.
2. Optional (deferrable): switch twin write to `fs.copyFile` canonical→twin and/or `node:fs/promises` to match plan verbatim — cosmetic only; current bytes already identical.

*Validator unblocked: V-H12 may proceed; re-run writer at merge HEAD + `cmp -s` twin + `check-evidence-head.mjs` as joint-acceptance gate.*
