# H8 Review — `CHANGES_REQUIRED`

*Reviewed 2026-09-09 by autoforge-reviewer (read-only; no files edited, no tests run; evidence from `git diff HEAD`, plan, execution report, H8 ticket).*

## Verdict: `CHANGES_REQUIRED`

Direction and seam are right (flag on the stream, branch inside `tickStream`, no new module/deps); the diff is a partial implementation of M-H8-1 and M-H8-2 is entirely missing. Fixable with precise edits — not a rejection.

## Spec (plan compliance)

| # | Plan item | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | `continuous` flag, default `true` | **PASS** | `src/discovery/harvest-stream.ts:30` (`continuous: boolean`), `:48` (`continuous: boolean = true`), `:67` stored |
| 2 | Migration for old streams (`loadStream … ??= false` + comment) | **FAIL** | `harvest-stream.ts:82-85` — `loadStream` returns raw `s.get(...)` with no normalization |
| 3 | APPEND packages/quality cap 50, dedupe-guarded, replacing overwrite | **FAIL** (core) | `harvest-stream.ts:168-169` still overwrites `stream.packages = … ?? []` |
| 4 | Per-iteration (not cumulative) verify verdict + code comment | **FAIL** | `harvest-stream.ts:179` is `verifyStream(stream)` on cumulative arrays; no delta verdict, no comment |
| 5 | `VERIFYING→RUNNING` always + `continuous next` log | **PARTIAL→FAIL** | RUNNING-both-ways present (`:181-190`), but logs lack `<cellKey>`/rotation label from `ctx.query` |
| 6 | DONE unreachable + maxIterations ignored when continuous | **PASS** (structural) | `:181-190` continuous branches never touch DONE/FAILED/cap — vacuous while (3)+(4) missing |
| 7 | Byte-for-byte legacy when `false` | **FAIL** | `:197` changed `join("; ")` → `join(";")` (space dropped) vs HEAD legacy |
| 8 | pause/resume/stop + poll-tick untouched | **PASS w/ note** | `:210-246` unchanged; required invariant comment (`// continuous:true → DONE unreachable…`) missing |
| 9 | Creation log appends `continuous=` | **FAIL** (minor, plan-explicit) | `:65` unchanged |

## Standards

No new imports/deps — glob guard holds. Smells (not blockers): (a) missing migration leaves `continuous: undefined` against a `boolean` type — persisted-shape lie, worst issue; (b) inconsistent log prefixes — H10 observability depends on it; (c) one-char legacy drift (`";"` vs `"; "`).

## Test-gap answer

Existing 4 tests do not cover the new semantics — M-H8-2 entirely unmet (`tests/domain/harvest-stream.test.ts` untouched). 1-tick test passes vacuously; `verifyStream` unit never calls `tickStream`; legacy path now has zero coverage; every `createStream(..)` without 3rd arg silently flipped to `continuous=true`.

## Cross-module risk

- `src/app/api/dev/harvest-stream/route.ts:24` — `createStream(cellKey, live)` now infinite-by-default (intended; H9 owns param); downstream `harvest-verify`/e2e expecting terminal states will hang until H10 — known dependency, not H8 blocker.
- Old persisted streams fall legacy by falsiness (safe direction) but un-normalized; fix with `??= false`.
- `tests/domain/ai-harvest.test.ts:98-111` pure `verifyStream` unit — default-flip harmless. No `scripts/` callers of `createStream`.

## Required fixes

1. `loadStream` (`:82-85`): normalize + comment — old streams stay single-shot, never flip in-flight to infinite.
2. Append (`:168-169`): `stream.packages = [...stream.packages, ...newPkgs].slice(-50)` (+ quality paired); dedupe persist unchanged.
3. Delta verify (`:179`): per-iteration verdict when continuous + one-line comment (cumulative arrays kept for evidence).
4. Logs: `continuous next ${cellKey ?? label from ctx.query}` both branches; creation log appends `continuous=`; invariant comment.
5. Restore legacy `:197` to `join("; ")`.
6. `tests/domain/harvest-stream.test.ts`: continuous 3-ticks (append growth, never DONE/FAILED, dedupe persists, `continuous next` logs, iteration 3); single-shot DONE + FAILED-at-cap; controls with iteration counts; cap ≤50 newest-retained. Then vitest + tsc + lint.
