# H9 Review — Start-continuous API+UI wiring

Verdict: **CHANGES_REQUIRED**

Method: read-only. `git diff HEAD -- <4 files>` + contract reads (`.autoforge/architecture/H9.md`, `.autoforge/plans/plan-H9.md`, ticket `H9-start-continuous-ui.md`, execution `H9.md`). No test runs, no source edits, no commits.

## (1) Plan compliance

| Check | Result | Evidence |
|---|---|---|
| Route validates `continuous` non-boolean→400 | NOT PRESENT — and NOT REQUIRED (see note) | `route.ts:17-36` has no 400 branch; ticket acceptance line 1 + arch §1a + plan M-H9-1 mandate coercion `!== false`, never a 400. Task parenthetical contradicts contract — do not add it. |
| Route defaults `!== false` | PASS | `route.ts:30` `const continuous = body.continuous !== false;` verbatim per arch §1a / plan M-H9-1 |
| Persists on stream before save | PASS | `route.ts:33` `stream.continuous = continuous;` between `status="RUNNING"` (:31) and `saveStream` (:34) |
| 201 shape unchanged | PASS | `route.ts:36` `NextResponse.json({ streamId: stream.id, stream }, { status: 201 })` — outer shape identical, inner gains field as required |
| NO `harvest-stream.ts` edit | PASS | `git diff HEAD --name-only` = exactly the 4 guard files; `git diff HEAD -- src/discovery/harvest-stream.ts` empty |
| Client optional pass-through, no default | PASS | `client.ts:131` adds `continuous?: boolean` only; no default logic (locality: default lives in route per arch §1b) |
| Control sends `continuous:true` | PASS | `ai-harvest-control.tsx:46` `JSON.stringify({ live, cellKey: cellKey \|\| null, continuous: true })` |
| Label `Start continuous` | PASS | `ai-harvest-control.tsx:146` `{busy ? "Starting…" : "Start continuous"}` |
| Truthy-gated badge + status suffix | PASS (style nip, §4) | `:187` `{stream.continuous && (<span …>continuous</span>)}` + `:197` `{stream.continuous ? " · continuous — Stop to end" : null}` — falsy-safe; `&&` ≡ plan's `!!`, `null` ≡ `""` in JSX |
| Footer append verbatim | PASS | `:222` contains `Reach: Exa+Jina via agent-reach` verbatim |
| `data-testid` unchanged | PASS | `:144` `data-testid="ai-harvest-start"` |
| `Stream.continuous` optional | PASS | `:21` `continuous?: boolean` (required would break H8 legacy streams per arch R1/R4) |
| E2E asserts POST-body `continuous:true` | FAIL (toothless) | `spec:139` inside `try { … } catch { // skip }` — assertion failure is swallowed; see Required-1 |
| E2E asserts badge visible | FAIL (toothless + imprecise) | `spec:151` `await expect(page.getByText(/continuous/)).toBeVisible().catch(() => {})` — swallowed; generic regex vs arch §1d's `/continuous — Stop to end/` + `/^continuous$/`; see Required-2 |
| `@harvest` tag kept | PASS | `spec:101` `test("@harvest ai harvest stream …` |

## (2) Lint-directive fix — FAIL (blocking)

- `spec:134` still `// @ts-ignore - postDataJSON may be unavailable in some runners`; diff adds the surrounding block but does not touch that line.
- Execution `H9.md:15-19` claims lint error `133:9 ban-ts-comment` was observed then "no further errors" after fixes — contradicted by the file on disk. The violation persists, so plan gate M-H9-1/M-H9-3 (`lint --max-warnings=0` on touched files) cannot pass.
- In-scope (blocking lint on a touched file), not scope creep. See Required-3.

## (3) Cross-module

| Check | Result | Evidence |
|---|---|---|
| Default-true flips no pre-existing caller | PASS | `git grep startHarvestStream` → only definition `client.ts:131`; sole POST caller is control's direct `adminApi` (`:44-47`). Optional field keeps helper compiling; no other caller to break |
| Old streams (`continuous` undefined) render legacy line | PASS | Truthy gates (`:187`, `:197`) + `harvest-stream.ts:88-90` migration (`typeof st.continuous !== "boolean"` → `false`); route always writes boolean for new streams |
| Note (non-blocking): `createStream` log lies on `false` path | NOTE | Route calls `createStream(cellKey, live)` (`route.ts:28`, default `continuous=true`, `harvest-stream.ts:48,65` logs `continuous=true`) then overwrites to `false` (`route.ts:33`). Harmless (false = debug-only) and guard-correct (no `harvest-stream.ts` edit per plan lock); converge only if H8 signature is later threaded |

## (4) Apple-design — PASS WITH NOTE

- Badge reuses `rounded bg-sunken px-1.5 py-0.5 text-text` idiom, text-only, no new keyframes/motion (`animate-pulse` at `:192-194` pre-exists untouched). Only addition vs plan's class string is spacing utility `ml-2` (`:188`) — not motion, acceptable.

## (5) Touches — PASS

- `git diff HEAD --stat` = 4 files, all inside guard (`route.ts`, `client.ts`, `ai-harvest-control.tsx`, `harvest-buttons.spec.ts`). No provider/loop/CI-gate edits; `Pause/Resume/Stop` (`:57-85`) and 2s poll + `ponytail:` ceiling (`:100-101`) unchanged.

## Required changes (worker, small diff, same 4-file guard)

1. **E2E POST-body assert must enforce** (`spec:130-142`): remove outer `try { … } catch { // skip }`; read body via `postDataJSON?.()` with a `postData()`-parse fallback, then bare `expect(posted.continuous).toBe(true)`. If the runner API is untyped, type it properly — do not swallow.
2. **E2E badge asserts must enforce + match arch §1d** (`spec:150-151`): drop `.catch(() => {})`; assert both `page.getByText(/continuous — Stop to end/)` and `/^continuous$/` visible. Keep `Poll 2s` check; optional `/Reach: Exa\+Jina/` footer assert still welcome. Keep harness/tag untouched.
3. **Fix lint directive** (`spec:134`): replace `@ts-ignore` so `ban-ts-comment` passes under `--max-warnings=0` (remove the directive with proper typing, or `ts-expect-error` only if the next line provably errors — verify with the touched-scope lint run from plan M-H9-3; do not leave the current line).
4. **Do not add 400-validation** for non-boolean `continuous`: contract mandates `!== false` coercion (arch §1a, plan M-H9-1, ticket line 1). Adding a 400 would violate the guard design.
5. Re-prove gates per plan and correct execution notes: touched-scope `tsc --noEmit`, `lint --max-warnings=0`, `build`, `playwright --list`; execution file must not claim a lint fix that is not in the diff.

## Summary

- Item results: (1) pass except 2 toothless e2e asserts; (2) fail — `@ts-ignore` persists vs execution claim; (3) pass; (4) pass-with-note (`ml-2` only); (5) pass.
- Worst blocking: lint violation on touched file (`spec:134`) + non-enforcing e2e "asserts" — ticket acceptance line 4 not met in any enforceable sense.
