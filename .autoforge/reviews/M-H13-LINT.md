# Review: M-H13-LINT — `--lint` / `--root` on scripts/wayfinder-tickets.ts

**Verdict: APPROVED_WITH_NOTES** (accept the M-R19-write removal in all modes; update plan + work-order to match. No source changes required.)

**Scope:** `scripts/wayfinder-tickets.ts` (57 lines, read in full) vs plan contract (`--root`, `--lint` exit 0/1/2, no artifact write, `--json` + default shapes preserved) + ADR-0016.
**Method:** read-only. No shell executed (§13–14): worker-quoted probes (lint exit 0/1, `--json` shape, typecheck 0) are **unverified**; HEAD diff via `git show` was **not** pulled (would require exec, forbidden to this reviewer), so "byte-identical to HEAD" is verified only as "shapes match the described HEAD contract", not by diff. `src/wayfinder/tickets.ts`, `src/wayfinder/ticket-types.ts`, `src/app/api/dev/tickets/route.ts`, `package.json`, `.gitignore`, `.autoforge/AGENTS.md`, `.autoforge/execution/M-R19.md`, `.autoforge/plans/plan.md`, `.autoforge/execution/work-order.json`, `docs/adr/0016-wayfinder-local-markdown-ops-residual.md`, `workflow/…/H13-ticket-index-leniency.md`, `.github/workflows/ci.yml:68-80` read as context. Every row below pairs a verbatim CURRENT-file quote (`path:line`); plan/report lines are cited as contract only, never as evidence.

## Spec axis (contract from review brief + plan.md:22-34 / work-order M-H13-LINT)

| # | Check | Result | Verbatim CURRENT-file evidence |
|---|-------|--------|--------------------------------|
| S1 | `--root <dir>` supported, forwarded to loader in both modes | PASS | `scripts/wayfinder-tickets.ts:24-25` — `const rootIdx = argv.indexOf("--root");` / `const root = rootIdx !== -1 ? (argv[rootIdx + 1] ?? process.cwd()) : process.cwd();` consumed at `:29` (`loadTicketsFromTree(root as string)`) and `:47` (`indexWayfinderTickets(root)`). |
| S2 | `--lint` uses the same lenient loader (CODE seam), not a reimplementation | PASS | `scripts/wayfinder-tickets.ts:10` — `import { indexWayfinderTickets, loadTicketsFromTree } from "../src/wayfinder/tickets";` with lint branch `:29` — `const { skipped } = loadTicketsFromTree(root as string);`. Seam is real: `src/wayfinder/tickets.ts:146` — `export function loadTicketsFromTree(root = process.cwd()): { tickets: WayfinderTicket[]; skipped: SkippedTicket[] } {`. |
| S3 | Lint prints `<file>: <reason>` per skipped entry to **stderr** | PASS | `scripts/wayfinder-tickets.ts:31-34` — `for (const s of skipped) {` / `const file = (s as any).file ?? "<unknown>";` / `const reason = (s as any).reason ?? "invalid";` / `` console.error(`${file}: ${reason}`); ``. Shape matches producer: `src/wayfinder/ticket-types.ts:37-40` — `export interface SkippedTicket {` / `file: string;` / `reason: string;` / `}`. |
| S4 | Exit 0 clean / 1 dirty / 2 IO-error | PASS (IO path real; usage-2 narrow — see N2) | `scripts/wayfinder-tickets.ts:30-41` — `if (Array.isArray(skipped) && skipped.length > 0) {` … `process.exit(1);` … `} else {` / `process.exit(0);` / `} catch {` / `process.exit(2);` plus outer `:54-56` — `} catch {` / `process.exit(2);`. Real IO trigger: `src/wayfinder/tickets.ts:140-141` — `const mapsDir = path.join(root, WAYFINDER_MAPS_DIR);` / `return readdirSync(mapsDir, …)` throws on missing tree → lint `catch` → exit 2. Consumer expects exactly this: `.github/workflows/ci.yml:75-76` — `- name: gate lint via wayfinder (lint gate)` / `run: npx tsx scripts/wayfinder-tickets.ts --lint`. |
| S5 | `--json` still `{counts}`; default still `{tickets, counts}` (byte-identical shapes) | PASS (as implemented; conflicts with plan "gains skipped" — see N1) | `scripts/wayfinder-tickets.ts:48-53` — `if (argv.includes("--json")) {` / `// Keep the historical shape requested for automation: { counts }` / `printCountsOnly(idx);` / `} else {` / `printFull(idx);` with `:12-14` — `function printCountsOnly(…) {` / `console.log(JSON.stringify({ counts: idx.counts }, null, 2));` and `:16-18` — `console.log(JSON.stringify({ tickets: idx.tickets, counts: idx.counts }, null, 2));`. Counts superset covers ADR-0016:7's five keys via `src/wayfinder/tickets.ts:189-198` (`total/open/claimed/blocked/closed/frontier/ready_without_owner/hitl_frontier`). |
| S6 | Static imports (tsx-invoked, no dynamic import) | PASS | `scripts/wayfinder-tickets.ts:10` (static `import {…} from "…"`; file contains zero `import(` / `require(`). Runtime present: `package.json:41` — `"tsx": "^4.23.12"` (devDeps). |
| S7 | No artifact write in lint mode | PASS | Full 57-line file contains zero `writeFileSync`/`mkdirSync`/`writeFile`/`appendFile` and imports only the two read-seam loaders (`:10`); declared at `:7` — `// - No artifact writes in lint mode; preserves original JSON shape in non-lint mode`. |
| S8 | Touches only `scripts/wayfinder-tickets.ts` | PASS | Plan/work-order limit LINT to that file; no evidence in the read files of any other file edited for LINT (`src/wayfinder/*`, `route.ts`, tests untouched by this module). |

No FAIL / CHANGES_REQUIRED rows on the Spec axis. Worker probe quotes (exit 0/1, `--json` counts, typecheck 0) are **not** re-asserted here — unverified under read-only constraints; validator must re-run (see Follow-ups).

## Standards axis

| # | Check | Result | Verbatim CURRENT-file evidence |
|---|-------|--------|--------------------------------|
| T1 | No new deps, no speculative abstraction | PASS | Only existing seam reuse (`:10`); no new imports beyond `../src/wayfinder/tickets`; `package.json:8-23` shows no added dep/script for LINT. |
| T2 | No Mysterious Name / Duplicated Code / Feature Envy | PASS (judgement) | `lint`/`rootIdx`/`root`/`skipped`/`file`/`reason` honest; lint branch delegates classification to `loadTicketsFromTree` rather than re-parsing front-matter (no envy, no duplication). |
| T3 | Exit-path clarity (dead `return`) | NOTE (non-blocking) | `scripts/wayfinder-tickets.ts:43` — `return;` sits after a branch whose every path `process.exit`s (`:36`/`38`/`41`); unreachable in practice, harmless. No change requested. |

## Ruling on the KNOWN DEVIATION: M-R19 markdown write removed in ALL modes, not just lint

**Ruling: ACCEPT the removal. Do NOT restore for non-lint. Update the plan instead (APPROVED_WITH_NOTES, not CHANGES_REQUIRED).** Blast-radius evidence (all CURRENT-file quotes):

1. What was removed: `.autoforge/execution/M-R19.md:8` (contract record, not code) — "Also writes a local markdown canonical artifact at `.autoforge/explanation/M-R19-ticket-index.md`." The current CLI writes nothing (S7 above).
2. The artifact dir is **ephemeral + git-ignored by policy**: `.autoforge/AGENTS.md:8` — "* Ephemeral (ignored): `tmp/`, `*.log`, `.autoforge/explanation/` (local markdown), …" and `.gitignore:49-65` — `.autoforge/*` ignored with a curated `!`-exception list (`state.json`, `discovery/`, `architecture/`, `plans/`, `requirements/`, `execution/`, `reviews/`, `validation/`) that **excludes** `explanation/`. A write there could never be a committed artifact.
3. **Zero readers/writers in code.** Repo-wide search for `M-R19-ticket-index|explanation/M-R19|ticket-index\.md` returns exactly 2 hits, both prose (`.autoforge/execution/M-R19.md:8`, `.autoforge/plans/plan.md:30`); zero hits in `src/**`, `scripts/**`, gates, or tests. No CI step, route, board component, or test imports that path. (`src/app/api/dev/tickets/route.ts:10` — `return NextResponse.json(indexWayfinderTickets());` serves the live index directly, never the markdown file.)
4. **No ADR impact.** ADR-0016:7 makes `workflow/wayfinder/maps/*.md` front-matter canonical and requires only "`npm run tickets` / `scripts/wayfinder-tickets.ts --json` and Mission Control Tickets board both read the same `TicketIndex`" with the five counts; ADR-0016:18-20 require CLI↔board parity and name the reversible path as "deleting `scripts/wayfinder-tickets.ts`" — the `explanation/` file appears nowhere in the ADR. Removing it changes no ratified contract.
5. **Plan scoping is the only conflict, and it is stale, not substantive.** `plan.md:23` ("skips M-R19 markdown-artifact write (`:27-39`) in lint mode") and `:30` ("no `…M-R19-ticket-index.md` write in lint mode") plus `work-order.json:32` ("lint mode writes no `.autoforge/explanation` artifact") all scope suppression to lint mode, implying retention elsewhere. Retaining an ignored, unread, uncommitted side effect in non-lint would be anti-ponytail (reintroduce `fs` writes + a second output channel for zero consumers). The worker's all-modes removal is the lazy-correct choice.
6. **Required doc fix (no code change):** amend `plan.md:23` → "skips M-R19 markdown-artifact write in all modes (artifact retired: ephemeral ignored dir, zero readers — see review)"; amend `:30` similarly; amend `work-order.json` M-H13-LINT acceptance `"lint mode writes no … artifact"` → `"writes no … artifact in any mode"`. Optionally append one line to `.autoforge/execution/M-R19.md` noting the artifact retired under M-H13-LINT.

## Notes (non-blocking, no code change)

- **N1 — Doc conflict on `skipped` in JSON outputs.** `plan.md:23` ("`--json` gains `skipped` array") and `plan.md:31` ("`--lint --json` emits parseable JSON with `skipped` array", echoed at `work-order.json:33,39`) are **not** implemented: lint ignores `--json` entirely (`scripts/wayfinder-tickets.ts:27` — `if (lint) {`, no `argv.includes("--json")` inside `:27-44`) and non-lint `--json`/default omit `skipped` (`:12-18`, quoted in S5) even though the library now carries it (`src/wayfinder/ticket-types.ts:57` — `skipped: SkippedTicket[];`, required; `src/wayfinder/tickets.ts:205` — `skipped,` in `buildTicketIndex` return; `route.ts:10` therefore serves `skipped` while the CLI drops it). The review brief's contract ("`--json` + default outputs byte-identical to HEAD") **sides with the implementation** (automation parsing `{counts}` / `{tickets,counts}` keeps working; the gate consumes exit codes, not JSON). Forcing `--lint --json` JSON would fork lint into two output channels and complicate the gate for no consumer. **Recommendation:** keep the code as-is; resolve the conflict on the plan side — either strike `plan.md:31`/`work-order.json:33` or reassign "JSON gains `skipped`" to a future module with a named consumer. Deliberately NOT filed as CHANGES_REQUIRED.
- **N2 — Exit-2 covers IO, not usage.** `--root` with no value falls back to cwd (`scripts/wayfinder-tickets.ts:25` — `(argv[rootIdx + 1] ?? process.cwd())`) rather than exiting 2, and `--root=<eq>` form is unparsed. No acceptance probe exercises usage-2, and speculative flag-validation is correctly skipped (ponytail). Document only.
- **N3 — Double log per skipped file in lint.** Library warns per file (`src/wayfinder/tickets.ts:175` — `` console.warn(`${rel}: ${reason}`); ``) and CLI errors per file (`scripts/wayfinder-tickets.ts:34` — `` console.error(`${file}: ${reason}`); ``); both go to stderr, so the gate still sees `file: reason` (S3 holds) with duplication. Cosmetic; no change requested (fix belongs to CODE if ever: drop one channel).
- **N4 — `npm run tickets` absent.** `package.json:8-23` defines no `tickets` script although ADR-0016:7 cites "`npm run tickets` / `scripts/… --json`". Direct `npx tsx scripts/wayfinder-tickets.ts` still satisfies the ADR's either/or; restoring the alias is out of LINT scope (LINT touches one file). Flag for the owning lane, not this verdict.
- **N5 — H13 ticket satisfied at the LINT layer.** `H13-ticket-index-leniency.md:32` (lint exits non-zero listing invalid files) is met by S3+S4; `:31` (`skipped` field, silence impossible) is met at the library layer (`ticket-types.ts:57`, `tickets.ts:205`), not the CLI — consistent with N1.

## Follow-ups for validator (not the worker)

1. Re-run probes (worker quotes unverified): `npx tsx scripts/wayfinder-tickets.ts --lint; echo $?` → 0; temp tree (1 good + 1 `status: in_progress`) `--lint --root <tmp>` → 1 with stderr `file: reason`; `--json` → `jq '.counts | has("total")'`; `npm run typecheck` scoped to the CLI if global gate is red for unrelated lanes.
2. Confirm HEAD byte-identity with a real diff (`git diff HEAD -- scripts/wayfinder-tickets.ts` + `git show HEAD:scripts/wayfinder-tickets.ts`) — unavailable to this read-only review.
3. Apply the two plan-side doc amendments (Deviation §6 + N1) with explicit `git add .autoforge/plans/plan.md .autoforge/execution/work-order.json .autoforge/reviews/M-H13-LINT.md` only.

**Summary:** Standards: 2 pass, 1 note; worst item is the unreachable `return` (cosmetic). Spec: 8 pass, 0 fail; strongest evidence is S3+S4 (stderr `file: reason` + 0/1/2 exits on the real CODE seam). M-R19 removal in all modes is accepted with blast-radius evidence above; plan updated, nothing restored.
