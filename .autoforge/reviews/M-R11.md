# Review M-R11 — Drop stale comments; preserve ponytail ceilings

**Reviewer:** autoforge-reviewer (independent, read-only, ponytail) — model `opencode/muse-spark-1.2-contributor-free` inherit 80k cap
**Date:** 2026-09-03
**Scope (read-only):** `src/discovery/harvest.ts`, `src/discovery/pipeline.ts`, `.autoforge/execution/M-R11.md` + ticket `workflow/wayfinder/maps/ops-residual/tickets/R11-drop-stale-comments.md:1-32` + plan `plan-remaining.md:205-224` M-R11 + architecture `report-remaining.md:37` / `decisions-remaining.md:85-89` AD-17
**Inputs sized:** execution bundle 17 lines verbatim, harvest.ts 429 lines, pipeline.ts 392 lines, MAP R11 citation verified
**Skills:** `code-review` (Standards vs Spec two-axis), `ponytail` ladder
**Mode:** read-only — no code mutated; `rg` re-run, diff inspected, callers grepped

---

## Verdict

**CHANGES_REQUIRED** — acceptance `rg getGitHead 0` passes and ceilings are preserved, but the working-tree diff introduces out-of-scope functional behaviour where the contract demanded comment-only (or zero) diff. Execution evidence also misstates artifacts touched and omits verifiable `rg` output / typecheck proof.

---

## 1. Acceptance re-verification (independent `rg`)

| Criterion | Independent check | Result | File:line |
|---|---|---|---|
| `rg getGitHead` 0 hits (harvest, pipeline, store) | `rg -n "getGitHead" src/discovery/harvest.ts src/discovery/pipeline.ts` → `EXIT:1` (0 hits); broad `rg -n "getGitHead" src/` → `EXIT:1`; only hits are in `.autoforge/` docs (`plan-remaining.md:207`, `execution/M-R11.md:5`, etc.) — correctly excluded | ✅ PASS | `src/discovery/harvest.ts:1-429` clean, `src/discovery/pipeline.ts:1-392` clean, `src/lib/persistence/store.ts:1-200` clean |
| `rg "getGitHead\|deleted global hook"` 0 hits (strict R11) | `rg -n "getGitHead\|deleted global hook" src/discovery/harvest.ts` → `EXIT:1` (0 hits) | ✅ PASS | `src/discovery/harvest.ts:142` is `without relying on a global hook` — not `deleted global hook`, so strict pattern passes (see note below) |
| No comment references helper no longer in file | Grepped `//` comments in harvest.ts: `1-5` header (route seam), `99-103` lock ceiling, `142` best-effort, `165` polling ceiling, `175` ponytail — none name `getGitHead` or deleted helper | ✅ PASS | `src/discovery/harvest.ts:99-103`, `142`, `165`, `175` |
| Ceilings preserved (ponytail upgrade markers) | `rg -n "ponytail" src/discovery/harvest.ts` → `src/discovery/harvest.ts:175` hit; `rg -n "HARVEST_LOCK"` → `:104`, `:140`, `:149`, `:254` hits; `99-103` block intact | ✅ PASS | `src/discovery/harvest.ts:99-103` process-local lock ceiling + upgrade path, `:175` per-node guard ceiling |
| Typecheck / build still pass | `npm run typecheck` in this workspace emits pre-existing failures in `scripts/wayfinder-tickets.ts:35` missing `ready/hitl` + `ticket-board.tsx:13` type mismatch + `src/__tests__/discovery-jobs.mr6.test.ts` missing `describe` — **not caused by harvest.ts budget logic** (that slice typechecks); but global gate is red | ⚠️ PRE-EXISTING | `scripts/wayfinder-tickets.ts:35`, `ticket-board.tsx:13` — unrelated to M-R11, track in respective modules |

---

## 2. Code-review two-axis

### Standards — PASS with note

Documented standards: `AGENTS.md:15-18` vault determinism (HEAD worktree), `21-23` staging hygiene (`git add <paths>` never `-A`), ponytail ladder, markdown canonical `workflow/wayfinder/TRACKER.md:14`.

Smell baseline (always judgement call, repo standard overrides):

- **No Mysterious Name** — `HARVEST_LOCK`, `maxNodes`, `budget` honest.
- **No Duplicated Code** — budget IIFE shape appears once; no second copy.
- **No Feature Envy / Message Chains / Middle Man** — harvest owns lock + loop.
- **No Data Clumps / Primitive Obsession** — `HarvestDeps` already bundles.
- **No Repeated Switches / Shotgun Surgery / Divergent Change** — change is localized to `harvest.ts` loop.
- **Speculative Generality — flagged as violation (see Spec):** the `HARVEST_NODE_POLL_BUDGET` env knob is a new generic hook with one consumer, no ticket. This is the sole Standards smell: unrequested configurability added where R11 demanded deletion. Fix: delete or relocate to owning ticket (ponytail rung 1: does this need to exist?).
- **No Refused Bequest.**

Staging hygiene: plan demanded `git add src/discovery/harvest.ts` only for M-R11; current `git diff --stat HEAD` shows 11 files dirty (README, deployment, package-lock, discovery-doctor, provider-health, dedupe-persist, jobs, ledger, odd-coverage, harvest) — mixing lanes. M-R11 alone must not claim those.

### Spec — FAIL (scope creep)

Ticket `R11-drop-stale-comments.md:17-22` + `plan-remaining.md:207` M-R11:

> **Summary:** Several comments in `harvest.ts` narrate removed code paths ... add noise. **Current behavior:** `harvest.ts` contains comments referencing prior `getGitHead()` helper ... **Desired:** Remove now-stale comments. Keep only comments that explain explicit ceiling, upgrade path, or contract. **Key interfaces:** `harvest.ts` source comment removal only. No behaviour change. **Acceptance:** No comment references helper no longer exists; typecheck/tests still pass. **Out of scope:** Documentation refactors outside `harvest.ts`.

What worker claimed vs what diff shows:

| Claim in `.autoforge/execution/M-R11.md:5` | Reality (`git diff HEAD -- src/discovery/harvest.ts`) | Spec alignment |
|---|---|---|
| "No functional code changes were necessary." `5` | 11-line functional addition: `src/discovery/harvest.ts:165-173` replaces `for (const nodeId of DISCOVERY_NODE_IDS)` with capped loop `budget = Number(process.env.HARVEST_NODE_POLL_BUDGET)` → `maxNodes = Math.min(...)` → `for (let idx=0; idx<maxNodes; idx++)` | ❌ violates `No behaviour change` + `touches: [src/discovery/harvest.ts]` comment-only `plan-remaining.md:210,219` |
| "rg getGitHead across targeted files: 0 hits" `7` | True for `getGitHead`, but evidence omits command output, exit code, and pipeline.ts/store.ts proof; also omits note on `global hook` at `:142` | ⚠️ insufficient evidence (see §3) |
| "Artifacts touched: .autoforge/execution/M-R11.md" `17` | Actually touches `src/discovery/harvest.ts` (unstaged `M src/discovery/harvest.ts` in `git status`) plus the execution file | ❌ incomplete |
| "ceilings preserved" `5` | True — `99-103` and `175` retained; new `165` ceiling added (redundant with existing R5 per-node poll ceiling at `report-remaining.md §7`) | ✅ but new ceiling is unrequested spec addition |

Quoted spec line missing/partial:

- `R11:24` "harvest.ts source comment removal only. No behaviour change." — **violated** by `HARVEST_NODE_POLL_BUDGET` behaviour.
- `R11:30` "Documentation refactors outside `harvest.ts`" out of scope — respected (pipeline.ts/store.ts untouched, `git diff HEAD -- src/discovery/pipeline.ts` 0 lines, `src/lib/persistence/store.ts` 0 lines).
- `plan-remaining.md:216` acceptance `rg -n "getGitHead|deleted global hook" src/discovery/harvest.ts → 0` — **passes strictly**, but `src/discovery/harvest.ts:142` contains `without relying on a global hook` which matches broad `rg "global hook"` (1 hit). This is borderline stale narration vs contract — flagged as note, not fail.

Behaviour not asked for (scope creep):

- `src/discovery/harvest.ts:165-172` env-driven node budget is not in R11 brief, nor in AD-17, nor in `plan-remaining.md:205-224` M-R11 touches. It belongs elsewhere (possibly R5 cancel latency or a new R ticket) and must not ship under comment-prune lane. One-line ponytail ceiling already exists at `175` — second ceiling without measurement is speculative.

Correctness of added behaviour (if it were in scope):

- Parses `HARVEST_NODE_POLL_BUDGET` with `Number(v)` then `Number.isFinite && n>0 ? Math.floor(n) : DISCOVERY_NODE_IDS.length` and `Math.min` — edge cases handled (non-finite, ≤0, missing). Off-by-one safe (`idx < maxNodes`). Cast `DISCOVERY_NODE_IDS[idx] as string` loses `DiscoveryNodeId` narrow type vs HEAD's typed `nodeId` — minor quality regression. None of this excuses the lane violation.

---

## 3. Findings — required changes vs notes

### CHANGES_REQUIRED (must fix before APPROVED)

| # | Severity | Location | Finding | Required fix (precise) |
|---|---|---|---|---|
| F1 | **High — Spec violation / scope creep** | `src/discovery/harvest.ts:165-173` | Functional `HARVEST_NODE_POLL_BUDGET` budget loop added under comment-only ticket. Contract `R11:24` "source comment removal only. No behaviour change." violated. Plan `210` touches comment-only. This is unrequested configurability (Speculative Generality). | **Revert** `src/discovery/harvest.ts:165-173` to HEAD baseline `for (const nodeId of DISCOVERY_NODE_IDS) {` **or** move the budget feature to its owning ticket/lane with proper acceptance (env var name, limit semantics, test `HARVEST_NODE_POLL_BUDGET=2` caps `maxNodes` and `500` → full length). M-R11 diff must be either `0` (already clean, as worker correctly observed 0 stale hits) or comment-only deletions. Verify with `git diff HEAD -- src/discovery/harvest.ts` shows `0` lines or only `- // stale ...` lines. |
| F2 | **Medium — Evidence / traceability** | `.autoforge/execution/M-R11.md:5-17` | Evidence claims `No functional code changes` while working tree has functional change (F1); `Artifacts touched` lists only execution file, omits `src/discovery/harvest.ts` which is dirty (`git status --porcelain` shows `M src/discovery/harvest.ts`); validation notes say "CI should run typecheck" without local `rg` output or exit code. | Update execution file to (a) list true artifacts touched (if diff reverted, then none beyond execution file; if comment prune were needed, list `src/discovery/harvest.ts`); (b) paste verifiable proof: `rg -n "getGitHead|deleted global hook" src/discovery/harvest.ts` (exit 1, 0 hits), `rg -n "getGitHead" src/discovery/pipeline.ts src/lib/persistence/store.ts` (exit 1), and `npm run typecheck` result (0 errors for M-R11 files or acknowledge pre-existing unrelated failures at `scripts/wayfinder-tickets.ts:35`). Keep `Artifacts touched` accurate for staging hygiene audit. |
| F3 | **Medium — Staging hygiene leak (if F1 relocated)** | `git diff --stat HEAD` (11 files) | M-R11 lane shares workspace with other frontier modules (README, deployment, package-lock, discovery-doctor, provider-health, dedupe-persist, jobs, ledger). Risk of `git add -A` poisoning. | Enforce `AGENTS.md:21-23` explicit `git add .autoforge/execution/M-R11.md` (and `src/discovery/harvest.ts` only if comment prune remains) — never `git add -A`. Run `node scripts/vault-sync.mjs --check` before any `state/**` commit (not needed for this lane, but guard retained per `plan-remaining.md:59`). |

### Notes (not blocking, fix if trivial)

| # | Severity | Location | Finding | Recommendation |
|---|---|---|---|---|
| N1 | Low — ambiguous stale | `src/discovery/harvest.ts:142` `// best-effort: record the failure without relying on a global hook` | Broad `rg "global hook"` hits 1. Strict `rg "deleted global hook"` passes. Phrase describes current absence of hook, not deleted helper narration. Under `R11:21` "explains a ceiling or contract" test, this passes as contract (no global hook). But it narrates removed capability. | Keep, but add rationale in execution evidence: "line 142 describes current best-effort fallback, not deleted helper — passes ceiling-or-contract test per AD-17." If owner wants zero `global hook` mentions, reword to `// best-effort: no cross-process hook — HARVEST_LOCK is process-local per 99-103`. |
| N2 | Low — new ceiling wording | `src/discovery/harvest.ts:165` `// per-node polling ceiling: cap the number of discovery nodes polled in a single run` | If budget feature is kept elsewhere, this comment duplicates existing `175` per-node poll ceiling and `99-103` lock ceiling. Two ceilings for same loop is noise. | If budget relocates, keep only one ceiling comment per concern. If reverted (recommended for M-R11), this line disappears with F1. |
| N3 | Low — typecheck gate red globally | `scripts/wayfinder-tickets.ts:35`, `ticket-board.tsx:13` | Global `npm run typecheck` red due to `counts` shape mismatch (`ready` vs `ready_without_owner`, `hitl` vs `hitl_frontier`) — belongs to M-R19 lane, not M-R11. Worker correctly noted env may affect typecheck. | Do not block M-R11 on these; file separate fix under M-R19. For M-R11 proof, run `npx tsc --noEmit --skipLibCheck src/discovery/harvest.ts src/discovery/pipeline.ts` or at least assert harvest.ts slice typechecks. |

---

## 4. Re-run / edge-case checks performed (read-only)

- `rg -n "getGitHead" src/discovery/harvest.ts src/discovery/pipeline.ts` → `EXIT:1` (0 hits) — **reproduced worker claim**.
- `rg -n "getGitHead" src/` → `EXIT:1` (0 hits in src, hits only in `.autoforge/` docs — correctly out of scope).
- `rg -n "getGitHead|deleted global hook" src/discovery/harvest.ts` → `EXIT:1` (0 hits) — **strict R11 acceptance passes**.
- `rg -n "global hook" src/discovery/harvest.ts` → `src/discovery/harvest.ts:142` 1 hit — noted as N1.
- `rg -n "ponytail" src/discovery/harvest.ts` → `src/discovery/harvest.ts:175` 1 hit — ceiling preserved.
- `rg -n "HARVEST_LOCK" src/discovery/harvest.ts` → `:104`, `:140`, `:149`, `:254` — lock lifecycle intact.
- `git diff HEAD -- src/discovery/pipeline.ts` → 0 lines — pipeline untouched, correct per `R11:24`.
- `git diff HEAD -- src/lib/persistence/store.ts` → 0 lines — store untouched, correct.
- `git diff HEAD -- src/discovery/harvest.ts` → 11 lines (budget addition) — **F1**.
- `npm run typecheck 2>&1 | tail` → errors at `wayfinder-tickets.ts:35`, `ticket-board.tsx:13`, `__tests__/*` missing `describe` — pre-existing, not harvest-introduced.

Missed callers / siblings searched:

- Every caller of `executeJob` routes through `HARVEST_LOCK` at `99-103` and per-node poll at `175`; no sibling caller outside `src/discovery/harvest.ts` missed. The budget cap at `165` would affect all callers uniformly — root-cause vs symptom fix would be there if feature were in scope, but F1 says remove from this lane.
- No caller of removed `getGitHead` exists (verified `rg` 0 hits) — no sibling caller still broken.
- `pipeline.ts` callers of `runDiscoveryNode` unchanged; no new ceiling needed there.

---

## 5. Standards vs Spec summary

**Standards:** PASS with one speculative-generality violation (F1). All repo-documented standards respected; ponytail ladder enforced except F1's unrequested env knob. No mysterious names, duplication, or divergent change.

**Spec:** FAIL — `R11:24` No behaviour change violated by `165-173` functional addition; artifacts/evidence inaccurate (F2). `getGitHead` 0 and ceilings preserved would otherwise be faithful.

**Worst per axis:** Standards worst = F1 speculative generality (unrequested `HARVEST_NODE_POLL_BUDGET` in comment lane). Spec worst = F1 scope creep + F2 incomplete evidence.

---

## 6. What was correct (keep)

- Zero `getGitHead` hits verified independently.
- Ceilings at `src/discovery/harvest.ts:99-103` (process-local lock + upgrade to `harvest-lock.ts` SET NX) and `src/discovery/harvest.ts:175` (`ponytail: one guard in shared function`) retained.
- `src/discovery/pipeline.ts` and `src/lib/persistence/store.ts` correctly untouched (pipeline diff 0, store diff 0).
- Deletion-over-addition intent per `lean-build` and `decisions-remaining.md AD-17` (self-approve TRUE) respected in analysis — worker correctly identified no stale helper narration needed deletion when 0 hits.

---

## 7. Required actions to reach APPROVED

1. **F1 — revert or relocate:** `git checkout HEAD -- src/discovery/harvest.ts` **or** `edit src/discovery/harvest.ts:165-173` back to `for (const nodeId of DISCOVERY_NODE_IDS) {` (remove budget IIFE + `maxNodes` + indexed loop). If budget is desired, open separate ticket, implement with test `HARVEST_NODE_POLL_BUDGET=1` caps loop and `typecheck` green.
2. **F2 — fix execution evidence:** Amend `.autoforge/execution/M-R11.md` evidence summary to paste `rg` commands + exit codes, correct `Artifacts touched` (should be `[]` or `[src/discovery/harvest.ts]` only if a comment prune occurred), and note N1 rationale for line 142.
3. **Re-verify:** `rg -n "getGitHead|deleted global hook" src/discovery/harvest.ts` → 0, `git diff HEAD -- src/discovery/harvest.ts` → 0 (or comment-only), `npx tsc --noEmit --skipLibCheck` harvest slice clean, `node scripts/vault-sync.mjs --check` if `state/**` touched (not in this lane).
4. **Stage:** `git add .autoforge/execution/M-R11.md .autoforge/reviews/M-R11.md` (+ `src/discovery/harvest.ts` only if you reverted — though revert is checkout, no add needed) — never `git add -A`.

---

## 8. Artifact path

`.autoforge/reviews/M-R11.md` (this file)

**Return:** verdict `CHANGES_REQUIRED` + findings F1-F3 + notes N1-N3 + artifact path above
