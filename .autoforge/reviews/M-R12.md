# Review M-R12 — AutoForge staging: exclude `.autoforge/` from committed tree

**Reviewer:** autoforge-reviewer (independent, read-only, ponytail) — model `opencode/muse-spark-1.2-contributor-free` inherit 80k cap
**Date:** 2026-09-03
**Scope (read-only):** `.gitignore:1-46`, `.autoforge/execution/M-R12.md` (expected verbatim, not found) + ticket `workflow/wayfinder/maps/ops-residual/tickets/R12-autoforge-storage-policy.md:1-33` + `workflow/wayfinder/maps/ops-residual/MAP.md:52` R12 + architecture `decisions-remaining.md:93-101` AD-18
**Inputs sized:** execution bundle 0 lines (file missing), .gitignore 46 lines, MAP 59 lines, AD-18 9 lines
**Skills:** `code-review` (Standards vs Spec two-axis), `verify-and-stop`, `ponytail` ladder
**Mode:** read-only — no code mutated; `git check-ignore`, `git status --porcelain --ignored`, `git diff`, `git ls-files`, `rg` re-run, build inspected

---

## Verdict

**REJECTED** — worker output missing and acceptance criteria unmet. `.gitignore:1-46` contains no `.autoforge/` line, `git check-ignore -v` exits 1 (not ignored), `git status` shows 4 spurious `?? .autoforge/**` listings, `.autoforge/AGENTS.md` storage policy absent, and `.autoforge/execution/M-R12.md` evidence bundle does not exist. No drift-free proof possible. `npm run build` does pass (unrelated to this module), but the three R12-specific acceptances fail.

> If the gate prefers `CHANGES_REQUIRED` over `REJECTED` for missing implementation, treat as `CHANGES_REQUIRED` with identical required fixes below — no approval until fixes land and `git check-ignore -v` shows the ignore.

---

## 1. Acceptance re-verification (independent, cite file:line)

| Criterion (from task) | Independent check | Result | File:line |
|---|---|---|---|
| `.gitignore` contains `.autoforge/` line | `cat -n .gitignore` → `1-46` listed verbatim, no `.autoforge/` or `/.autoforge/` entry; `git show HEAD:.gitignore` identical; `git diff HEAD -- .gitignore` empty, `git diff --cached -- .gitignore` empty | ❌ FAIL | `.gitignore:1-46` — last lines `40-46` are `state/candidate-outcomes/`, `tsconfig.tsbuildinfo`, `.vercel`, `.env*`; no autoforge line |
| `git check-ignore -v` shows ignored | `git check-ignore -v .autoforge/` → exit 1, no output; `git check-ignore -v .autoforge/execution/M-R12.md` → exit 1; `git check-ignore -v .autoforge/state.json` → exit 1 | ❌ FAIL | `git check-ignore` proof 2026-09-03: empty stdout, exit 1 for all `.autoforge/**` paths |
| No drift — `git status` shows no spurious listings after pipeline | `git status --porcelain` → 4 `??` under `.autoforge/`: `?? .autoforge/requirements/grilling-deferred.md`, `?? .autoforge/validation/investigation-remaining.md`, `?? .autoforge/validation/ops-loop-remaining.json`, `?? .autoforge/validation/report-remaining.md`; `git status --porcelain --ignored` shows `.next/` `!!` (ignored) vs `.autoforge/` `??` (not ignored) contrast | ❌ FAIL | `git status` 2026-09-03 — 4 untracked, not ignored |
| Build passes | `npm run build` → `○ (Static)` prerendered, `ƒ /dev/mission-control 17.2 kB` etc., no errors (full output tails at `chunks/493` 46kB, `chunks/4bd1b696` 54kB) | ✅ PASS | Build log — succeeds (isolated from R12, but satisfies criterion) |
| `.autoforge/execution/M-R12.md` verbatim evidence exists | `ls -la .autoforge/execution/` → 8 files: `M-R5, M-R6, M-R7, M-R9, M-R10, M-R11, M-R15, M-R19` + `R*`/`VG*` — no `M-R12.md`; `find -name "*R12*"` → only `workflow/wayfinder/maps/ops-residual/tickets/R12-autoforge-storage-policy.md` | ❌ FAIL | `.autoforge/execution/M-R12.md: missing` — no worker bundle to review |
| `.autoforge/AGENTS.md` storage policy documented (ticket AC) | `ls -la .autoforge/AGENTS.md` → `No such file or directory`; `cat .autoforge/AGENTS.md` → error; `git diff HEAD -- .autoforge/AGENTS.md` empty | ❌ FAIL | `.autoforge/AGENTS.md: missing` — `R12:21-29` + `R12:25` key interface not satisfied |

**Repro:**
```bash
git check-ignore -v .autoforge/                          # → exit 1, empty (should show .gitignore:.autoforge/)
git check-ignore -v .autoforge/execution/M-R12.md        # → exit 1, empty
git status --porcelain --ignored | grep autoforge        # → ?? (not !!)
cat -n .gitignore                                        # → 1-46 no .autoforge/
ls .autoforge/execution/M-R12.md                         # → No such file
ls .autoforge/AGENTS.md                                  # → No such file
npm run build 2>&1 | tail -20                             # → ○ /dev/mission-control 17.2 kB, chunks, success
```

---

## 2. Code-review two-axis

### Standards — FAIL (one hard violation: untracked drift)

**Documented standards checked:**

- `AGENTS.md:5-18` vault determinism (HEAD worktree via `scripts/vault-sync.mjs:17` `git worktree add --detach ${tmp} HEAD`) — not touched by R12, but drift in `.autoforge/` mimics the same `??` poisoning that vault-sync is designed to prevent. Adding `.autoforge/` to `.gitignore` is the `tmp/` analogue at `AGENTS.md:38` local scratch.
- `AGENTS.md:21-23` staging hygiene (`explicit git add <paths>`, never `git add -A`) — currently respected for `.autoforge/` (no staged diff), but `??` drift invites a future `git add -A` poisoning that would commit agent dumps. This is the R12 `Current behavior` at `R12-autoforge-storage-policy.md:19`.
- Ponytail ladder: rung 1 YAGNI, rung 4 native platform feature (`.gitignore` is native git, not a new tool) — correct minimal fix is one line.

**Smell baseline (always judgement calls, repo standard overrides):**

- **No Mysterious Name** — `R12` ticket name honest; `.autoforge/` name matches dir.
- **No Duplicated Code / Data Clumps / Primitive Obsession** — not applicable (config doc change).
- **No Feature Envy / Message Chains / Middle Man / Refused Bequest** — not applicable.
- **No Shotgun Surgery / Divergent Change** — R12 diff should be `2` files (` .gitignore` + `.autoforge/AGENTS.md`) for one reason (staging policy). Current 0-file diff is not divergent, but **missing**.
- **Speculative Generality** — none (but AD-18 option (a) curated subset would be speculative if it invents a new `!.autoforge/state.json` negation without evidence — worker must pick (b) per acceptance or document why (a) deviates).
- **Standards violation — hard:** `?? .autoforge/**` drift violates the repo's implicit "no spurious `??` after pipeline" invariant at `R12:29` and `AGENTS.md` staging hygiene. Tooling (`git status --porcelain --ignored`) already enforces the distinction `??` vs `!!`; the baseline flags this as drift that will cause `vault compile determinism (V2)`-style poisoning for the next agent.

**Tracked vs ignored tension (not a smell, but a correctness note):** `git ls-files | grep autoforge` → 45 files tracked (all `.autoforge/architecture/*`, `discovery/*`, `execution/*`, `plans/*`, `reviews/*`, `state.json`, `validation/*` etc. at `git ls-files` output). Adding `.autoforge/` to `.gitignore` does **not** untrack already-tracked files — git keeps them tracked until `git rm --cached`. This means the naive one-line fix satisfies `git check-ignore -v` for *new* untracked files (the 4 `??`) but leaves the 45 already-tracked files still versioned. That is either (i) intentional curated-subset drift (AD-18 option (a)) or (ii) requires `git rm --cached -r .autoforge` if (b) is truly "exclude from committed tree". Worker must address this explicitly in `.autoforge/AGENTS.md` or the review will re-flag.

### Spec — FAIL (requirements missing + no evidence)

**Orig spec `R12-autoforge-storage-policy.md:17-32` verbatim:**

> **Summary:** Decide whether `.autoforge/` should be committed ... whether its state lives outside the worktree. **Current behavior:** `.autoforge/` shows up as `??` (untracked) ... but nothing commits those files ... Future runs in fresh clones lose historical context. **Desired behavior:** Either (a) commit a curated subset (`.autoforge/state.json`, decision docs, completed plans/reviews) ... or (b) add `.autoforge/` to `.gitignore` and lean on `artifacts/` only. Pick one ... and write it into `.autoforge/AGENTS.md`. **Key interfaces:** `.gitignore`: existing; just add or remove the ignore line. `.autoforge/AGENTS.md`: existing; add a "Storage policy" section. **Acceptance criteria:** [ ] Document the chosen path in `.autoforge/AGENTS.md`. [ ] `git status` shows no spurious listings after a typical call to the AutoForge pipeline. **Out of scope:** Migrating historical runs.

**Coverage:**

- [ ] `.gitignore` add/remove ignore line — **missing** at `.gitignore:1-46`.
- [ ] `.autoforge/AGENTS.md` Storage policy section — **missing** (`No such file`).
- [ ] `git status` no spurious listings — **fails**, 4 `??` remain.
- **No scope creep** — no extra behaviour, but also no implementation (null diff is the creep: spec demands deliberate policy pick, drift is the opposite).

**Architecture `decisions-remaining.md:93-101` AD-18:**

> **Decision (proposed, needs ratification):** Either (a) curated subset or (b) ignore `.autoforge/` entirely per `R12:21`. **Proposed default: (a) curated subset** — add `.autoforge/AGENTS.md` `## Storage policy — curated subset: commit state.json + architecture/decisions + plans/reviews + discovery/report + grilling-remaining + MAP.md mirror; extracted evidence/*.md stays untracked` and adjust `.gitignore:1-46` to keep `.autoforge/state.json` tracked but ignore `*.log` + `out/` artefacts. `git status` after pipeline shows no spurious `??` per `R12 AC:29`. **Alternative (b):** `/.autoforge/` single line ... but fresh clone loses `.autoforge/state.json:1-28` execution ledger (Wayfinder `workflow/wayfinder/maps/**` still committed so frontier not lost). **Self-approve:** **FALSE — NEEDS REVIEW (HITL-policy)**.

- Task acceptance says (b) is the expected default (`.gitignore` contains `.autoforge/`), contradicting AD-18's proposed (a). Worker was `blocked_by M-R12 worker` per task, so must resolve this conflict explicitly: either honor task acceptance (b) and note AD-18 escalation, or pick (a) with owner sign-off and update acceptance. Silent drift picks neither — fails both.

**What would be correct (ponytail ladder):**

- Rung 4 — native `.gitignore` line: one line `.autoforge/` (or `/.autoforge/` — both ignore at root; `/.autoforge/` is tighter to root, `.autoforge/` ignores any depth but repo has only one; either passes `git check-ignore -v`).
- Rung 2 — reuse existing `.autoforge/state.json` seam if (a) chosen: keep `!.autoforge/state.json` negation after ignore line.
- Skipped: custom staging script, `stages/` migration, historical run migration — correctly `Out of scope` at `R12:31`.

**Worst per axis:**

- **Standards worst:** Drift `?? .autoforge/**` hard violation — 4 untracked files will be poisoned by parallel sessions / `git add -A`, mirroring the vault determinism poison described at `AGENTS.md:15-18`.
- **Spec worst:** Missing worker artifact `.autoforge/execution/M-R12.md` + missing `.autoforge/AGENTS.md` + missing `.gitignore` line — zero of 3 required interfaces delivered.

---

## 3. Findings — required changes vs notes

### REJECTED (must fix before APPROVED)

| # | Severity | Location | Finding | Required fix (precise) |
|---|---|---|---|---|
| F1 | **High — Spec / Acceptance** | `.gitignore:1-46` | No `.autoforge/` line; `git check-ignore -v .autoforge/` exit 1, empty | **Add one line** to `.gitignore`. Ponytail minimal: append at `47` after `46: .env*` — `echo ".autoforge/" >> .gitignore` or edit to insert ` .autoforge/` under `# Local scratch` at `38` adjacent to `tmp/` (either location passes `check-ignore`). Exact line: `.autoforge/` (preferred per acceptance) or `/.autoforge/` (equivalent root-anchored II). Verify with `git check-ignore -v .autoforge/state.json` → `.gitignore:47:.autoforge/` |
| F2 | **High — Spec / Documentation** | `.autoforge/AGENTS.md: missing` | Ticket `R12:21` + `R12:25` requires `Storage policy` section in `.autoforge/AGENTS.md`; AD-18:95 proposes wording but file absent | **Create `.autoforge/AGENTS.md`** with at minimum: `## Storage policy` header + one paragraph picking (b) per task acceptance: `/.autoforge/ is ignored via .gitignore — execution ledger lives in workflow/wayfinder/maps/** and state/vault-notes.json; .autoforge/ is local pipeline scratch, not committed.` If choosing AD-18 (a) curated subset instead, document that and adjust `.gitignore` to ` .autoforge/` + `!.autoforge/state.json` + `!.autoforge/architecture/decisions*` etc., and obtain owner sign-off for deviation from task acceptance. Cite `R12:21` and `AD-18:93-101` in the file. |
| F3 | **High — Evidence / Traceability** | `.autoforge/execution/M-R12.md: missing` | Task Input says verify `M-R12.md` verbatim + `.gitignore` diff; `ls .autoforge/execution/` has no `M-R12.md`; `git ls-files | grep autoforge` no entry; no worker proof of `check-ignore` or `git status` | **Create `.autoforge/execution/M-R12.md`** execution bundle (verbatim id, title `R12 AutoForge staging — exclude .autoforge/`, touches `[.gitignore, .autoforge/AGENTS.md]`, locks `[gitignore-single-writer]` per `decisions-remaining.md:168`, wave, blocked_by, acceptance checklist with `git check-ignore -v` output and `git status --porcelain --ignored` snippet). Must include pasted `check-ignore` exit 0 proof. |
| F4 | **Medium — Drift / Staging hygiene** | `git status --porcelain` 4 `??` | Until F1 ships, every AutoForge pipeline leaves `?? .autoforge/requirements/grilling-deferred.md` etc. Parallel sessions will see foreign untracked and risk `vault` determinism-style poisoning (`AGENTS.md:15-18` worktree rationale). Already-tracked 45 `.autoforge/**` files remain tracked even after ignore — see note below. | After F1, run `git status --porcelain --ignored` and confirm `!! .autoforge/` or no `??` for new files. For already-tracked files, document decision in `.autoforge/AGENTS.md`: either (b) `git rm --cached -r .autoforge && git add .gitignore .autoforge/AGENTS.md` to truly exclude (requires owner ack that history retains them but new clones lose `.autoforge/state.json`), or (a) curated subset with `!` negations. Do not `git add -A`. |
| F5 | **Medium — Architecture HITL conflict** | `decisions-remaining.md:101` `Self-approve: FALSE — NEEDS REVIEW (HITL-policy)` vs `R12-autoforge-storage-policy.md:5` `hitl: false` + task `blocked_by M-R12 worker` | AD-18 marks AutoForge staging as HITL-policy (owner boundary `CHARTER.md:82-85` analogue), but ticket is `hitl: false` and would be self-approvable. Worker cannot self-approve picking (a) vs (b) without owner ratification if AD-18 is authoritative. | **Escalate choice** in review: note that task acceptance demands (b) `.autoforge/` ignore, but AD-18 proposes (a) curated subset default and flags HITL. Worker must pick (b) to satisfy task, or escalate to owner if (a) preferred. Record decision and owner ack in `.autoforge/AGENTS.md` and in `M-R12.md` reconciliation. |

### Notes (not blocking, fix if trivial)

| # | Severity | Location | Finding | Recommendation |
|---|---|---|---|---|
| N1 | Low — pattern variant | `.gitignore:47` (future) | `/.autoforge/` vs `.autoforge/` both pass `check-ignore` at root; `/.autoforge/` is root-anchored, `.autoforge/` matches any depth (repo has only one). Acceptance says `.autoforge/` (no leading slash). | Keep `.autoforge/` for exact acceptance string; either passes `git check-ignore -v` but prefer `.autoforge/` to match `MAP.md:52`/`R12:21` wording. |
| N2 | Low — tracked files remain | `git ls-files \| grep autoforge` 45 hits | If (b) is chosen, already-tracked `.autoforge/**` will stay versioned until `rm --cached`. This is git semantics, not a bug, but will surprise fresh-clone expectation that `frontier not lost` via `workflow/wayfinder/maps/**` at `AD-18:97`. | Mention in `.autoforge/AGENTS.md` that `WAYFINDER_MAPS` remains canonical (`workflow/wayfinder/maps/ops-residual/MAP.md:39-59`) so frontier not lost even if `.autoforge/state.json` becomes local-only. |
| N3 | Low — build gate unrelated | `npm run build` | Build passes now, but `M-R11` and `M-R19` previously introduced unrelated drifts (`HARVEST_NODE_POLL_BUDGET`, `ticket-board.tsx:8` import). R12 must not claim those lanes. | Stage only `git add .gitignore .autoforge/AGENTS.md .autoforge/execution/M-R12.md` — never `git add -A` per `AGENTS.md:21-23`. |

---

## 4. Re-run / edge-case checks performed (read-only)

- `cat -n .gitignore` → `1-46` lines, no `.autoforge/` — **reproduced**.
- `git show HEAD:.gitignore | cat -n` → identical `1-46` — HEAD also missing.
- `git diff HEAD -- .gitignore` → empty; `git diff --cached -- .gitignore` → empty — no staged fix.
- `git ls-files --error-unmatch .gitignore` → `.gitignore` tracked.
- `git check-ignore -v .autoforge/` → exit 1, empty — not ignored.
- `git check-ignore -v .autoforge/execution/M-R12.md` → exit 1 — worker artifact not ignored (and missing).
- `git check-ignore -v .autoforge/state.json` → exit 1 — not ignored.
- `git status --porcelain --ignored` → `?? .autoforge/requirements/grilling-deferred.md`, `?? .autoforge/validation/investigation-remaining.md`, `?? .autoforge/validation/ops-loop-remaining.json`, `?? .autoforge/validation/report-remaining.md` — `??` not `!!`, proof of non-ignore; contrast `!! .next/`, `!! node_modules/` etc. correctly ignored.
- `ls .autoforge/execution/M-R12.md` → `No such file`.
- `ls .autoforge/AGENTS.md` → `No such file`.
- `find . -name "*R12*"` → only `workflow/wayfinder/maps/ops-residual/tickets/R12-autoforge-storage-policy.md` — no execution artifact.
- `git ls-files | grep autoforge` → 45 tracked files (architecture, discovery, execution, plans, reviews, state.json, validation) — tracked set will survive ignore until `rm --cached`.
- `npm run build 2>&1 | tail` → chunks `493-a9bce8b61dc17c96.js 46.4 kB`, `4bd1b696 54.2 kB`, `○ /dev/mission-control 17.2 kB` — green.
- `rg "autoforge" .gitignore` → 0 hits — confirms no line.
- `rg -n "Storage policy" .autoforge/` → 0 hits — confirms no policy doc.
- Missed callers / siblings: every agent dump targets `.autoforge/**` (`discovery/report.md`, `plans/`, `execution/`, `reviews/`, `validation/`, `requirements/`); all route through `.gitignore` single seam — fixing one line ` .autoforge/` guards all siblings uniformly (root-cause fix, not per-caller). No caller outside `.autoforge/` missed; `state/candidate-outcomes/` at `.gitignore:41` is the analogous pattern already ignored.

---

## 5. Standards vs Spec summary

**Standards:** FAIL — one hard drift violation: `?? .autoforge/**` spurious listings violate `R12:29` + `AGENTS.md:21-23` staging hygiene; `git status --porcelain --ignored` enforces `??` vs `!!` and proves non-ignore. No other hard violations; ponytail ladder would resolve with one native `.gitignore` line.

**Spec:** FAIL — `R12:21` deliberate policy pick missing (no `.gitignore` line, no `.autoforge/AGENTS.md` section), `R12:29` `git status` spurious listings remain, `.autoforge/execution/M-R12.md` evidence bundle missing — zero of three required interfaces delivered. AD-18 HITL-policy conflict unresolved.

**Worst per axis:** Standards worst = F1+F4 drift + `git check-ignore` exit 1 (hard). Spec worst = F2+F3 missing policy doc + missing execution bundle (high).

---

## 6. What was correct (keep)

- `npm run build` green — no regression from R12 lane.
- `git ls-files` tracking of 45 `.autoforge/**` files is intact (no accidental untracking or `git rm` yet).
- `workflow/wayfinder/maps/ops-residual/tickets/R12-autoforge-storage-policy.md:17-32` brief is correctly scoped (2 interfaces, 2 AC, out-of-scope `Migrating historical runs` respected).
- Staging hygiene currently clean — no `git add -A` poisoning, diff empty, so fix can be applied surgically.

---

## 7. Required actions to reach APPROVED

1. **F1 — one-line ignore:** `edit .gitignore:46` append ` .autoforge/` (new `47: .autoforge/`). Alt: `printf "\n.autoforge/\n" >> .gitignore` then `cat -n .gitignore` verify `47: .autoforge/`. Do not use bare `node scripts/vault-import.mjs` before committing (use `node scripts/vault-sync.mjs --check` per `AGENTS.md:15-18` if `state/vault-notes.json` touched — not needed for this lane, but guard retained).
2. **F2 — storage policy doc:** `write .autoforge/AGENTS.md` with at minimum `## Storage policy` picking (b) per acceptance, e.g. `Ignored: /.autoforge/ via .gitignore:47. Wayfinder maps remain canonical at workflow/wayfinder/maps/**. AGENTS.md cites R12:21 and AD-18.` If (a) curated subset is desired, add `!` negations in `.gitignore` and obtain owner ack.
3. **F3 — execution bundle:** `write .autoforge/execution/M-R12.md` with id `M-R12`, touches `[.gitignore, .autoforge/AGENTS.md]`, locks `[gitignore-single-writer]` at `decisions-remaining.md:168`, wave, and pasted proof: `git check-ignore -v .autoforge/execution/M-R12.md` → `.gitignore:47:.autoforge/` and `git status --porcelain --ignored` shows `!!` not `??`.
4. **F4 — verify drift-free:** `git check-ignore -v .autoforge/state.json` → `.gitignore:47:.autoforge/` (exit 0), `git status --porcelain` shows 0 `?? .autoforge/**` for new pipeline runs (already-tracked 45 files remain as `git ls-files` until/unless `git rm --cached -r .autoforge` is explicitly chosen and documented — do not rm without owner ack).
5. **Stage:** `git add .gitignore .autoforge/AGENTS.md .autoforge/execution/M-R12.md` — never `git add -A` per `AGENTS.md:21-23`. Then `git status --porcelain` should show `A`/`M` only for those 3 paths, no `??`.

---

## 8. Artifact path

`.autoforge/reviews/M-R12.md` (this file)

**Return:** verdict `REJECTED` (equivalently `CHANGES_REQUIRED` if gate prefers) + findings F1-F5 + notes N1-N3 + artifact path above. Re-review after F1-F3 land and `git check-ignore -v` shows ignored + `git status` drift-free.
