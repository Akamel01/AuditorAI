# Review R10 — `state/dedupe-index.json` KV-truth — Re-review after fix

**Verdict: CHANGES_REQUIRED** (partial fix landed — 2/3 objectives verified, DataStore seam still missing)

## Scope verified (task brief)
- `src/discovery/dedupe-persist.ts:16-65` — serialized `writeQueue` + WARN token + `readFromKVSync` seam + ponytail tags
- `src/lib/persistence/keys.ts` — `DISCOVERY_DEDUPE_INDEX_KEY` + `DEDUPE_INDEX_PATH`/`dedupeKvKey`
- `.autoforge/execution/R10.md` — artifact claims
- Plan acceptance `plan.md:151-160` — KV-first when available, file seed fallback with WARN, no silent file truth when KV present

## Acceptance (plan.md:151-160)
- Objective: KV-truth when `KV_REST_API_URL` present; break file/KV fork under ROFS.
- Seam: `dedupe-persist.ts:16-72` KV-first load/persist + file best-effort `EROFS → WARN`.
- Touches: [`src/discovery/dedupe-persist.ts`, `src/lib/persistence/keys.ts`] Hazard `src/lib/persistence/**`, `state/**` Locks `persistence-single-writer` + `vault-state-single-writer` Wave B
- Acceptance: [~] ROFS throw → KV succeeds + WARN token (WARN now present, KV via `persistDedupeFromResult` only); [~] local dev writes both (only via `persistDedupeFromResult` path); [ ] no silent file truth when KV present — **FAIL** (see below)

## Evidence inspected (post-fix diff `5d12c45..81b17bb`)

- `src/discovery/dedupe-persist.ts:16-17` — `// ponytail: EROFS ceiling — KV-first with file seed fallback; per-account locks if throughput matters` + `let writeQueue: Promise<void> = Promise.resolve()` ✅ **verified** — serialized queue exists
- `src/discovery/dedupe-persist.ts:50-51` — `// ponytail: serialized queue is single-writer; global lock ceiling` + `writeQueue = writeQueue.then(() => {` ✅ **verified** — queue is used for every `saveDedupeIndex` call, `.catch(()=>{})` prevents poison
- `src/discovery/dedupe-persist.ts:57-61` — `catch(e){ const msg=...; if(msg.includes("EROFS")||msg.includes("ENOENT")||msg.includes("read-only")) console.warn("WARN dedupe persist EROFS/ENOENT — KV-truth kept, file seed skipped: ...")}` ✅ **verified** — WARN token present, matches spec `EROFS → WARN`, `grep WARN.*dedupe` 1 hit, `grep -c WARN` was 0 before fix now 1
- `src/discovery/dedupe-persist.ts:19-26` — `readFromKVSync(): DedupeIndexDoc|null { try{ const g=globalThis as {__KV_DEDUPE_INDEX__}; if(g?.__KV_DEDUPE_INDEX__) return ...} }` ❌ **FAIL — global hack still, no DataStore seam** — does NOT call `getDataStore().get(DISCOVERY_DEDUPE_INDEX_KEY)`; comment says "use DataStore seam where available" but code never imports/calls it. `store.ts:16-26` has no `getSync`, so sync KV read must be async variant. This is the same `globalThis.__KV_DEDUPE_INDEX__` stub flagged in prior review :13.
- `src/discovery/dedupe-persist.ts:28-32` — `loadDedupeIndex` does `const kv=readFromKVSync(); if(kv) return kv;` before file fallback ✅ KV-first *intent* but only via fake global — in real Vercel/MemoryStore production, `s.get(DISCOVERY_DEDUPE_INDEX_KEY)` is never consulted, so **silent file truth when KV present** still occurs. Spec requires `await s.get(DISCOVERY_DEDUPE_INDEX_KEY)` else file seed (`architecture/report.md:143`).
- `src/discovery/dedupe-persist.ts:66-94` — `persistDedupeFromResult` does `saveDedupeIndex(index,cwd)` (file queued with WARN) then `await s.put(DISCOVERY_DEDUPE_INDEX_KEY,index)` best-effort ✅ **verified** — that path writes both, KV-first for that caller. But `saveDedupeIndex` direct callers (any future import) still file-only, no KV `put`. Previous review required `saveDedupeIndex` itself to be KV-first + file best-effort or be renamed `saveDedupeIndexKV`.
- `src/lib/persistence/keys.ts:50-55` — `DISCOVERY_DEDUPE_INDEX_KEY="discovery:dedupe-index"` + `DEDUPE_INDEX_PATH` + `dedupeKvKey(name)` ✅ present but `grep dedupeKvKey` 0 hits outside keys.ts — **dead code** (LOW), retains prior finding.
- `.autoforge/execution/R10.md:6-8` — claims "KV-first load/write using host DataStore if available; otherwise fall back ... Serialized writes to avoid race conditions via an in-module queue." ✅ queue + WARN claims now true, but "KV-first load/write using host DataStore" still overclaims for the `load` path (only `persist` uses DataStore).
- `npm run typecheck` — green ✅ (post-fix still green)
- `tests/discovery/**` — still 0 files ❌ **no runnable check** for `EROFS → WARN` + KV success (prior finding :27 still open)

## What the fix got right (close these findings)
- **Serialized writeQueue — FIXED:** `writeQueue` at :17 serialized at :51 satisfies `plan.md:154` + `work-order.json:25` `persistence-single-writer` hazard (global lock ceiling documented). Prior finding :17/:34 **resolved**.
- **WARN token — FIXED:** `console.warn("WARN dedupe persist EROFS/ENOENT — ...")` at :60 with `EROFS|ENOENT|read-only` guard satisfies `EROFS → WARN` token (`architecture/report.md:143`, `plan.md:158`). Prior finding :15/:31 **resolved**. Token is searchable (`WARN dedupe persist`).
- **Ponytail tags — FIXED:** `// ponytail: EROFS ceiling` at :16 and `// ponytail: serialized queue is single-writer; global lock ceiling` at :50 + `// ponytail: sync KV read is accepted ceiling` at :20 satisfy `architecture/report.md:169-173` ceiling-tag requirement. Prior finding :13 ponytail note now **resolved** (tags present).

## Remaining findings (block verdict)

1. **Read is not KV-first via DataStore seam (HIGH — blocks acceptance):** `readFromKVSync` must use DataStore seam, not `globalThis.__KV_DEDUPE_INDEX__`. `DataStore` is async-only (`store.ts:16-26` `get<T>:Promise<T|null>`), so a sync `loadDedupeIndex` cannot be truly KV-first without an async variant. Current code will return file content on Vercel even when KV holds newer truth → fork not broken. `plan.md:154` KV-first + `Acceptance: no silent file truth when KV present` fails.
2. **Save is not KV-first (HIGH):** `saveDedupeIndex` queues only `writeFileSync`. Direct callers never `put` to KV. The KV `put` lives only in `persistDedupeFromResult:91-93`. If spec intends every `saveDedupeIndex` to be KV-truth, it must `await s.put` before file best-effort (or document that only `persistDedupeFromResult` is KV-truth and `saveDedupeIndex` is file-mirror-only).
3. **Dead code (LOW):** `keys.ts:53-55 dedupeKvKey` unused — either delete or wire to `DISCOVERY_DEDUPE_INDEX_KEY` (e.g., `export const dedupeKvKey = () => DISCOVERY_DEDUPE_INDEX_KEY` or remove).
4. **Missing runnable check (MED):** Still no `tests/discovery/dedupe-persist.test.ts` proving `EROFS → WARN` while KV `put` succeeds (mock `writeFileSync` throw, spy `console.warn` + `MemoryStore.put`). Prior required change :42 still open. Lean-build one-file `vitest` is sufficient.

## Required changes (precise, minimal)

- [ ] `src/discovery/dedupe-persist.ts:19-26` — Replace `readFromKVSync` global-hack with DataStore-backed path. Minimal compliant options (pick one, document ceiling):
  - **Option A (preferred, smallest diff):** Keep `loadDedupeIndex(cwd?)` sync for file-seed compatibility, add:
    ```ts
    export async function loadDedupeIndexKV(store?: DataStore, cwd?: string): Promise<DedupeIndexDoc> {
      try { const s = store ?? getDataStore(); const kv = await s.get<DedupeIndexDoc>(DISCOVERY_DEDUPE_INDEX_KEY); if (kv) return kv; } catch {}
      return loadDedupeIndex(cwd);
    }
    ```
    and call `loadDedupeIndexKV` from `persistDedupeFromResult:74` (or any caller that has a store). Keep `readFromKVSync` only as deprecated shim or remove it; if kept, make it delegate to an in-memory sync mirror populated by `persistDedupeFromResult` and tag `// ponytail: sync KV read is accepted ceiling — async loadDedupeIndexKV is truth`.
  - **Option B:** Make `readFromKVSync` attempt sync read from `MemoryStore` singleton when `getDataStore().kind==="memory"` by exposing a test-only `__getSyncForTests` seam — heavier, not recommended.

- [ ] `src/discovery/dedupe-persist.ts:49-64` — Make `saveDedupeIndex` KV-first OR document its scope. Minimal:
  ```ts
  export function saveDedupeIndex(doc: DedupeIndexDoc, cwd?: string, store?: DataStore): void {
    const s = store ?? getDataStore();
    s.put(DISCOVERY_DEDUPE_INDEX_KEY, doc).catch(()=>{});
    writeQueue = writeQueue.then(()=>{ try{ writeFileSync(...) } catch(e){ if(EROFS...) console.warn(...)} }).catch(()=>{});
  }
  ```
  Alternatively, rename/comment: `// file-mirror only; KV truth via persistDedupeFromResult` and ensure no external caller relies on it for KV truth.

- [ ] `src/lib/persistence/keys.ts:53-55` — Delete `dedupeKvKey` or make it `export const dedupeKvKey = () => DISCOVERY_DEDUPE_INDEX_KEY` and use it in `dedupe-persist.ts`.

- [ ] Add `tests/discovery/dedupe-persist.test.ts` (one runnable check):
  - Mock `node:fs` `writeFileSync` to throw `EROFS: read-only file system`,
  - Call `saveDedupeIndex(doc)` + `await new Promise(r=>setTimeout(r,0))` to flush queue,
  - Assert `console.warn` called with `WARN dedupe persist EROFS`,
  - Assert `MemoryStore.get(DISCOVERY_DEDUPE_INDEX_KEY)` still holds `doc` when using `persistDedupeFromResult` with injected `MemoryStore`.

- [ ] Re-run `npm run typecheck && npx vitest run tests/discovery/dedupe-persist.test.ts` and paste output into `.autoforge/execution/R10.md` evidence plan.

## Ponytail / seam re-check
- **Ladder rung 2 (reuse DataStore):** `saveDedupeIndex` still file-only — should reuse `DataStore.put` seam at `store.ts:18` (one line `s.put(...)`). Cost: +2 lines.
- **Serialized writes:** ✅ now satisfies `persistence-single-writer` via `writeQueue`.
- **EROFS ceiling:** ✅ tagged at :16 and :50.
- **Sync KV ceiling:** ⚠️ tagged at :20 but tag documents a non-seam; accepted only if async `loadDedupeIndexKV` is added alongside.

## Notes
- Vault determinism untouched (`state/vault-notes.json` not written by this module) — hazard `vault-state-single-writer` still applies due to `state/**` write, correctly flagged in `work-order.json:47`.
- Execution artifact `.autoforge/execution/R10.md:6-8` should be tightened to "load via `loadDedupeIndexKV` is KV-first; sync `loadDedupeIndex` is file-seed fallback with `ponytail: sync ceiling`" once async variant lands, to avoid overclaim.

---
*Reviewer: autoforge-reviewer (muse-spark-1.2 80k cap, ponytail full) — Re-review 2026-09-02, diff `5d12c45..81b17bb`, typecheck green, no new deps, 2/5 prior HIGH findings resolved (WARN + queue + ponytail), 2 HIGH + 1 MED remain (KV-first load/save + test).*
