# Review R13 — Eval freshness automation

**Verdict:** `CHANGES_REQUIRED`

**Scope:** `scripts/check-eval-gate-freshness.mjs`, `.github/workflows/ci-gates.yml`, `docs/validation/eval-gates.md`, `.autoforge/execution/R13.md`, `plan.md:178-187`, `workflow/wayfinder/maps/ops-residual/tickets/R13-eval-gate-freshness.md`

## Findings

### 1. Threshold parsing never matches doctrine — script always fails (BLOCKER)
- `scripts/check-eval-gate-freshness.mjs:14` regex: `/R13[\s\S]*?threshold\s*[:=]\s*([0-9]+(?:\.[0-9]+)?)/i`
- `docs/validation/eval-gates.md:27-34` threshold table contains `Corpus pass mark ≥ 90 %`, `per-finding minimums`, `zero-drop` — **no line containing `R13` + `threshold = 0.75`**. Grep confirms 0 hits for `threshold` tied to `R13` (only `When a scorecard is below threshold` at line 56).
- Repro: `node scripts/check-eval-gate-freshness.mjs` → `R13 threshold not found in eval-gates.md` exit 1 on HEAD (verified `a715ee8`). Gate would fail permanently in CI, not stale/fresh gated.

### 2. Missing staleness semantics vs plan acceptance
- Plan `plan.md:180-186` acceptance: *CI fails when Tier-1 archive >7d; threshold sourced from doctrine; msg `run tier1 --topup`*. Execution doc `.autoforge/execution/R13.md:17` claims `exit 0 if threshold parsed`. Actual script is read-only reporter (`scripts/check-eval-gate-freshness.mjs:22-23` `console.log` threshold only) — does not inspect `state/eval-scorecards/` mtime, does not compare to 7d, does not emit the required actionable message. Ticket `R13-eval-gate-freshness.md:22-24` explicitly requires reading `state/eval-scorecards/` last-modified and failing with actionable message.
- `state/eval-scorecards/` newest dir `2026-08-25T19-54-07-248Z` (7 days old at 2026-09-01) would be stale under 7d rule — script cannot detect.

### 3. Workflow filename drift
- Execution `.autoforge/execution/R13.md:10` cites `.github/workflows/ci.yml`; actual file is `.github/workflows/ci-gates.yml:1` (`Gate: R13 eval freshness & R17 evidence HEAD`). `ci.yml` still exists but gate lives in `ci-gates.yml`. Not a functional blocker but doc drift; review aligns to `ci-gates.yml:20-21` which does wire `node scripts/check-eval-gate-freshness.mjs`.

### 4. No test evidence for fresh/stale (per ticket AC)
- Ticket requires test case for fresh vs stale inputs. No `tests/**` or script self-test covers mtime logic (script has none).

## Acceptance matrix (plan.md:186)

| Criterion | Status |
|---|---|
| Threshold lives outside script, sourced from `eval-gates.md` | ❌ script looks for non-existent pattern, not §3 table (§3 is 90% corpus mark, not a 7d freshness knob) |
| Fresh → pass, stale → fail with `run tier1 --topup` | ❌ no mtime check; always fails with `R13 threshold not found` |
| Doctrine authority preserved | ❌ invents `R13: threshold =` syntax absent from doctrine |

## Ponytail check

- Ladder: stdlib `fs.stat` + `Date.now()` would suffice; adding a threshold constant parse is unnecessary abstraction when doctrine defines freshness as 7d (ticket). Current script adds regex complexity for a threshold that doesn't exist — deletion candidate. **Keep threshold at literal `7 * 24*3600*1000` or single `EVAL_FRESHNESS_DAYS` env default 7; source comment cites `eval-gates.md:14-25` §2 trigger paths, not a numeric `R13:` line.** Skipped: custom parser class — add only when doctrine actually publishes `R13: threshold =` in eval-gates.

## Required changes

1. **Fix `scripts/check-eval-gate-freshness.mjs:12-20`**: remove `R13.*threshold` regex. Implement:
   ```js
   const MAX_AGE_MS = 7*24*60*60*1000; // doctrine §2 freshness, cite eval-gates.md:14-25
   const scorecardsDir = path.resolve(__dirname,'../state/eval-scorecards');
   // stat most recent subdir mtime; if none -> fail stale
   // if Date.now()-mtime > MAX_AGE_MS -> console.error('eval gate stale: last archive ... — run: npm run eval -- --topup <runId>') exit 1 else exit 0
   ```
   Optionally parse `90 %` from `eval-gates.md:31` if threshold truly means corpus pass mark, but freshness gate is age-based per `plan.md:180` description — keep age as primary.

2. **Align execution doc** `.autoforge/execution/R13.md:17-18` to describe age check and correct workflow `ci-gates.yml:20-21`, or move gate into `ci.yml` if intended.

3. **Add minimal test** (or script self-check) covering fresh (<7d) and stale (>7d) as ticket AC requires — `tests/eval-gate-freshness.test.ts` using `fs.stat` mocked or temp dirs.

4. **CI message** must contain `topup` token (plan `R14` adjacency) for operator action.

## Evidence

- `scripts/check-eval-gate-freshness.mjs:14` regex, `docs/validation/eval-gates.md:27-34` table, `plan.md:182` seam, `workflow/wayfinder/maps/ops-residual/tickets/R13-eval-gate-freshness.md:18-24` AC, run reproducer `node scripts/check-eval-gate-freshness.mjs → exit 1`.

## Risk if shipped as-is

CI gate in `ci-gates.yml:21` would permanently red, blocking merges despite fresh archives — violates frozen doctrine intent (eval-gates gate is release-blocking on trigger paths only, not every PR).
