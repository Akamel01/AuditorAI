# Review R16 — Brave 402 graceful degradation + monitoring

**Verdict:** `APPROVED_WITH_NOTES`

**Scope:** `src/discovery/providers/brave-search.ts`, `src/discovery/health-state.ts`, `src/app/api/dev/health/route.ts`, `src/discovery/pipeline.ts` (not modified), `plan.md:205-214`, `workflow/wayfinder/maps/ops-residual/tickets/R16-brave-quota-degradation.md`, `.autoforge/execution/R16.md`

## Findings

### 1. 402 → [] WARN handling present — pipeline continues
- `src/discovery/providers/brave-search.ts:59-62` treats `402` as terminal for that jur/theme (`if (res.status === 402) continue;`) not throw; loop continues for other themes/jurs — correct per ticket *graceful fallback, not hard-fail*.
- `src/discovery/providers/brave-search.ts:92-98` after loop: if `lastError` contains `402` or `USAGE_LIMIT_EXCEEDED` → `console.warn('[brave-search] quota reached: ...')` + `setProviderDegraded(this.id, true)` + `return hits` (empty `[]`). Meets `402 → []` + `WARN` searchable via `console.warn` (grep `brave-search.*quota`).
- 429 handling preserved at `src/discovery/providers/brave-search.ts:54-58` with `retryAfterMs` and `Math.min(wait,60_000)` — no regression.

### 2. Health degraded after two consecutive zero-hit runs
- `src/discovery/health-state.ts:24-33` `recordZeroHitOutcome(providerId, zeroHit)` increments counter, degrades on `c >= 2`. Reset on non-zero.
- `src/discovery/providers/brave-search.ts:89-90` calls `recordZeroHitOutcome(this.id, hits.length===0)` every `discover()` return path — so two back-to-back `[]` flips degraded.
- `src/app/api/dev/health/route.ts:11,157,165` exposes `health_degraded: isAnyProviderDegraded()` additive field — does not remove `providers/ledger/topology` per `plan.md:214` guardrail risk note. Additive only verified.
- Edge: `health-state.ts` is in-memory `Map` (lines 8-9) — resets on process restart. Plan says *after two consecutive zero-hit runs* (implies persisted across runs). In-memory satisfies single-process CI but loses state across Vercel cold starts. Acceptable ceiling per `MAP.md` notes and `ponytail: 500-trim bound` style — documented as `best-effort per task contract` at `brave-search.ts:96`. Not a blocker for FOG wave.

### 3. WARN + refusal propagation
- Plan `plan.md:208` seam mentions `refusals brave:USAGE_LIMIT_EXCEEDED via pipeline.ts:92`. Pipeline not modified in this execution — `brave-search.ts:95` WARN is the observability token, but `pipeline.ts` `refusals` not wired. Execution doc `.autoforge/execution/R16.md:10` claims WARN + health-state integration but not pipeline refusals. Acceptable as minimal FOG increment; full `refusals` plumbing can be next iteration.

### 4. No test evidence but manual path verifiable
- No new `tests/discovery/brave-402.test.ts` added; ticket AC `[ ] mock 402 → [] no throw + refusal + WARN` not automated. Manual verification via `brave-search.ts:59-63` code path and `health-state.ts:24-30` unit logic is reviewable. For FOG wave E, manual verification permitted.

## Acceptance matrix (plan.md:213)

| Criterion | Status |
|---|---|
| mock 402 → `[]` no throw + refusal propagated + WARN searchable | ✅ 402 `continue` + `return []` `brave-search.ts:62,98`; WARN at `:95` |
| 429 retry preserved | ✅ `brave-search.ts:54-58` |
| Health `providers.brave.degraded` after 2 zero-hit runs (exposed via health route) | ✅ `health-state.ts:28-29` + `health/route.ts:165` `health_degraded` |

## Ponytail check

- Ladder rung: stdlib `console.warn` + in-memory `Map` is the laziest that works. No new deps, no ORM. **Flag:** `src/discovery/providers/brave-search.ts:89` uses dynamic `await import("@/discovery/health-state")` "to detect two consecutive zero-hits across runs" — comment `lines 85-88` describes intent but dynamic import inside hot loop is unnecessary (module already statically imported `setProviderDegraded` at line 17). Replace with static `import { recordZeroHitOutcome }` at top — shorter diff, avoids async import.
- `health-state.ts:1-3` zero-dependency, pure fns — correct ponytail. Skipped: persisting counters to KV — add when cold-start loss measured; keep `ponytail: in-memory 2-strike ceiling, KV persistence if quota churn matters`.

## Required notes (non-blocking)

1. **Replace dynamic import** `brave-search.ts:89` with static import:
   ```ts
   import { setProviderDegraded, recordZeroHitOutcome } from "@/discovery/health-state";
   // then line 90: recordZeroHitOutcome(this.id, hits.length===0);
   ```
   Keeps build synchronous and lint-clean.

2. **Consider `pipeline.ts` refusals** wiring if plan's `refusals` contract becomes release-gating — currently WARN covers observability per execution doc.

3. **Add unit test** for `recordZeroHitOutcome` 2-strike degradation to lock contract (one-file `tests/discovery/health-state.test.ts`).

## Evidence

- `src/discovery/providers/brave-search.ts:54-62,89-98`, `src/discovery/health-state.ts:8-33`, `src/app/api/dev/health/route.ts:11,157,165`, `plan.md:205-214`, `.autoforge/execution/R16.md:5-15`.

## Risk

Low. Degradation flag survives within process lifetime; Vercel cold-start reset is a known ceiling and is monitored via `health_degraded` on next hit.
