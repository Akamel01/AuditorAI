# Review R17 — Evidence HEAD anchoring automation

**Verdict:** `CHANGES_REQUIRED`

**Scope:** `scripts/check-evidence-head.mjs`, `.github/workflows/ci-gates.yml:22-23`, `state/**`, `.autoforge/validation/ops-loop-evidence.json`, `stages/07_validate/output/ops-loop-evidence.json`, `plan.md:216-224`, `workflow/wayfinder/maps/ops-residual/tickets/R17-evidence-reconciliation-automation.md`

## Findings

### 1. Wrong evidence file path — script never finds evidence (BLOCKER)
- `scripts/check-evidence-head.mjs:11` reads `state/evidence-head.json`. That file does not exist on HEAD (`a715ee8`): `cat state/evidence-head.json → No such file`. Canonical evidence is ` .autoforge/validation/ops-loop-evidence.json` (commit `056b75fd...` at `state.json` evidence) and its twin `stages/07_validate/output/ops-loop-evidence.json`. Expected seam per plan `plan.md:219` and `work-order.json` global guardrail `cmp -s .autoforge/validation/ops-loop-evidence.json stages/07_validate/output/ops-loop-evidence.json` is not referenced.
- Repro: `node scripts/check-evidence-head.mjs → evidence-head.json not found or unreadable exit 1` (fresh checkout, no file).

### 2. HEAD matching + freshness window partially implemented but mis-wired
- `scripts/check-evidence-head.mjs:28-38` correctly computes `currentHead = git rev-parse HEAD` and checks `commit==HEAD` and `generatedAt` ±24h (`freshMs = 24*3600*1000`) — aligns to `plan.md:223` acceptance `commit==HEAD` and `generatedAt ±24h`.
- However `data.commit` / `data.generatedAt` field names assume `state/evidence-head.json` shape `{"commit": "<sha>", "generatedAt": "..."}`. Real twin shape is `{ "commit": "...", "generatedAt": "...", ... }` inside `ops-loop-evidence.json` — compatible if path were corrected, but `t1/t2` nesting not validated. Must read the correct JSON path; also tolerate legacy `commit` top-level vs nested.

### 3. Byte-identity `cmp -s` is a no-op hack (BLOCKER)
- Plan `plan.md:222-223` requires `cmp -s` identical twins; work-order `global_guardrails.evidence_byte_identity` is the enforcement. `scripts/check-evidence-head.mjs:48-52` does `execSync('cmp -s twin twin')` — compares file to itself, always succeeds, unrelated to real twins. CI gate `ci-gates.yml:23` will pass freshness even if twins diverge.
- Expected: `cmp -s .autoforge/validation/ops-loop-evidence.json stages/07_validate/output/ops-loop-evidence.json` (or at least validate both exist and are byte-identical).

### 4. Twin-write regeneration not addressed
- `plan.md:219` seam: *checks `commit==HEAD` and `generatedAt` ±24h; CI `evidence-head-check`; regen twin-write*. Ticket `R17-evidence-reconciliation-automation.md:24` expects regen writes both twins atomically. Script is read-only checker; no regen path — acceptable if separate harvest does twin-write, but script should at least mention `gh workflow run discovery-harvest` hint correctly. Current `line 46` hint `run: gh workflow run discovery-harvest` is correct per acceptance, but path error prevents reaching it except via stale branch.

### 5. Workflow wiring
- `ci-gates.yml:22-23` does run `node scripts/check-evidence-head.mjs` on push/PR — correct per plan. However name `evidence-head-check` vs `ci.yml` again drifts (`exec/R17.md` claims `ci.yml`).

## Acceptance matrix (plan.md:223)

| Criterion | Status |
|---|---|
| Exit 0 when `commit==HEAD` else `evidence stale: commit X != HEAD Y — run: gh workflow run discovery-harvest` | ⚠️ logic present `scripts/check-evidence-head.mjs:45-47` but unreachable due to missing file |
| `generatedAt` ±24h freshness | ⚠️ implemented `scripts/check-evidence-head.mjs:32-38` but file miss hides it |
| `cmp -s` twin identical | ❌ `scripts/check-evidence-head.mjs:49` no-op `cmp -s twin twin` |

## Ponytail check

- Ladder: stdlib `fs.readFile` + `execSync('git rev-parse HEAD')` + `cmp -s` is already minimal — correct rung. Over-engineering flag: `execSync('cmp -s twin twin')` is dead code that only satisfies string search for `cmp -s`; it should be deleted and replaced with real twin comparison (2 paths). Skipped: adding a Node-side `fs.readFile` byte compare instead of `cmp` — keep `cmp -s` as global guardrail literal, but point it at real twins.

## Required changes

1. **Fix path** `scripts/check-evidence-head.mjs:11` to read canonical evidence:
   ```js
   const primary = path.resolve(__dirname,'../.autoforge/validation/ops-loop-evidence.json');
   const twin = path.resolve(__dirname,'../stages/07_validate/output/ops-loop-evidence.json');
   // try primary, fallback to state/evidence-head.json for legacy if desired
   ```
   Validate both exist and are parseable JSON with `commit` and `generatedAt`.

2. **Fix `cmp -s`** `scripts/check-evidence-head.mjs:48-52` to:
   ```js
   try { execSync(`cmp -s "${primary}" "${twin}"`); } catch { console.error('evidence twins diverge — run: gh workflow run discovery-harvest'); process.exit(1); }
   ```
   Keep `cmp -s` literal for CI grep compliance; do not leave `twin twin`.

3. **Ensure message** matches acceptance verbatim: `evidence stale: commit ${currentHead} != HEAD ${commitInHead} ...` already matches `plan.md:223` — keep.

4. **Verify twin generation**: ensure harvest/validation writes both paths atomically (out of scope for this script but CI must enforce). Add check that `generatedAt` parses as valid ISO date within ±24h.

5. **Update docs** `.autoforge/execution/R17.md:11-12` to reference correct file `ops-loop-evidence.json` not `state/evidence-head.json`.

## Evidence

- `scripts/check-evidence-head.mjs:11,28-52`, `ci-gates.yml:22-23`, `state/evidence-head.json` missing vs `.autoforge/validation/ops-loop-evidence.json:2` present (`commit: 056b75f`), `plan.md:219-223`, reproducer `node scripts/check-evidence-head.mjs → exit 1 not found`.

## Risk if shipped as-is

Gate always fails (file not found) OR no-ops twin check hides divergence; evidence staleness quarantined by validator but CI passes false-positive on twin identity.
