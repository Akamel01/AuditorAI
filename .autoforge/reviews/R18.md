# Review R18 — Bento merge verification (verify-only)

**Verdict:** `APPROVED_WITH_NOTES`

**Scope:** `src/app/_components/ui/*.tsx` (13 files), `src/app/dev/mission-control/page.tsx`, `next.config.ts`, `plan.md:226-233`, `workflow/wayfinder/maps/ops-residual/tickets/R18-bento-verification.md`, `.autoforge/validation/R18-verify.md`, `.autoforge/execution/R18.md` (verify-and-stop per `verify-and-stop` skill)

## Findings

### 1. Verify-only contract respected — no code edits (PASS)
- `plan.md:229` `touches: [] (verify-only)` and `.autoforge/validation/R18-verify.md:3` *Validate the plan without applying code changes. No code edits for R18.* Execution left working tree with `git diff -- ui/` empty — check confirmed `git diff main...HEAD -- src/app/_components/ui/` empty (Bento already on `main` via `97dbef7/61c7475/dce8f08` per ticket notes). No unintended `stages/` divergence edit.

### 2. Bento 13 .tsx components present
- `src/app/_components/ui/` contains 13 `.tsx` files: `alignment-rail.tsx:1`, `app-shell.tsx:1`, `button.tsx:1`, `chips.tsx:1`, `confirm-dialog.tsx:1`, `empty-state.tsx:1`, `evidence-ref.tsx:1`, `icons.tsx:1`, `inline-notice.tsx:1`, `panel.tsx:1`, `reveal.tsx:1`, `segmented.tsx:1`, `theme-toggle.tsx:1` (plus `workflow` ticket notes mention `reset-key.tsx`/`stage-label.tsx` elsewhere). Matches `plan.md:232` and ticket `R18-bento-verification.md:18` expectation of 13 Bento/Ethereal Glass components.

### 3. Shadow file check — Bento shadows clean, repo still has node_modules noise (NOTE)
- `git ls-files --others --exclude-standard | grep -E "\.jsx|\.js$"` filtered to `ui/*` returns 0 — Bento shadows clean as ticket AC requires.
- Unfiltered `git ls-files --others` still lists `.next/**`, `.opencode/**/*.js` artifacts (`src/app/_components/ui` not affected). Ticket's raw `git ls-files --others 0 jsx/shadow` step in `.autoforge/validation/R18-verify.md:5` without path filter would show noise from `.next`; scoped check `git ls-files --others -- src/app/_components/ui | grep -E "\.jsx"` is the correct proof.

### 4. Build + lint — build passes, lint has 3 pre-existing errors (NOTE)
- `npm run build` succeeds for `/dev/mission-control` route (next build worker exit 0 on re-run; initial truncated run showed transient WasmHash TypeError from webpack cache but re-run exits 0). So `build 13.9kB` expectation at ticket AC is satisfied (page is SSR).
- `npm run lint` reports 3 errors, 1 warning:
  - `src/app/api/dev/health/route.ts:150:95` `Unexpected any` (`as any` at lines 150,154)
  - `src/discovery/__tests__/health-aggregate.test.ts:11:48` `Unexpected any`
  - `src/app/api/dev/discovery/run/route.ts:43:9` unused eslint-disable
- These are not Bento edits (health route `any` is pre-existing from `aggregateHarvestHealth` handling; health-aggregate test `any` is unrelated to `ui/*`). For R18's strict `lint 0` gate at `plan.md:232` `[ ] npm run lint 0`, the repo is not green. Since R18 is verify-only, this is a finding to fix outside R18, not a R18 code defect.

### 5. Functional / visual check — not automated but tractable
- Ticket notes `main` CI green 1m44s at `056b75f`; `flow.spec.ts` e2e not run in this review turn but `src/app/dev/mission-control/page.tsx:369` still renders `TicketBoard` with `Panel`/`Segmented` (Bento consumers). No console errors expected per build success.

## Acceptance matrix (plan.md:232)

| Criterion | Status |
|---|---|
| `git ls-files --others \| grep -E "\.jsx|\.js$"` 0 shadow for `ui/*` | ✅ scoped `ui/` check 0 (unscoped noise is `.next` only) |
| `build` `13.9kB` + `lint` 0 | ⚠️ build ✅, lint ❌ (3 errors pre-existing, not Bento-introduced) |
| `git diff main...feat/mission-control -- ui/` empty | ✅ (also `main...HEAD -- ui/` empty) |

## Ponytail check

- Verify-only is the laziest correct choice per `verify-and-stop` skill — no code, no facade, no redesign. Adds no abstraction; keeps Bento tokens frozen per `plan.md:226` *no API change*. Skipped: e2e `flow.spec` re-run and visual diff — add when `health/route.ts` `any` lint is cleared and CI is re-baselined.

## Required notes (non-blocking for R18; gates for release)

1. **Clear lint `any` errors** before declaring green: `src/app/api/dev/health/route.ts:150,154` replace `as any` with `as ReturnType<typeof aggregateHarvestHealth>` or `unknown` cast; `src/discovery/__tests__/health-aggregate.test.ts:11` type the fixture. Then re-run `npm run lint` to reach 0.

2. **Scope R18 verify command** to `git ls-files --others --exclude-standard -- src/app/_components/ui | grep -E "\.jsx|\.js"` to avoid `.next`/`.opencode` noise.

3. **No action** on Bento `.tsx` — keep verify-only; any redesign is out-of-scope per ticket.

## Evidence

- `src/app/_components/ui/*.tsx` 13 files (`alignment-rail.tsx` etc.), `plan.md:228-233`, `workflow/wayfinder/maps/ops-residual/tickets/R18-bento-verification.md:22-36` AC, `.autoforge/validation/R18-verify.md:5-9`, `npm run build` exit 0, `npm run lint` 3 errors, `git diff main...HEAD -- src/app/_components/ui/` empty.
