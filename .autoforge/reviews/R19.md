# Review R19 — Wayfinder plumbing traceability

**Verdict:** `APPROVED`

**Scope:** `src/wayfinder/tickets.ts`, `src/wayfinder/ticket-types.ts`, `scripts/wayfinder-tickets.ts`, `src/app/dev/mission-control/page.tsx:18-19,79,369`, `src/app/dev/mission-control/_components/ticket-board.tsx`, `src/app/api/dev/tickets/route.ts`, `workflow/wayfinder/maps/ops-residual/MAP.md:58-59`, `workflow/wayfinder/maps/ops-residual/tickets/R19-wayfinder-plumbing-traceability.md`, `plan.md:235-243`, `work-order.json` waves D/E/A, `.autoforge/execution/R19.md`

## Findings

### 1. Ticket compilation is canonical markdown → index (PASS)
- `src/wayfinder/tickets.ts:44-58` `parseTicketFrontMatter` with `FRONT_MATTER_RE:1` (`/^---\r?\n([\s\S]*?)\r?\n---/`) correctly parses front-matter; `ticketFromFields:91-117` validates `id/title/status` via `parseStatus:83-89` against `KNOWN_STATUS` set `open|claimed|blocked|closed|resolved|out-of-scope` — prevents drift.
- `loadTicketsFromTree:144-167` walks `WAYFINDER_MAPS_DIR` (`src/wayfinder/ticket-types.ts` defines `workflow/wayfinder/maps`), reads `maps/*/tickets/*.md`, dedupes via `seen` Set `161`, throws on duplicate key.
- `classifyTickets:119-134` computes `frontier = status===open && unassigned && depsClosed` and `ready_without_owner = frontier && !hitl` — matches ticket AC `ready_without_owner, hitl, frontier, blocked`.
- `buildTicketIndex:169-189` produces `TicketIndex` with `counts:172-181` (`total, open, claimed, blocked, closed, frontier, ready_without_owner, hitl_frontier`) and sorted `tickets` — single source for both CLI and board.

### 2. CLI and board share same index — counts same as board (PASS)
- CLI: `scripts/wayfinder-tickets.ts:24-33` calls `indexWayfinderTickets()` and prints `wayfinder <N> tickets · frontier <N> · ready <N> · hitl <N> · blocked <N>` at line 28. With `--json`, emits full `TicketIndex` plus lane filter `rows` at line 25. Repro `npm run tickets -- --json` returns valid JSON with `counts.total=60, frontier=26, ready_without_owner=19, hitl_frontier=7, blocked=1` and per-ticket `key,status,hitl,ready_without_owner` fields — matches acceptance.
- Board: `src/app/dev/mission-control/page.tsx:18` `import { TicketBoard }` + `fetchTickets<TicketIndex>()` at `79,155-160` and `src/app/dev/mission-control/_components/ticket-board.tsx:21-53,55-71` renders same `index.counts` (`total, frontier, ready, hitl, blocked`) and lane filter `inLane:24-29` mirroring CLI `inLane:5-11` — counts identical by construction (both consume `buildTicketIndex`).

### 3. R19 ticket traceability at MAP.md:58-59 correct
- `workflow/wayfinder/maps/ops-residual/MAP.md:58-59` lists:
  - `R18 — Bento ...`
  - `R19 — Wayfinder plumbing traceability — ticket index + Mission Control board (triaged 2026-09-01) — see maps/ops-residual/tickets/R19-wayfinder-plumbing-traceability.md`
  Matches `plan.md:235-243` acceptance: `tracker-index.md:93-95` ↔ `MAP.md:58-59` alignment; `work-order.json` includes `R19` in wave A (`parallel_safe true, locks []`). Front-matter at `workflow/wayfinder/maps/ops-residual/tickets/R19-wayfinder-plumbing-traceability.md:2-10` has `id:R19 status:open hitl:false` — compiles to `ops-residual:R19 frontier true ready_without_owner true` in CLI output.

### 4. No GitHub sync for ops-residual — local-markdown only preserved
- No new GitHub sync code introduced for `ops-residual`; `src/wayfinder/tickets.ts:136-142` only reads local `WAYFINDER_MAPS_DIR`. `v2-agentic-platform` remains GitHub-canonical per ticket out-of-scope note — respected.

### 5. Acceptance matrix (plan.md:242)

| Criterion | Status |
|---|---|
| `npm run tickets` prints R19 OPEN | ✅ CLI shows `ops-residual:R19 ready` and JSON `key:ops-residual:R19 status:open` |
| Mission Control Tickets tab lists R19 | ✅ `page.tsx:369` `<TicketBoard index={tickets}>` with same `TicketIndex` |
| `tracker-index.md:93-95` matches `MAP.md:58-59` | ✅ both enumerate R19 traceable |
| `ponytail: pure fn compile` | ✅ pure `classifyTickets/buildTicketIndex` no side effects |

## Ponytail check

- Ladder: stdlib `readdirSync/readFileSync` + pure fns — laziest correct. No new deps, no facade, no ORM. **Minimal diff**: execution doc `.autoforge/execution/R19.md:19-21` correctly claims *no code changes required* beyond existing plumbing; execution is `npm run tickets --json` verification only. Skipped: adding a persistence layer or GitHub sync — explicitly out-of-scope.

## Required changes

None blocking. Optional polish:
- Add `npm run tickets -- --lane` CI check if `tracker-index.md` staleness quarantined by validator (risk note `plan.md:389` *Ticket index drift*).

## Evidence

- `src/wayfinder/tickets.ts:34-58,83-89,119-134,169-189`, `scripts/wayfinder-tickets.ts:24-33`, `src/app/dev/mission-control/page.tsx:18,79,155-160,369`, `src/app/dev/mission-control/_components/ticket-board.tsx:10-29,33-53`, `workflow/wayfinder/maps/ops-residual/MAP.md:58-59`, `plan.md:235-243`, `npm run tickets -- --json` output `60 tickets · frontier 26 · ready 19 · hitl 7 · blocked 1` with `ops-residual:R19`.
