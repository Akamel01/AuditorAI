# Review R8 — Health route `harvestHealth`

**Verdict: APPROVED_WITH_NOTES**

## Acceptance (plan.md:133-141)
- Objective: Centralize freshness/lock observability at `health/route.ts:11-138`.
- Seam: New pure `src/discovery/health-aggregate.ts` `harvestHealthSummary(store?)` from `getLedgerTailKV` + `listJobs(1)`.
- Touches: [`src/discovery/health-aggregate.ts`, `src/app/api/dev/health/route.ts`] Hazard `src/lib/persistence/**` Lock `persistence-single-writer` Wave B
- Acceptance: [ ] `GET /health` additive shape nullable when KV unavailable preserves `providers/ledger/topology`.

## Evidence inspected
- `src/discovery/health-aggregate.ts:20-60` — pure `aggregateHarvestHealth(entries?)` derives `lastRunAt` (max `at`), `lastHits` (filter `discovery_hits`), `lastSuccessAt`, `degraded:false` ✅ side-effect-free, unit-testable
- `src/discovery/__tests__/health-aggregate.test.ts:1-27` — 2 vitest cases: nulls when empty, correct max timestamp + hit count ✅ `npx vitest run` passes (2/2, 329ms)
- `src/app/api/dev/health/route.ts:10-13` imports `aggregateHarvestHealth`, `isAnyProviderDegraded`, `withPersistenceSingleWriter` ✅
- `src/app/api/dev/health/route.ts:135-166` — builds `harvestHealth` via `withPersistenceSingleWriter` → `readFileSync(state/discovery-ledger.json)` → `aggregateHarvestHealth(entries)` with fallback nulls on catch; merges `health_degraded` via `isAnyProviderDegraded()` ✅ additive, preserves `providers/ledger/ledgerAge/topology/topologyDrift` at :158-165
- `src/app/api/dev/health/route.ts:135-155` fallback returns nulls when file unreadable ✅ meets "nullable when KV unavailable" but via file-path not KV seam (see notes)
- `src/lib/persistence/single-writer.ts:1-17` — `withPersistenceSingleWriter` in-process boolean lock ✅ minimal, test-friendly, but extra touch beyond plan
- `src/discovery/health-state.ts:1-34` — `isAnyProviderDegraded`/`recordZeroHitOutcome` global maps ✅ not in plan touches, used for `health_degraded` (R16 concern leakage)
- `npm run typecheck` — green ✅ no new deps ✅

## Ponytail / seam checks
- **Smallest diff:** pure fn + route additive field avoids service class (arch report R8 alt C rejected) ✅
- **Reuse DataStore seam:** **PARTIAL** — spec says `harvestHealthSummary(store?)` from `getLedgerTailKV` + `listJobs(1)` (plan.md:136). Implementation reads `state/discovery-ledger.json` via `readFileSync` not `DataStore`/`getLedgerTailKV`. File read survives local dev but breaks KV-truth in prod (R10 pattern). `withPersistenceSingleWriter` wraps file read but doesn't solve split-brain.
- **No new deps:** none ✅
- **Touched globs correct:** plan expects 2 files; actual diff touches 2 planned + 2 extra (`single-writer.ts`, `health-state.ts`) — scope creep but low risk
- **Locks respected:** `persistence-single-writer` via `withPersistenceSingleWriter` wrapper; read hazard respected (serializes under persistence lock) ✅ wave B serial respected
- **One runnable check:** **PASS** — `health-aggregate.test.ts` is the ponytail check, runs green
- **Acceptance:** **PASS** — `GET /health` shape is additive (`harvestHealth` + `health_degraded`), all fields nullable, preserves `providers/ledger/topology` (route.ts:158-163). KV-unavailable fallback returns nulls as required.

## Findings (notes, not blocking)
1. **Seam drift — file vs KV (NOTE):** Replace `readFileSync(...discovery-ledger.json)` with `getLedgerTailKV(50, store)` + `listJobs` to honor DataStore seam and survive ROFS/Vercel. Current file path will diverge from KV ledger under prod. Suggested lazy fix: `try { const {entries}=await getLedgerTailKV(50); return aggregateHarvestHealth(entries)} catch { return nulls }` — remove file read entirely.
2. **Extra files not in work-order (NOTE):** `single-writer.ts` and `health-state.ts` introduce two new modules where `store.ts:68` `KvRestStore.call()` + existing ledger seam would suffice. `single-writer.ts` is redundant with `harvest.ts:103 HARVEST_LOCK` pattern; consider deleting and using `store?` injection directly. `health-state.ts` overlaps R16 `refusals` concern — keep but document that `health_degraded` is R16-owned.
3. **Naming:** `aggregateHarvestHealth` vs planned `harvestHealthSummary` — alias is fine but add `export const harvestHealthSummary = aggregateHarvestHealth` for traceability to plan/arch report.
4. **Test edge:** Add one case for `entries=[]` vs `null` both yield `degraded:false` (already covered) and for malformed `at` (Date.parse NaN) — currently skips NaN correctly.

## Required changes (optional for APPROVED, required before wave F)
- None blocking for APPROVED_WITH_NOTES. For full seam compliance before M8, refactor health route to use `getLedgerTailKV` seam and remove file read. No re-review needed unless refactor touches `src/lib/persistence/**`.

## Evidence
- `npx vitest run src/discovery/__tests__/health-aggregate.test.ts` 2 passed
- `npm run typecheck` green
- `git diff HEAD -- src/app/api/dev/health/route.ts` shows additive `harvestHealth` + `health_degraded` without removing existing keys
