# Review: VG-01 — Settle journal-deletions-and-tz gotcha

**Verdict: APPROVED_WITH_NOTES**
**Reviewer:** autoforge-reviewer (muse-spark-1.2-contributor-free)
**Date:** 2026-09-02
**Scope:** `vault/gotchas/journal-deletions-and-tz.md`, `vault/journal/2026-09-02-journal-deletions-settled.md`, `.autoforge/execution/VG-01.md`
**Inputs:** `.autoforge/execution/VG-01.md:1-12` verbatim, `vault diff` `HEAD...working-tree`, `vault/CHARTER.md:16-20,38-60,64-74`, `scripts/lib/frontmatter.mjs:1-133`, `scripts/vault-import.mjs:14-90`, `scripts/vault-sync.mjs:16-44`, `.autoforge/execution/work-order.json:VG-01`

---

## Acceptance (all PASS)

| Criterion | Evidence | Status |
|---|---|---|
| **status settled** | `vault/gotchas/journal-deletions-and-tz.md:5` `status: settled` (was `open` in `git show HEAD:…:5`); `git diff HEAD -- vault/gotchas/journal-deletions-and-tz.md` single-line `open→settled` | ✅ |
| **journal date 2026-09-02** | `vault/journal/2026-09-02-journal-deletions-settled.md:4` `date: 2026-09-02`, filename prefix `2026-09-02-` matches, `vault/CHARTER.md:42-46` contract `YYYY-MM-DD` | ✅ |
| **no state/\*\* mutation (VG-01 scope)** | `git diff HEAD -- vault/gotchas/journal-deletions-and-tz.md` only prose touch; `git diff HEAD -- state/vault-notes.json` empty after restore; working-tree `M state/discovery-ledger.json` + `M state/odd-coverage.json` are foreign lane (discovery harvest), not VG-01 touches; `.autoforge/execution/VG-01.md:11` correctly declares `No state/vault-notes.json or vault/views edits` deferred to VG-SYNC | ✅ |
| **front-matter valid** | Both files PASS `parseFrontMatter` + `validateNoteFrontMatter` (`frontmatter.mjs:15-27,78-109`): gotcha `type:gotcha status:settled owner:agent` at `:2-6`, journal `type:journal status:null owner:agent` at `:2-10` + `links.evidence_ids: []` resolves; `node scripts/vault-import.mjs` emits `26 chartered notes` with `path: vault/journal/2026-09-02-journal-deletions-settled.md status:null` | ✅ |
| **body preservation** | Gotcha body `body_chars:1984` unchanged (`vault-import` preview confirms); title/type/date/owner preserved exactly vs HEAD | ✅ |

---

## Standards

*Charter zones* `vault/CHARTER.md:16-20` — Prose `vault/gotchas` human-curated vs Views `vault/views` machine vs Registries `state/*.json` deterministic. VG-01 correctly touches only Prose zone (`vault/gotchas` + `vault/journal` per `work-order.json:touches`), leaves Views/Registries to VG-SYNC lock `vault-state-single-writer` (`decisions.md:AD-09`).

*Front-matter seam* `scripts/lib/frontmatter.mjs:78-109` — reuse of single seam, no new parser, no new dep (`yaml` already installed per `decisions.md:AD-07`). Validation enforces `title|type|date|owner` at `:79-83`, `STATUSES` at `:7`, `DATE_RE` at `:4`.

*Staging hygiene* `AGENTS.md:21-23` — VG-01's execution evidence anticipates explicit `git add <paths>` via VG-SYNC; no `git add -A` observed.

*Smell baseline:* no Duplicated Code, Speculative Generality, Shotgun Surgery, or Feature Envy — one-line status flip + one append-only journal is the minimal shape. No Middle Man / Message Chain.

## Spec

*Work-order acceptance* `.autoforge/execution/work-order.json:VG-01:acceptance` — verbatim: "status open→settled preserves title/type/date/owner; new journal vault/journal/YYYY-MM-DD-journal-deletions-settled.md with type:journal date owner status null and body refs gotcha; parseFrontMatter+validateNoteFrontMatter pass; no state/views mutated" — all satisfied.

*No scope creep:* no `state/**` write, no `vault/views/**` hand-edit, no new scripts, no FS watcher code — ponytail ceiling correctly deferred.

*Correct implementation:* journal body at `vault/journal/2026-09-02-journal-deletions-settled.md:12-18` references `vault/gotchas/journal-deletions-and-tz.md (settled status)` and deterrent `watch-vault-journal.sh` runbook, matching `vault/gotchas/journal-deletions-and-tz.md:38-44`.

---

## Findings

### Notes (non-blocking)

1. **NOTE — `links.path` not chartered** — `vault/journal/2026-09-02-journal-deletions-settled.md:6-8` uses `links: - path: vault/gotchas/journal-deletions-and-tz.md` with `note:`. Charter `vault/CHARTER.md:49-53,59` only charters `links.evidence_ids|issues|adr` and forbids other machine-parsed fields without amendment. `frontmatter.mjs:111-123` `normalizeLinks` safely ignores unknown `path` and only validates `evidence_ids`, so `vault-import` passes (`evidence_ids: []` resolves at `vault-import.mjs:52-54`). No fix required; if `path` is intended as machine link, charter amendment should formalize it, otherwise keep as body `References:` prose only.

2. **NOTE — curated-note human-owned vs `owner:agent`** — `vault/CHARTER.md:66` states curated notes are human-owned / agents propose. Both gotchas carry `owner: agent` (`vault/gotchas/journal-deletions-and-tz.md:6`, `vault/gotchas/opencode-api-key-invocation.md:6`), and `decisions.md:AD-04` explicitly authorizes settlement as `status: open→settled` + journal when `owner:agent`. No charter violation; noted for traceability.

3. **NOTE — `evidence_ids: []` explicit vs omitted** — Journal declares `evidence_ids: []` at `:9`. Charter `vault/CHARTER.md:57-58` requires resolution; empty array is valid and passes `loadRegistryIds` at `vault-import.mjs:30-39`. No evidence linkage needed for this operational lesson.

### No blocking defects

- Ponytail ladder enforced: `one-line status edit + one journal file; skipped FS immutable flag / pre-commit hook — add when second deletion incident measured` (`work-order.json:VG-01:ponytail`) — correct deferral, no abstraction added.
- Vault determinism untouched: `vault-sync.mjs:16-44` HEAD-worktree path not invoked in VG-01, correctly deferred to VG-SYNC per `work-order.json:edges VG-01→VG-SYNC`.
- No regressions: second gotcha remains `vault/gotchas/opencode-api-key-invocation.md:5 status: open` — VG-01 disjoint from VG-02 as planned wave P.

---

## Ponytail assessment

**Minimal + lazy-correct.** Reused `scripts/lib/frontmatter.mjs` seam, stdlib only, no new deps, no new files beyond required journal, wholesale `O(n)` regen deferred to VG-SYNC at `n=161` ceiling (`decisions.md:AD-05`). Short explanation, short diff — ladder rung 1-2 satisfied.

---

## Required changes

None. VG-01 is ready for commit; VG-SYNC will compile `state/vault-notes.json:note_count 25→26` and regenerate `vault/views/**` from HEAD.

---

## Artifacts

- Reviewed: `vault/gotchas/journal-deletions-and-tz.md:1-44`
- Reviewed: `vault/journal/2026-09-02-journal-deletions-settled.md:1-18`
- Evidence: `.autoforge/execution/VG-01.md:1-12`
- Verifier: `node scripts/vault-import.mjs` → 26 notes, `parseFrontMatter` PASS both files
