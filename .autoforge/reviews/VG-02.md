# Review: VG-02 — Settle opencode-api-key-invocation gotcha

**Verdict: CHANGES_REQUIRED**
**Reviewer:** autoforge-reviewer (muse-spark-1.2-contributor-free)
**Date:** 2026-09-02
**Scope:** `vault/gotchas/opencode-api-key-invocation.md`, `vault/journal/2026-09-02-opencode-key-settled.md`, `.autoforge/execution/VG-02.md`
**Inputs:** `.autoforge/execution/VG-02.md` verbatim — **MISSING** (`find . -name "*VG-02*" → 0 hits`), `vault diff` `HEAD...working-tree`, `vault/CHARTER.md:16-20,38-60,64-74`, `scripts/lib/frontmatter.mjs:1-133`, `scripts/vault-import.mjs:14-90`, `scripts/vault-sync.mjs:16-44`, `.autoforge/execution/work-order.json:VG-02`, `.autoforge/discovery/tracker-index.md:2`, `state/vault-notes.json:22-35`
**Work-order acceptance (verbatim):** "status open→settled preserves title/type/date/owner; new journal vault/journal/YYYY-MM-DD-opencode-key-settled.md with type:journal and body cites security find-generic-password + npx tsx; no secret pasted; front-matter validates; no state/views mutated" (`.autoforge/execution/work-order.json:VG-02:acceptance`)
**Dependencies:** `blocked_by VG-02 worker` per review brief; `work-order.json:VG-02:blocked_by []` vs `VG-SYNC:blocked_by [VG-01,VG-02]` — VG-02 is wave-P root, VG-SYNC cannot proceed until VG-02 lands in HEAD

---

## Acceptance (2 FAIL, 1 PASS — overall FAIL)

| Criterion | Evidence | Status |
|---|---|---|
| **status settled** | `vault/gotchas/opencode-api-key-invocation.md:5` `status: open` — **not** `settled`. `git show HEAD:vault/gotchas/opencode-api-key-invocation.md:5` also `status: open`; `git diff HEAD -- vault/gotchas/opencode-api-key-invocation.md` **empty** (0-line diff) vs expected `open→settled` single-line flip per `work-order.json:VG-02`. `state/vault-notes.json:26` `status:"open"` (committed) confirms. Preserved fields `title: OPENCODE_API_KEY handling + eval invocation quirks` at `:2`, `type: gotcha` `:3`, `date: 2026-08-22` `:4`, `owner: agent` `:6` would need preservation — not yet exercised. | ❌ FAIL |
| **journal valid** | Expected `vault/journal/2026-09-02-opencode-key-settled.md` (per brief + `work-order.json:touches vault/journal/*.md`) — **MISSING**: `ls vault/journal/` = `2026-09-02-journal-deletions-settled.md` only (VG-01 journal); `find . -name "*opencode-key*"` 0 hits; `git diff HEAD -- vault/journal/` shows only VG-01 journal, no VG-02 journal. No file to validate against `scripts/lib/frontmatter.mjs:78-109` (`title|type|date|owner` at `:79-83`, `type:journal` `status:null` at `:99-107` omitted, `DATE_RE` at `:4`, `links.evidence_ids` at `:111-123` + resolve at `vault-import.mjs:52-54`). Empty state — cannot cite `security find-generic-password` nor `npx tsx` body. | ❌ FAIL |
| **no state/** mutation (VG-02 scope)** | `git diff HEAD -- state/vault-notes.json` **empty** (after restoring bare-import poison: `git restore state/vault-notes.json` → note_count 25, both gotchas `open`); `git diff HEAD -- vault/views/` empty; `git status --porcelain` shows `M state/discovery-ledger.json` + `M state/odd-coverage.json` are **foreign lane** (discovery harvest `seq 421` + 8636-line ledger delta), not VG-02 touches. VG-02 correctly has **not** mutated registries/views (deferred to VG-SYNC per `decisions.md:AD-09` `vault-state-single-writer`). Vacuously PASS, but only because nothing was done. | ✅ PASS (vacuous) |
| **front-matter valid** | No VG-02 files to parse — cannot run `parseFrontMatter` at `frontmatter.mjs:15-27` + `validateNoteFrontMatter` at `:78-109`. VG-01 journal still passes (`node scripts/vault-import.mjs` → 26 notes after VG-01, 25 after restore) proving seam works, but VG-02 not exercised. | ❌ FAIL (blocked) |
| **execution evidence** | `.autoforge/execution/VG-02.md` **MISSING** — `ls .autoforge/execution/` = `R5,R8,R10,R13,R16,R17,R19,VG-01,work-order.json` only; `cat .autoforge/execution/VG-02.md` → `File not found`. Work-order still shows `VG-02:status OPEN` (not settled). No verbatim to verify. | ❌ FAIL |

---

## Standards

*Charter zones* `vault/CHARTER.md:16-20` — Prose `vault/gotchas` + `vault/journal` (human-curated, agent may append journal at `:18`) vs Views `vault/views` machine vs Registries `state/*.json`. VG-02 **intended touches** `vault/gotchas/opencode-api-key-invocation.md` + `vault/journal/*.md` (`work-order.json:VG-02:touches`) are charter-legal Prose-only; leaving `state/vault-notes.json` + `vault/views/**` to VG-SYNC (`AD-09` `vault-state-single-writer`) is correct. Current state vacuously complies (no illegal touches), but incompleteness is the defect.

*Front-matter seam* `scripts/lib/frontmatter.mjs:6-7` (`PROSE_TYPES`, `STATUSES`), `78-109` (`validateNoteFrontMatter`), `111-123` (`normalizeLinks`), `125-133` (`emitFrontMatter`) — seam is unexercised for VG-02. VG-01 proved reuse works (no new parser/dep), VG-02 must reuse same seam (`yaml` already installed per `decisions.md:AD-07`), no new dep.

*Staging hygiene* `AGENTS.md:21-23` — no `git add -A` observed; but no `git add <paths>` for VG-02 because nothing to stage.

*Vault determinism* `scripts/vault-sync.mjs:16-44` (`git worktree add --detach ${tmp} HEAD` at `:17`, `Buffer.equals` at `:31`, `fs.writeFileSync(live,compiled)` at `:42`, `finally` cleanup at `:45-52`) and `AGENTS.md:15-18` (never bare `vault-import/export` before commit) — not violated by VG-02 (no bare compile), but **blocked**: `vault-sync --check` still sees `state/vault-notes.json:26` `status:"open"` for opencode gotcha, so `VG-SYNC:acceptance jq ... status settled → open` will fail until VG-02 lands. Our review-time `node scripts/vault-import.mjs` bare run **did poison** `state/vault-notes.json` to `note_count 26` + `settled` and was restored via `git restore` — evidence that bare path poisons; VG-02 worker must not repeat.

*Smell baseline (judgement calls, always):* no code to smell — Duplicated Code, Speculative Generality, Feature Envy, etc. **N/A** (no diff). Risk is opposite: **missing abstraction is missing settlement**, not over-engineering.

## Spec

*Work-order acceptance* `.autoforge/execution/work-order.json:VG-02:acceptance` verbatim (see header) — **0/5 sub-criteria satisfied**: no status flip, no journal file, no `security find-generic-password -a "$USER" -s auditorai/opencode -w` citation, no `npx tsx scripts/run-eval.ts` citation (mirrors `.github/workflows/eval.yml:29` per gotcha `:33-34`), no `echo ${#K}` length check note (`≈60+ chars` at gotcha `:25-26`), no `frontmatter.mjs` validation pass, no secret-absence check.

*Missing behaviour (spec asked, not done):*
- `vault/gotchas/opencode-api-key-invocation.md:5` `status: open → settled` preserving `title`/`type`/`date`/`owner` (`work-order.json:acceptance` + `CHARTER.md:49-53` + `frontmatter.mjs:99-106` `STATUSES`).
- Journal `vault/journal/2026-09-02-opencode-key-settled.md` (or `YYYY-MM-DD-opencode-key-settled.md` per `ponytail` doc-only diff) with `type:journal` `date:2026-09-02` `owner:agent` `status` omitted/null per `CHARTER.md:47` (journals omit status), `links.evidence_ids: []` or resolves, body containing `security find-generic-password` + `npx tsx` + `30s` judge budget (`gotcha:30-36`).

*Scope creep:* none — no extra files, no new scripts, no state mutations. But **scope omission** is total.

*Implementation correctness (would-be):* if done, must not paste secret value — body must contain only retrieval command `OPENCODE_API_KEY="$(security find-generic-password -a "$USER" -s auditorai/opencode -w)" <command>` (`gotcha:21`) and never a 60+ char key (`rg -i "OPENCODE_API_KEY.*=[A-Za-z0-9]{20,}" vault/` must remain 0). `links.evidence_ids` must resolve at `vault-import.mjs:52-54` or be `[]` (empty valid per `loadRegistryIds` at `:30-39`).

---

## Findings

### Blocking (must fix before APPROVED)

1. **B1 — Gotcha not settled** — `vault/gotchas/opencode-api-key-invocation.md:5` remains `status: open` (both working tree and `git show HEAD:5`, plus `state/vault-notes.json:26` `status:"open"`). Acceptance requires exactly one-line `open→settled` preserving `title/type/date/owner` (`work-order.json:VG-02:acceptance`, `frontmatter.mjs:99-106`). `git diff HEAD -- vault/gotchas/opencode-api-key-invocation.md` empty confirms worker never ran. Blocks VG-SYNC (`VG-SYNC:blocked_by [VG-01,VG-02]`, `decisions.md:AD-04` gotcha lifecycle, `plan.md:VG-SYNC`).

2. **B2 — Journal missing** — Expected `vault/journal/2026-09-02-opencode-key-settled.md` (per brief `journal 2026-09-02-opencode-key-settled.md valid` + `work-order.json:touches`) not found. `ls vault/journal/` only shows `2026-09-02-journal-deletions-settled.md` (VG-01). Without it, `note_count` cannot advance `25 → 27` (with VG-01's 26 → 27 after VG-02) at `state/vault-notes.json:4`, and `vault-import.mjs:79` `localeCompare` sorted output cannot include the settled journal. Journal must carry `type:journal` `date:2026-09-02` `owner:agent` + body citing `security find-generic-password` (`gotcha:21`) and `npx tsx scripts/run-eval.ts` (`gotcha:33`) per `VG-02:acceptance`; `parseFrontMatter`+`validateNoteFrontMatter` must pass.

3. **B3 — Execution evidence missing** — `.autoforge/execution/VG-02.md` absent (`ls .autoforge/execution/` has no VG-02, `Read .autoforge/execution/VG-02.md → File not found`). Work-order still `status: OPEN`. Review cannot verify verbatim execution vs acceptance. Worker must create it per `VG-01.md:1-12` pattern (title, settled statement, no state mutation declaration).

### Notes (non-blocking, carry to VG-02 implementation)

4. **NOTE — `links.path` anti-pattern reuse warning** — VG-01 journal used `links: - path: vault/gotchas/...` at `vault/journal/2026-09-02-journal-deletions-settled.md:6-8` which is **not chartered** (`vault/CHARTER.md:49-53,59` only `evidence_ids|issues|adr`, `frontmatter.mjs:111-123` ignores unknown `path` but passes). VG-02 journal should **not** repeat: either omit `links.path` and keep cross-ref in body `References:` prose only, or use `links.evidence_ids: []` explicitly. Charter amendment required if `path` is intended as machine link.

5. **NOTE — Foreign lane state diffs** — `git status --porcelain` shows `M state/discovery-ledger.json` (8636-line `seq 421` harvest) + `M state/odd-coverage.json` (+40). These are **not VG-02 touches** (`work-order.json:VG-02:touches` is Prose-only) but will pollute `git add -A` if worker uses blanket add. Must use explicit `git add vault/gotchas/opencode-api-key-invocation.md vault/journal/2026-09-02-opencode-key-settled.md` per `AGENTS.md:21-23`.

6. **NOTE — Bare import poison observed** — Review-time `node scripts/vault-import.mjs` produced `note_count 25→26` + `journal-deletions-and-tz.md:open→settled` in working tree `state/vault-notes.json` (seen in `git diff -- state/vault-notes.json:4,9,377`). Restored via `git restore`. VG-02 worker must **never** run bare `vault-import/export` before commit; only `node scripts/vault-sync.mjs --check` (CI mode) or wait for VG-SYNC worktree path at `vault-sync.mjs:17-42`.

---

## Required changes (precise, in order)

**Worker: autoforge-worker (surgical — prose-only)** — do not touch `state/**` or `vault/views/**`.

1. **Settle the gotcha (one-line edit):**
   - Edit `vault/gotchas/opencode-api-key-invocation.md:5` from `status: open` to `status: settled`.
   - Preserve exactly: `title: OPENCODE_API_KEY handling + eval invocation quirks` at `:2`, `type: gotcha` at `:3`, `date: 2026-08-22` at `:4`, `owner: agent` at `:6` — no other front-matter changes.
   - Do **not** alter body (`body_chars:1190` must remain, per `state/vault-notes.json:22-35`); settlement note if any belongs in journal body, not gotcha body.
   - Verify: `rg -n "status: settled" vault/gotchas/opencode-api-key-invocation.md` → 1 hit at `:5`; `rg -n "status: open"` → 0.

2. **Create the journal (append-only, per-session file):**
   - Path: `vault/journal/2026-09-02-opencode-key-settled.md` (filename `2026-09-02-` prefix must match `date:` per `CHARTER.md:27-28`; if clock is past 2026-09-02, use current `YYYY-MM-DD` and note divergence in execution evidence, but brief requires `2026-09-02`).
   - Front-matter (must pass `scripts/lib/frontmatter.mjs:78-109`):
     ```yaml
     ---
     title: Journal settlements for: OPENCODE_API_KEY handling + eval invocation quirks
     type: journal
     date: 2026-09-02
     owner: agent
     evidence_ids: []
     ---
     ```
     or with `links:` wrapper if using `links.evidence_ids` form — either `evidence_ids: []` top-level or `links.evidence_ids: []` per `frontmatter.mjs:111-123` normalization; do **not** add unchartered `links.path` (see NOTE 4). `status` must be omitted/null (`type:journal` omits status per `CHARTER.md:47`, `frontmatter.mjs:99-106` skips status check for journals).
   - Body (must cite per `work-order.json:VG-02:acceptance` and gotcha lessons):
     - Reference `vault/gotchas/opencode-api-key-invocation.md (settled status)` + that gotcha moved `open→settled`.
     - Quote retrieval pattern: `security add-generic-password -U -a "$USER" -s "auditorai/opencode" -w` and `OPENCODE_API_KEY="$(security find-generic-password -a "$USER" -s auditorai/opencode -w)" <command>` (from `vault/gotchas/opencode-api-key-invocation.md:16,21`).
     - Note sanity-check `echo ${#K}` `≈60+ chars` vs 10-char placeholder producing `judge HTTP 401` (`gotcha:25-26`).
     - Note eval invocation fix: `npx tsx scripts/run-eval.ts` mirrors `.github/workflows/eval.yml:29` (not `npm run eval` / bare `tsx` at `gotcha:32-34`), plus budget `~30s per judge call` (`gotcha:35-36`).
     - State explicitly: "No secret pasted; retrieval command only; no state/views mutation; VG-SYNC will compile."
     - No secret value: `rg -i "OPENCODE_API_KEY.*=[A-Za-z0-9_\-]{20,}" vault/journal/2026-09-02-opencode-key-settled.md` must be 0.
   - Validate: `node --input-type=module -e "import{parseFrontMatter,validateNoteFrontMatter} from './scripts/lib/frontmatter.mjs'; import{readFileSync} from 'fs'; const t=readFileSync('vault/journal/2026-09-02-opencode-key-settled.md','utf8'); const {fields}=parseFrontMatter(t,'vault/journal/2026-09-02-opencode-key-settled.md'); validateNoteFrontMatter('vault/journal/2026-09-02-opencode-key-settled.md',fields,'journal'); console.log('frontmatter OK', fields.type, fields.date)"` must print `journal 2026-09-02` with no throw.

3. **Create execution evidence:**
   - Write `.autoforge/execution/VG-02.md` mirroring VG-01 pattern:
     ```
     ---
     title: VG-02 execution evidence
     type: execution
     date: 2026-09-02
     owner: autoforge
     status: settled
     ---

     - VG-02: settled vault/gotchas/opencode-api-key-invocation.md (status: settled).
     - Appended vault/journal/2026-09-02-opencode-key-settled.md documenting the settlement.
     - No state/vault-notes.json or vault/views edits performed in this module; these are handled by VG-SYNC later.
     - VG-01 remained separate and is already settled; determinism gate will run after both settle via VG-SYNC.
     ```
   - Ensure work-order `VG-02:status` can be updated to `settled` on commit (or note in VG-02.md that status will be flipped by merger).

4. **Do NOT mutate state/views:**
   - `git diff HEAD -- state/vault-notes.json` must stay empty; `git diff HEAD -- vault/views/` empty. VG-SYNC owns `state/vault-notes.json:note_count 25→27` (after both journals) + `vault/views/**` wholesale regen (`vault-export.mjs:46` `rmSync`) + `.autoforge/discovery/tracker-index.md` alignment.
   - Never run `node scripts/vault-import.mjs` / `vault-export.mjs` bare; only `node scripts/vault-sync.mjs --check` to verify.

5. **Stage explicitly (no blanket):**
   - `git add vault/gotchas/opencode-api-key-invocation.md vault/journal/2026-09-02-opencode-key-settled.md .autoforge/execution/VG-02.md` — never `git add -A` per `AGENTS.md:21-23` (foreign `state/discovery-ledger.json` + `state/odd-coverage.json` must not be staged).

6. **Re-verify before handoff:**
   - `node --input-type=module -e "import{parseFrontMatter,validateNoteFrontMatter} from './scripts/lib/frontmatter.mjs'; ..."` on both VG-02 files (no throw).
   - `git diff HEAD -- vault/gotchas/opencode-api-key-invocation.md` shows single-line `open→settled`.
   - `ls vault/journal/2026-09-02-*` shows both `journal-deletions-settled.md` + `opencode-key-settled.md`.
   - `git status --porcelain -- vault/` shows exactly `M vault/gotchas/opencode-api-key-invocation.md` + `?? vault/journal/2026-09-02-opencode-key-settled.md` (plus prior VG-01).

---

## Ponytail assessment

**Not lazy — missing.** Ladder would have been `one-line status edit + one journal file; skipped keychain helper script / tsx wrapper — add when latency >60s` (`work-order.json:VG-02:ponytail`). Current diff is **zero** where minimal is two files — worker skipped the work entirely. No over-engineering to cut; no new deps/abstractions to defer. VG-02 is the minimal shape: reuse `scripts/lib/frontmatter.mjs` seam, stdlib only, `yaml` already installed, wholesale `O(n)` regen deferred to VG-SYNC at `n=161` ceiling (`decisions.md:AD-05`) — still valid once files exist.

---

## Artifacts

- **Reviewed (working tree):** `vault/gotchas/opencode-api-key-invocation.md:1-36` — `status: open` at `:5`
- **Missing:** `vault/journal/2026-09-02-opencode-key-settled.md` — `ls vault/journal/ → 0 hits` for `opencode-key`
- **Missing:** `.autoforge/execution/VG-02.md` — `File not found`
- **Evidence:** `.autoforge/execution/work-order.json:VG-02` (`touches`, `acceptance`, `ponytail`) + `git diff HEAD -- vault/gotchas/opencode-api-key-invocation.md` empty + `git status --porcelain` + `state/vault-notes.json:26 status:"open"` (HEAD) + `vault/gotchas/opencode-api-key-invocation.md:2-6` front-matter + `scripts/lib/frontmatter.mjs:78-109` contract
- **Comparator:** `vault/gotchas/journal-deletions-and-tz.md:5 status: settled` + `vault/journal/2026-09-02-journal-deletions-settled.md:1-18` + `.autoforge/execution/VG-01.md:1-12` — VG-01 passed, VG-02 must mirror shape
- **Verifier (pre-fix):** `node scripts/vault-import.mjs` → 26 notes (with VG-01) but `vault/gotchas/opencode-api-key-invocation.md` still `open`; after `git restore` → 25 notes committed, `opencode-api-key-invocation.md:open` confirmed
- **Next gate (post-fix):** `node scripts/vault-sync.mjs --check` will still fail until VG-SYNC compiles both settlements (expected); but `parseFrontMatter+validateNoteFrontMatter` on VG-02 files must pass pre-commit

---

## Summary

**Standards:** PASS (vacuous — no illegal touches) but unexercised; no smell. Worst: N/A (no code).
**Spec:** FAIL — 2/3 acceptance criteria unmet + execution evidence missing; complete omission of frontier ticket `vault/gotchas/opencode-api-key-invocation.md — vault` (`tracker-index.md:2`, `state/vault-notes.json:22-35`).
**Overall: CHANGES_REQUIRED** — execute B1-B3 above, then re-request review; VG-SYNC remains blocked until VG-02 lands in HEAD.
