# Plan-HOLD Critique — HOLD Frontier R1–R4 (self-approving MV, daemon HITL deferred)

**Reviewer:** autoforge-reviewer (independent, read-only, ponytail) — model `opencode/muse-spark-1.2-contributor-free` inherit, 80k tok cap
**Date:** 2026-09-03
**Scope (read-only):** `.autoforge/plans/plan-HOLD.md:1-333` (57k verbatim <80k) + `.autoforge/execution/work-order-HOLD.json:1-326` (20k verbatim) vs `.autoforge/discovery/tracker-index.md:3-20` (4 BLOCKED lines verbatim) + `.autoforge/requirements/grilling-HOLD.md:10-67` R1–R4 MV 57 lines + `M1–M8:69-76` + `.autoforge/architecture/report-HOLD.md:1-406` seams `store.ts:16-26 jobs.ts:41-42 harvest.ts:99-256 ledger.ts:4-40` + alternatives §3 + locks §6 + `.autoforge/architecture/decisions-HOLD.md:1-163` AD-26..33 + `vault/CHARTER.md:14-20` + `AGENTS.md:15-23` vault+staging + `workflow/wayfinder/maps/ops-residual/MAP.md:41-45` tickets R1–R4 + `R1-cross-instance-harvest-lock.md:1-40` `R2:1-35` `R3:1-37` `R4:1-36`
**Blocked_by:** grilling-HOLD done → architect-HOLD done (report-HOLD + decisions-HOLD AD-26..33) → this plan — satisfied per `plan-HOLD.md:15`
**Skills:** `code-review` (Standards vs Spec two-axis, read-only), `ponytail` ladder, `lean-build`, `codebase-design`, `verify-and-stop`
**Mode:** planning-only, no implementation dispatched; `node scripts/vault-sync.mjs --check` remains gate before any `state/**` commit including `state/production-harvest-proof.json`

---

## Verdict

**CHANGES_REQUIRED** — 3 required fixes before dispatch, 4 notes. Plan is otherwise complete, traceable, hazard-correct, ponytail-minimal, and testable. Enumeration 4/4 correct, deferred M1–M8 correctly gated, HITL split for R4 bundle vs live correct, and single-writer guards correctly flagged where `touches` intersect. Required changes are mechanical and self-approving at plan-amendment level (no HITL):

| # | Required | What breaks if not fixed |
|---|---|---|
| **RC-1** | Add `createFallbackStore` delegation of `setIfAbsent` to `work-order-HOLD.json:191-196` / `plan-HOLD.md:94` acceptance | `getDataStore()` returns fallback wrapper without `setIfAbsent` → `acquireHarvestLock` fallback path triggers `WARN harvest lock: KV unavailable, using process-local guard` even when KV is healthy (fallback shadow swallowed `StoreUnavailableError` at `store.ts:138-150`), re-introducing burst race `harvest.ts:99-105` documents as ceiling |
| **RC-2** | Canonicalize TTL: ticket `3600s` vs grilling `120s` → pin one default + note configurability | Scheduler tests `R1 AC:32-33` vs `grilling-HOLD.md:18`/`R1:24` mismatch; worker may pick wrong TTL vs `HARVEST_NODE_POLL_BUDGET:166-172` bound, causingmid-harvest sneak-in or deadlock |
| **RC-3** | Align R3 acceptance: `R3-regression-tests.md:29-31` `onRun invoked exactly once under repeated terminal fetches` vs plan's `checkDuplicate/claimFingerprints` dedup | Spec drift — dispatching without explicit `onRun` scope note leaves ticket AC uncovered; either add `provider-health.tsx` onRun spy or record that onRun is covered by remaining lane `M-R7` and close R3 second bullet as intentional narrow |

After RC-1..3 amended (one-line doc edits, no new seam), **M-R1, M-R2, M-R3 self-approve TRUE, M-R4 TRUE for MV bundle / HITL for live, M1–M8 FALSE** — ready for waved dispatch `P:[M-R1-seam M-R2 M-R3 M-R4-module]` → `S1:M-R1-wire` → `S2:M-R4-wire` → `V:vault-sync --check`.

---

## 1. Enumeration 4/4 — PASS

| Check | Evidence | Result |
|---|---|---|
| **Tracker BLOCKED count** | `tracker-index.md:3-4` R1 `BLOCKED` + `:7-8` R2 + `:13` R3 + `:18` R4 = 4 lines; `tracker-index.md:22-96` 12 OPEN `R5:23 R6:28 R7:33 R8:38 R9:43 R10:48 R11:53 R12:58 R13:62 R14:67 R15:72 R19:93` plus 3 FOG `R16:78 R17:82 R18:87` =19 total; `report-HOLD.md:28-34` delta table maps same 4 | ✅ |
| **Plan §1 reconciles all** | `plan-HOLD.md:21-26` rows 1–4 with verbatim `tracker-index.md:3` `7` `13` `18` + `MAP.md:41-45` cites; rows 5–19 `OUT OF LANE` `R5:23 … R19:93` + `16-18` `OUT OF SCOPE` FOG + `M1–M8:103-140` `DEFERRED` `AD-30` | ✅ |
| **Counting proof** | `plan-HOLD.md:42` `4 BLOCKED lines =4 modules M-R1..M-R4 self-approving MV per grilling Self-approve Yes at :24,:39,:52,:65 + AD-26..AD-29 TRUE (R4 true for bundle, HITL for daemon)` + `work-order-HOLD.json:7-9` `tracker_total_blocked_hold:4 tracker_count:4 tracker_total_open:12` + `:33` duplicate proof | ✅ |
| **Work-order mirrors plan 1:1** | `work-order-HOLD.json:42-158` 4 modules `M-R1:43 M-R2:73 M-R3:101 M-R4:126` each with `frontier_ticket` `R1:1-40 R2:1-35 R3:1-37 R4:1-36` + `touches`/`hazard_touches`/`locks`/`wave` matching `plan §4` `90-191` tables | ✅ |
| **No merge / no omission / no invention** | `plan-HOLD.md:42` `no merge: distinct operational domains (distributed lock vs ledger ordering vs regression tests vs proof bundle); dedup would fail per report-HOLD.md §1 map-ownership + MAP.md:14 reuse-seam rule — each hides different mutable key (harvest:lock vs INDEX_KEY vs test surface vs discovery:proof:bundle)` — verified `touches` disjoint per §2 except `harvest.ts` hazard; first-item-only failure flagged last sentence | ✅ |

**Self-approve flag per deferred — correctly flagged (see §10):** M1–M8 listed only in `deferred_verbatim:16-25` + `plan-HOLD.md:40` `DEFERRED — no code per AD-30` and not in `modules` DAG; report `§2 M1–M8` + `decisions AD-30 FALSE` + `grilling-HOLD.md:69-76 HITL/FOG at :74` consistent.

---

## 2. Touches overlap guard (harvest-single-writer) — PASS with RC

| Module | touches (plan §4 / work-order) | hazard_touches that force serialization | Lock declared |
|---|---|---|---|
| M-R1 lock | `src/lib/persistence/store.ts, src/discovery/harvest-lock.ts, src/discovery/harvest.ts` at `plan:95` / `work-order:50-54` | `store.ts, harvest.ts` `96`/`55-58` | `datastore-seam-single-writer + harvest-single-writer` `98`/`60` |
| M-R2 ledger | `src/discovery/ledger.ts, src/lib/persistence/single-writer.ts` `119`/`80-83` | `ledger.ts` `120`/`84-86` | `discovery-ledger-index-single-writer` `122`/`88` |
| M-R3 tests | `tests/discovery/hold-r3.test.ts` `145`/`108-110` | `[]` `146`/`111` | none hermetic `148`/`113` |
| M-R4 bundle | `src/discovery/proof-bundle.ts, src/discovery/harvest.ts, src/app/api/dev/discovery/proof/route.ts, state/production-harvest-proof.json, tests/discovery/proof-bundle.test.ts` `173`/`133-139` | `harvest.ts, state/production-harvest-proof.json` `174`/`140-143` | `proof-bundle-single-writer + vault-state-single-writer + harvest-single-writer` `176`/`145` |

**Overlap intersection correctly flagged sequential:**
- `M-R1-wire ↔ M-R4-wire` share `src/discovery/harvest.ts` `harvest.ts:99-105,118-157,214-247,252-255` → `harvest-single-writer` sequential — plan `99,174,249-250` / work-order `177-183,249,286-290` `sequential_groups[0] ["M-R1-wire","M-R4-wire"]` + §5 locks `224-225`.
- `M-R1 + M-R2` disjoint at file level `store.ts vs ledger.ts` → parallel_safe P (distinct keys `harvest:lock` vs `INDEX_KEY:5` + `discovery:ledger:index-entry:*`) — plan `96-97,245-246` / work-order `276-284` `parallel_safe_groups` includes `[M-R1-seam M-R2]`.
- `M-R3` tests file-only `tests/**` disjoint from all prod → parallel_safe with all — plan `146-147,247` / work-order `115,283`.
- `vault-state-single-writer` declared for `state/production-harvest-proof.json` `224` / `168-176` guard `scripts/vault-sync.mjs:16-44` — only writer in HOLD lane is M-R4, so uncontended but correctly declared for future `state/vault-notes.json` / `vault/views/**` / `tracker-index.md` writers per `AGENTS.md:15-18` `report-HOLD.md §6`.

**Staging hygiene guard present:** `AGENTS.md:21-23` `explicit git add <paths> only, never -A` cited at `plan:54,107,130,155,185` and `work-order:298,467` with per-module exact sets e.g. `M-R1 git add src/lib/persistence/store.ts src/discovery/harvest-lock.ts src/discovery/harvest.ts` `107` — prevents foreign `vault/journal/**` or `workflow/wayfinder/maps/**` poison.

**RC-1 — fallback wrapper `setIfAbsent` missing:**
`store.ts:130-181` `createFallbackStore()` returns `{kind:"kv", put/get/getMany/keys/del/delByPrefix}` at `136-180` with per-method `try kv → catch → fallback` at `138-150` but has **no** `setIfAbsent` delegation. `DataStore` optional `setIfAbsent?` at `store.ts:16-26` added by M-R1 (`plan:101 Ad-26`) would then be `undefined` on the singleton when `KV_REST_API_URL` absent or `StoreUnavailableError:9-14` shadow triggers fallback, causing `acquireHarvestLock` at `harvest-lock.ts` to take `!s.setIfAbsent` fallback path and emit `WARN harvest lock: KV unavailable…` `R1 AC:32` even though `MemoryStore` path is healthy. Fix: extend fallback object with `async setIfAbsent(key,val,ttl){ try{ return await kv.setIfAbsent(key,val,ttl);}catch{ return fallback.setIfAbsent(key,val,ttl); } }` and mirror `MemoryStore` TTL `setTimeout unref` at `28-57`. Work-order `191-196` policy must list fallback delegation; plan acceptance `101` must add `createFallbackStore:130-181` to `touches` note. Severity: silent lock degradation → burst race reintroduced on Vercel.

---

## 3. Parallel safety — PASS

**Waves & locks (plan §5:206-262 vs report §6 vs work-order waves:241-274):**

| Wave (plan `237-243` / work-order `241-274`) | Members | Parallel? | Guard rationale |
|---|---|---|---|
| **P — pure code, disjoint touches** | `M-R1-seam (store.ts + harvest-lock.ts) + M-R2-heal (ledger.ts) + M-R3-tests (tests/**) + M-R4-module (proof-bundle.ts)` `238` / `242-249` `wave-P-pure-disjoint` | **parallel_safe: true within P if touches disjoint** | distinct files `store.ts` `setIfAbsent:16-26` vs `ledger.ts:4-40` `INDEX_KEY:5` vs `proof-bundle.ts` new vs `tests/discovery/hold-r3.test.ts` — no shared lock per `report-HOLD.md §6 P wave` `344-346` — may dispatch same turn as four parallel `Task autoforge-worker` calls (module boundary ≠ child-session boundary) `plan:84` `work-order:292,248` |
| **S — single-writer serialize** | `M-R1-wire + M-R4-wire` both touch `harvest.ts:106-256` `240` / `251-257` `wave-S-harvest-writers-serialize` | **serialize per file** — `harvest.ts:118-157` acquire before `D00-QUEUED:140-157` and `237-247` persistProofBundle after `setJobDone:237` both on `harvest.ts` so must not overlap same commit lane; `ledger.ts:8-26` heal already in P (different file) can stay parallel | `harvest-single-writer` for `harvest.ts`, `discovery-ledger-index-single-writer` for `ledger.ts:5`, `datastore-seam-single-writer` for `store.ts` `241` |
| **V — validation** | `proof bundle read-back + vault-sync` for `state/*.json` `243` / `259-265` `wave-V-validation` | sequential after S | `vault-state-single-writer` at `vault-sync.mjs:16-44` — `node scripts/vault-sync.mjs --check` |
| **H — HITL gate** | `M1–M8 + live daemon capture (launchctl at R4:21)` `242` / `267-273` `wave-H-HITL-deferred` | **blocked until owner HITL ack** — no worker code | `plan-decision-single-writer` for `.autoforge/plans/**` |

**Resource locks table (`plan:222-232` / `work-order:168-240`):** `vault-state-single-writer` `M-R4` only writer this lane but declared global `224`/`168-176` (touches `state/vault-notes.json vault/views/** .autoforge/discovery/tracker-index.md vault/gotchas/** state/production-harvest-proof.json state/discovery-ledger.json`); `harvest-single-writer` `M-R1+M-R4` on `harvest.ts` `225`/`177-183`; `discovery-ledger-index-single-writer` singleton `M-R2` `226`/`184-190`; `datastore-seam-single-writer` singleton `M-R1` `227`/`191-196`; `dedupe-index-single-writer` heritage retained `228`/`198-204`; `proof-bundle-single-writer` singleton `M-R4` `229`/`205-211`; `job-index-single-writer` heritage `212-219`; `wayfinder-compile` heritage `219-225`. All cite exact `file:line` verified (`HARVEST_LOCK:104` at `harvest.ts:104`, `INDEX_KEY:5` at `ledger.ts:5`, `writeQueue:17` at `dedupe-persist.ts:17`, `vault-sync.mjs:17 worktree + 31 Buffer.equals`).

**DAG edges:** `plan:208-216` + `work-order:159-167` `DAG nodes [M-R1 M-R2 M-R3 M-R4] edges []` with reason "All 4 have `blocked_by:[]` per tickets `R1:8 R2:8 R3:8 R4:8` — no semantic DAG; sequencing is hazard-only (same-file writes) via resource locks, not blocked_by." Verified each ticket front-matter `blocked_by: []` (`R1:8 R2:8 R3:8 R4:8`). Deferred `M1–M8` correctly not nodes (`plan:41 work-order:160`). Prior vault lane `VG-01→VG-SYNC` worktree edge retained as heritage but not part of HOLD DAG — correct isolation.

**Hazard split correctness:** Full `M-R1` and `M-R4` each touch `harvest.ts`, so **full modules are NOT parallel_safe** `249-250` `parallel_safe: false` flagged; only their **P-phase files** `harvest-lock.ts vs proof-bundle.ts vs store.ts` are `parallel_safe` `239-240` — scheduler must split into `P-seam+module` → `S-wire` (plan `255-260` 5-step schedule `P1: M-R1-seam+M-R2+M-R3+M-R4-module` → `S1:M-R1-wire` → `S2:M-R4-wire`). This wave split is explicitly documented `241,286` `module boundary ≠ child-session boundary — P members distinct modules may run in one child session batch (four parallel Tasks); S wires must follow in later turns after previous harvest.ts writer committed`. No hidden hazard missed.

**Note (non-blocking):** `work-order-HOLD.json:164` `topological_order: [["M-R1","M-R2","M-R3","M-R4"]]` serializes P members that are actually parallel_safe together. `waves` `242-273` is authoritative for dispatch; flat `topological_order` is valid hazard-respecting serialization but masks parallelism — scheduler must use `waves` + `parallel_safe_groups:275-285` not flat order (same note as `plan-remaining-critique.md:79`).

---

## 4. Architecture consistency — PASS

**Seam reuse & depth (ponytail rung 2 enforced, `MAP.md:14 no new deps` `MP:16-17 ceiling`):**
- Plan §2 `50-58` and work-order `34-40,313` cite `DataStore seam store.ts:16-26 MemoryStore:28-57 KvRestStore:60-126 call:68-99 5s abort:70-71 getMany:108-109 keys:112-114 getDataStore:183-188 setDataStoreForTests:192-194`, Job store `jobs.ts:41-42 PREFIX/INDEX_KEY createJob:133-197 getJob:199-215 listJobs:223-274 updateJob:276-318 appendLog:320-357 setJobError:369`, Harvest `harvest.ts:99-105 HARVEST_LOCK 106-256 executeJob 214-247 persist ordering 258-334 persistDiscoveryState 336-429 harvest()`, Ledger `ledger.ts:4-40 ENTRY_PREFIX:4 INDEX_KEY:5 appendLedgerKV:8-26 getLedgerTailKV:28-39`, Dedupe `dedupe-persist.ts:12-109 keys.ts:50 DISCOVERY_DEDUPE_INDEX_KEY`, Single-writer `single-writer.ts:7-17 withPersistenceSingleWriter:7 throws persistence write in progress` — all verified exists via grep at review time; `ledger.ts:36 filter v!==null` leaves dirty, `harvest.ts:215 VITEST!==true guard` exists.
- `report-HOLD.md §3` alternatives matrices design-it-twice per concern and §5 Interfaces exact signatures `acquireHarvestLock(store?,ttlSec=120,holder?=randomUUID)→{acquired,release}`, `appendLedgerKV(entries,store?)` + `getLedgerTailKV(limit,store?)` unchanged `slice(-500):25`, `proof-bundle.ts build/persist/readProofBundle` — keep variation behind existing seams, no new deps (stdlib `fs/path/crypto randomUUID`, `withPersistenceSingleWriter`, existing `yaml tsx`), native `title` already elsewhere — consistent with `decisions-HOLD.md AD-26..33` pony ceilings `AD-26 120s TTL AD-27 O500 hint+lazy heal AD-32 locks AD-33 risks`.

**Least-privilege & auth:** Mapped `plan:96-97` `requireAdmin` style: `harvest.ts executeJob` only writer of `harvest:lock` single KV key `store.ts:131-132 KV_REST_API_URL/TOKEN` reused no new secret, when absent → `MemoryStore.setIfAbsent` in-process fallback + `WARN harvest lock: KV unavailable…` per `R1 AC:32` `plan:102` `work-order:64`; ledger `isKv():53-60` branch unchanged; proof `GET /api/dev/discovery/proof` admin-gated at `health/route.ts:16` style `plan:171`; KV-first then file fallback at `discovery/route.ts:15-45` for tail/proof — correct.

**Ceilings explicit vs `MAP.md:16-17` accepted ceilings:** `process-local HARVEST_LOCK is ceiling per MAP:16` → `harvest-lock.ts SET NX EX 120` upgrade; `best-effort KV mirror is ceiling` → ledger `INDEX_KEY` + dedupe `writeQueue:17` + proof `EROFS warn+continue at dedupe-persist.ts:70` + vault `worktree+symlink at vault-sync.mjs:16-44` — all cited at `report §7` hazard ceilings `368-376` and `decisions AD-31 seam depth` + `AD-32 locks`.

**Vault determinism retained for HOLD lane:** `plan:52,58,60` + `work-order:173-176,294-298` `vault-state-single-writer` declared global, guard `node scripts/vault-sync.mjs --check` (`AGENTS.md:15-18` HEAD worktree `vault-sync.mjs:17 git worktree add --detach HEAD + 19 symlink node_modules + 31 Buffer.equals`) — even though only `M-R4` writes `state/production-harvest-proof.json` (not `state/vault-notes.json`), plan correctly notes `state/production-harvest-proof.json` is **not** vault-compiled (`state/vault-notes.json` only, `ci.yml:35-50` at `plan:187,312`) but still `explicit git add state/production-harvest-proof.json` per `AGENTS.md:21-23` + `vault-sync --check` before push keeps determinism green — matches `decisions AD-29:91` + `AD-32:133-135`. No `vault/journal/**` lane poison.

---

## 5. Ponytail (ladder) — PASS

- Every module climbs correctly: R1 rung 2 reuse `DataStore` seam → rung 3 `crypto.randomUUID` → rung 5 no new dep (smallest correct `SET NX EX` `["SET",key,val,"NX","EX",ttl]` at `store.ts:68-99`) skipped `GET→PUT` non-atomic race and `INCR harvest:seq` allocator per `report §3.1` speculative; R2 rung 2 reuse `store.keys/getMany/put` + rung 6 small delta (per-entry `NX` hint `30d` at `ledger.ts:12-22`) before lazy heal, skipped CAS `EVAL` pipeline per `store.ts:64-67` pipeline rejection doc; R3 rung 2 reuse `MemoryStore` + `setDataStoreForTests:192-194` + rung 6 one file before separate harness, skipped live KV E2E; R4 rung 2 reuse `DataStore` + `store.ts:4 node:crypto createHash` before per-harvest indexed history, no new dep — all shortest working diff per `work-order:323` ladder `R1 rung2+3+5 … R4 rung2+3`.
- No new dependency introduced: `proof-bundle.ts` uses `node:crypto` already at `store.ts:4`, `MemoryStore` at `store.ts:28-57` — verified `MAP.md:14`.
- `ponytail:` ceiling comments retained per module `plan:110,133,158,188,268` (`harvest:lock 120s TTL global per-cell locks deferred MAP:30`, `filter+heal on read is ceiling; full KEYS rebuild deferred 1%`, `one file is ceiling`, `single KV object is ceiling`, `worktree+symlink ceiling`) — matches decisions `AD-26..30` ponytails and `report §7` ceilings table `367-376`.

---

## 6. Testability — PASS with RC

**Per-module runnable check (no fixtures beyond `MemoryStore`/`vitest` `package.json:14`):**

| Module | Plan §4 acceptance `100-107,124-130,150-155,178-185` | Work-order acceptance `64,92,117,149` | Runnable gate |
|---|---|---|---|
| M-R1 lock | `store.ts:16-26 setIfAbsent` `KvRestStore ["SET",key,val,"NX","EX",ttl] → result==="OK"` `28-57 MemoryStore in-map + setTimeout unref` + `harvest-lock.ts ≤40 lines HARVEST_LOCK_KEY="harvest:lock"` + `acquireHarvestLock(store,120)` after `118-130` before `157` `if(!acquired) setJobError→return` `finally release holder-guarded DEL get===holder` + concurrent `Promise.all([acquire(h1),acquire(h2)])→one true one false` third after release succeeds `R1 AC:30-31` `executeJob busy does not throw R1 AC:34-35` | concurrent true/false + release `vitest` | `vitest` MemoryStore hermetic |
| M-R2 ledger | `ledger.ts:8-26 per-entry put + setIfAbsent "discovery:ledger:index-entry:"+seq 30d` + `INDEX_KEY merge !includes sort slice(-500) put` no pipeline `store.ts:64-67` + `28-39 orphanSeqs filter + idx.filter !orphanSet + withPersistenceSingleWriter put cleaned` + `50-concurrent Promise.all` → 50 unique no dups `R2 AC:28` + orphan inject `s.del(entryKey(3))` → tail drops 3 + `INDEX_KEY` cleaned `R2 AC:29` | 50-concurrent + orphan `vitest` | `vitest MemoryStore` |
| M-R3 tests | `tests/discovery/hold-r3.test.ts` 3 cases `beforeEach store=new MemoryStore(); setDataStoreForTests(store):192` `afterEach null` + `Promise.all([executeJob(id1),executeJob(id2)])` trivial `runDiscoveryNode` stub → one `done` one `error.includes("already running"):143` + `HARVEST_LOCK:104 reset:252` (swaps to `acquireHarvestLock` when R1 ships) + dedup `checkDuplicate+claimFingerprints+persistDedupeFromResult` identical `doc.sha256` `text_sha256:81-84 bundleTextKey` → second `duplicate/near_dup:59-60` only one `clusters:76-78` + ordering spy `Date.now() before delegate append:8 vs dedupe:77 vs setJobDone:365 → t_append < t_dedupe < t_done` `harvest.ts:214-247` `VITEST!==true guard:215 preserves FS-skip` | busy/dedup/ordering `vitest` | `npm run test vitest run hold-r3.test.ts` repeatable |
| M-R4 bundle | `proof-bundle.ts ≤70 lines HarvestProofBundle version:"1.0.0" jobId ranAtIso providers ledgerDigest ledgerTail dedupeDigest stateDigest capturedAt manifest?{deployId,daemonSnippet}` + `buildProofBundle via getLedgerTailKV(20) sha256(JSON.stringify(entries)) node:crypto:4` + `persistProofBundle s.put("discovery:proof:bundle") single-key atomic + mkdirSync+writeFileSync state/production-harvest-proof.json try/catch EROFS warn` + `PROOF_BUNDLE_ENABLED=0 short-circuit` + `harvest.ts:237 wire try{build+persist} catch{appendLog PERSIST-WARN:219} 202 {jobId} unchanged` + `readProofBundle KV-first then file fallback discovery/route.ts:15-45` + `tests/proof-bundle.test.ts MemoryStore → bundle non-null ledgerDigest===sha256(entries) version 1.0.0` + optional `GET /api/dev/discovery/proof` admin-gated `health/route.ts:16` | bundle digest repro + state file valid | `vitest proof-bundle.test.ts` + `node scripts/vault-sync.mjs --check` |

**Global proof before handoff (`plan:263,296` / `work-order:263,300`):** `npm run lint && npm run typecheck && npm run test (vitest holds R3+R1/R2) && npm run build && node scripts/vault-sync.mjs --check` green + `git diff --exit-code -- vault/views state/vault-notes.json` if unchanged — present in every module's DoD `§7` `291-297`. Verified conservative: lint/typecheck/build not mutated by tests but still gated.

**No over-tested fixtures:** each non-trivial branch has ONE runnable check (assert-based `vitest`), trivial `title` etc none — matches ponytail "non-trivial logic leaves ONE runnable check" rubric.

**RC-3 detail — R3 second bullet scope:** `R3-regression-tests.md:29-31` requires `One test proves onRun is invoked exactly once under repeated terminal fetches for same job id` referencing `provider-health.tsx:?? lastOnRunJobId` — plan's M-R3 second case instead covers `persistDedupeFromResult` idempotence `dedupe.ts:59 shaHit near_dup:60 clusters:76` at `plan:140-143` / `work-order:117` per `grilling-HOLD.md:50` reinterpretation. Report `§2 R3` similarly narrows to `checkDuplicate/claimFingerprints`. This is a ticket vs grilling divergence. Either restore onRun spy (fake `fetchJobById` mock + `lastOnRunJobId` guard) or amend ticket acceptance to record that `onRun` dedup is covered by remaining lane `M-R7` provider-health refresh parity and R3 intentionally tests storage-layer dedup only — add plan note `R3 AC:30 onRun covered by work-order-remaining M-R7`. Without note, worker dispatch may claim R3 green while ticket's `onRun` AC stays unchecked.

---

## 7. Rollback / Modularity / Scope — PASS

- **Modularity:** 4 modules = 4 distinct operational domains (KV lock `SET NX EX harvest:lock` vs ledger `INDEX_KEY:5 ENTRY_PREFIX:4` vs test surface vs proof `discovery:proof:bundle`) — single-responsibility per file seam, interface small (`acquired:boolean,release`, `limit,store?`, `build/persist/readBundle`), deletion test passes per `report §8` speculative deferrals `380-394`.
- **Rollback:** implicit is `git revert <module-commit>` because plan enforces **one module → one commit with explicit `git add <touches>` only** `plan:261,305,384` `work-order:298,325` staging audit — each module's touches set is its revert boundary (`M-R1: delete harvest-lock.ts + store.ts:setIfAbsent + harvest.ts wire → HARVEST_LOCK bool only` `plan:106`; `M-R2: revert ledger.ts:4-40` `134`; `M-R3: delete tests file` `160`; `M-R4: delete module+wire+artifacts PROOF_BUNDLE_ENABLED=0 toggle` `189`). No migration, no schema change.
- **Scope containment:** `R5–R15,R19` 12 OPEN correctly `OUT OF LANE` `plan:27-39` `work-order:26,34` as `work-order-remaining.json 8 modules self-approving remaining frontier` `plan:13`; FOG `R16:78 R17:82 R18:87` correctly `OUT OF SCOPE` triaged 2026-08-31 `plan:38`; M1–M8 correctly `DEFERRED — no code per AD-30` `plan:40,192-202` `work-order:16-25` `AD-30 FALSE HITL/FOG gate` per `grilling-HOLD.md:74-75` `M1–M8 BLOCKED` no dispatch — no speculative platform drift.

---

## 8. Risks & mitigations — PASS

Plan §8 `301-313` + report §7 `366-376` matrix cover 7 risk families with bite location, mitigation, ponytail ceiling & upgrade path — no missing carrier:
KV split-brain `store.ts:136-180 fallback shadow` → R1 WARN + file mirror, ledger race `ledger.ts:12-26` RMW → R2 per-entry `SET NX hint 30d + merge sort slice(-500) + R1 lock primary`, orphan dirty `ledger.ts:36` → R2 tail prune `idx.filter !orphanSet` + `withPersistenceSingleWriter:7`, TTL expiry `harvest.ts:166-172 budget` → fixed 120 TTL ceiling vs `HARVEST_NODE_POLL_BUDGET` + holder guard + heartbeat deferred, `KV vs Memory` fallback, file ROFS `dedupe-persist.ts:70 EROFS harvest.ts:312 swallow proof-bundle.ts same`, staging poison `AGENTS.md:21-23`, vault poison `vault-sync.mjs:16-44` `ci.yml:35-50`. Each mitigation is hazard serialization or ponytail ceiling with deferred upgrade to `R1/R2` CAS / per-cell locks `MAP.md:30`.

---

## 9. Acceptance (global) — PASS

Work-order `313` global acceptance enumerates exactly 4 self-approving HOLD modules per `AD-26 AD-27 AD-28 AD-29-TRUE-MV-bundle` with touches for `store.ts+harvest-lock.ts+harvest.ts` `ledger.ts+single-writer.ts` `hold-r3.test.ts` `proof-bundle.ts+harvest.ts+proof route+state/production-harvest-proof.json+proof-bundle.test.ts` plus locks `vault-state-single-writer harvest-single-writer discovery-ledger-index-single-writer datastore-seam-single-writer proof-bundle-single-writer`, parallel groups `P parallel disjoint, S harvest.ts sequential`, testability `MemoryStore + vault-sync --check + vitest + typecheck/lint/build green`, staging hygiene `explicit git add <paths> never -A` — matches plan §4 per-module `[ ]` checkboxes (6-7 per module) + global gates §7. Every acceptance is file:line citable and `vitest` runnable.

---

## 10. Self-approve verdict per module (grilling → decisions override → independent review)

Protocol: self-approve where evidence in ticket MDs/CHARTER.md/AGENTS.md/implemented seams is sufficient; otherwise NEEDS REVIEW/HITL. Grilling `Self-approve: Yes` at `grilling-HOLD.md:24,39,52,65` for R1–R4 MV overridden by decisions AD-26..30 where contract fully documented and seam reuse mechanical.

| Module | Tracker `hitl` & grilling | Decision `AD` | Plan §10 verdict | **Independent review verdict** | Rationale (cite) |
|---|---|---|---|---|---|
| **M-R1 harvest lock** | `R1:5 hitl:false` `blocked_by:[]:8` contract `19-27 SET NX EX` `["SET",key,"1","NX","EX",3600]` + `R1 AC:30-35` two `acquire→one true one false` + fallback WARN `:32` TTL config `:33` + grilling `24 Yes` reversible where KV gateway available `25 Gate HITL if KV not available` | **AD-26 TRUE** `decisions:30` reversible minimal wiring `setIfAbsent + harvest-lock.ts` + `executeJob` wire after `118-130` before `157` `finally release holder-guarded DEL` | TRUE self-approving | **TRUE — SELF-APPROVE (with RC-1,RC-2)** | Contract fully in ticket `R1:19-27` + code seams `store.ts:16-26,60-126` `harvest.ts:99-105,118-130,140-149,252-255` already wired + KV auth `store.ts:131` reuse no new secret; ponytail global `harvest:lock 120s TTL ceiling` per `MAP:30` + 3600 configurable per-call `work-order:64` covers ticket default 3600 — document `ttlSec=120` default grilling vs 3600 ticket configurable to close RC-2; Test `MemoryStore` concurrent true/false+release is mechanical. **RC-1 fallback delegation must land** otherwise fallback shadow reintroduces burst race. No HITL unless KV gateway absent `grilling:25`. |
| **M-R2 ledger heal** | `R2:5 false` `8 []` `19-25` atomic index-entry + orphan sweep `::42-43` + `R2 AC:28 50-concurrent no dups :29 drops orphans :30 tests still pass` + grilling `39 Yes` local ledger path `40 Gate HITL if orphan corrupts` | **AD-27 TRUE** `51` `ledger.ts:4-40` per-entry `setIfAbsent discovery:ledger:index-entry:{seq} 30d` + `INDEX_KEY merge sort slice(-500) put` + `getLedgerTailKV orphan prune withPersistenceSingleWriter:7` | TRUE | **TRUE — SELF-APPROVE** | Signatures unchanged `appendLedgerKV:8 getLedgerTailKV:28` keep `slice(-500):25` window `out of scope R2:34` ; `R1 lock primary serialization` shrinks race to TTL-expiry window only `report §6`. `50-concurrent + orphan inject` test via `MemoryStore` foreseeable per `R2:19-25` + `grilling:36-37`. No schema migration, revert by `ledger.ts` diff. No HITL. |
| **M-R3 regression tests** | `R3:5 false` `8 []` `18-24` busy-harvest + parent-reload dedup + persist-before-done `:29-32` + grilling `52 Yes` tests non-invasive `53 Gate HITL if KV-backed locks` | **AD-28 TRUE** `71` one file `hold-r3.test.ts` via `MemoryStore:28-57` `setDataStoreForTests:192` `VITEST!==true guard:215` | TRUE | **TRUE — SELF-APPROVE (with RC-3 note)** | In-process `HARVEST_LOCK:104` + `setJobError:369` + `appendLedgerKV:8` `persistDedupeFromResult:77` `setJobDone:365` at `harvest.ts:214-247` ordering `t_append < t_dedupe < t_done` plus `checkDuplicate:39 shaHit:59 textHit:60 claimFingerprints:65-79 clusters:76` are all hidden behind `DataStore` seam; `vitest` already at `package.json:14` no new runner, `MemoryStore.m Map:30` isolated hermetic. **RC-3 onRun scope note required** — either add `provider-health.tsx onRun` spy or document `R3:30 onRun covered by remaining M-R7`. If note added, stays self-approving; HITL only if KV-backed locks required (they aren't `harvest.ts:215` guard). |
| **M-R4 proof bundle** | `R4:5 hitl:true` `open []:8` but brief distinguishes `ledgerGrowth.verified:false R4:21` `productionDeploymentVerified:false` `daemonVerified:false` `R4:27-31` requires live `auditorai-gamma.vercel.app/api/dev/discovery` + `launchctl print gui/$(id -u)/com.auditorai.discovery | grep state :21` `verified true with launchctl snippet` + grilling `65 Yes for MV bundle tightly scoped reversible toggle` `65-66 Gate HITL if no durable path` | **AD-29 TRUE for MV bundle · HITL for live daemon/deploy** `95` `harvest.ts:237 wire after setJobDone` `proof-bundle.ts ≤70 lines version:"1.0.0" build/persist/readBundle` `put discovery:proof:bundle single-key atomic` `state/production-harvest-proof.json mkdir+write EROFS warn+continue PROOF_BUNDLE_ENABLED=0` `GET /proof admin-gated health/route.ts:16` `manifest.daemonSnippet best-effort` | TRUE for MV bundle generation; HITL for live | **TRUE for MV bundle · HITL for live (bundle self-approve, live NEEDS REVIEW)** | MV is small reversible (`delete module+wire+artifacts` toggles `PROOF_BUNDLE_ENABLED=0`) per `grilling:65` no core path change `harvest.ts:219 swallow` `202 {jobId} unchanged` `426`; bundle `ledgerDigest sha256(JSON.stringify(entries)) node:crypto:4` + `ledgerTail:{total,entries}` + file mirror `state/production-harvest-proof.json` deterministic but **not** vault-compiled `state/vault-notes.json` only `ci.yml:35-50` — staging explicit `git add` `AGENTS.md:21-23` + `vault-sync --check` keeps CI green `AD-29:91`. **Live capture stays HITL/FOG** per `R4:5 hitl:true` + AC `ledgerGrowth.verified/productionDeploymentVerified/daemonVerified` `27-32` blocked→resolved requires `launchctl active count >0` + deploy id — cannot auto-flip in local worktree `report §4 AD-29`. After bundle ships, live evidence `.autoforge/validation/ops-loop-evidence-live.json` can cmp `ledgerDigest` like `vault-sync.mjs:31 Buffer.equals` but HITL confirms. |
| **M1–M8 platform** | `M1:103-104 BLOCKED CITATIONS .autoforge/plans/plan.md:66-68` … `M8:138-140 BLOCKED` + `grilling-HOLD.md:69-76 HITL/FOG at :74` `Staging decision: HITL/FOG gate` | **AD-30 FALSE HITL/FOG** `99-107` `M1 Quote-bearing GF-6..10, M2 blob hatch, M3 vault UX, M4/M5 assists, M6 retention, M7 RSC, M8 Postgres third adapter` `M8 without domain model violates one-adapter hypothetical` `MAP:14 no new deps` `CHARTER:62-74 curated-notes ownership` | DEFERRED no code | **FALSE — NEEDS REVIEW (HITL/FOG) ✅ correctly deferred** | Platform tickets imply `DataStore` third adapter, vault UX, workflow assists needing `CONTEXT.md` glossary + seam map + ADRs (grill-with-docs) before `DataStore` third adapter `store.ts:28-126` second variation proved seam but third is irreversible without migration. Keep `BLOCKED HITL/FOG` no sprint. |

**Summary self-approve:** **4/4 frontier MV self-approving for code** (R1 TRUE, R2 TRUE, R3 TRUE with onRun note, R4 TRUE for bundle / HITL for live) per `AD-26 AD-27 AD-28 AD-29-TRUE-MV-bundle`; **R4 live daemon/deploy HITL + M1–M8 HITL/FOG FALSE** per `AD-29 HITL-for-live` + `AD-30` correctly not dispatched.

---

## Findings (code-review two-axis)

### Standards — within tolerance (ponytail + lean-build enforced) — PASS with RC-1 note

- Documented standards cited: `AGENTS.md:15-18` vault determinism (HEAD worktree `vault-sync.mjs:17,19,31,42`), `21-23` staging hygiene `git add <paths> never -A`, `docs/validation/eval-gates.md:31-38` thresholds frozen, `vault/CHARTER.md:14-20` zones + `69-71` `state/*.json` canonical, `workflow/wayfinder/maps/ops-residual/MAP.md:14 no new deps + 16-17 ceiling notes`, `MAP:9 local-markdown ops-residual` — all respected.
- Smell baseline: **No Mysterious Name** (`acquireHarvestLock`, `setIfAbsent`, `appendLedgerKV`, `getLedgerTailKV`, `buildProofBundle` honest); **No Duplicated Code** (KV `SET NX` encoded once in `store.ts:68-99 call`, reused by harvest-lock + ledger hint); **No Feature Envy** (lock owns holder/TTL/DEL guard, not scattered `executeJob`); **No Data Clumps** (touches as file-list minimal); **No Primitive Obsession** (`HarvestProofBundle{version,ledgerDigest,ledgerTail}` typed); **No Repeated Switches**; **No Shotgun Surgery** (one lock per file: harvest vs ledger vs proof); **No Divergent Change** (docs vs code vs tests own files); **No Speculative Generality** (`INCR harvest:seq`, per-cell locks `MAP:30`, heartbeat `EXPIRE`, pipeline `MULTI/EXEC EVAL` all explicitly skipped with ponytail ceiling); **No Message Chains**; **No Middle Man** (orchestrator `harvest.ts` delegates, not pass-through). Only `Speculative Generality` risk was present and **correctly suppressed**.
- Ponytail enforced: reuse `DataStore/MemoryStore/getDataStore/setDataStoreForTests:192`, `withPersistenceSingleWriter:7`, stdlib `fs/path/crypto randomUUID`, no new deps — shortest diff wins.

### Spec — faithful (no scope creep, one intentional narrow) — PASS with RC-2/RC-3

- Each spec line traced: `R1:19-27` → plan `91-95` `store.setIfAbsent` + `harvest-lock.ts` + `executeJob:140-149,252` acquire before `D00-QUEUED` release in `finally`; `R2:19-25` → `114-118` `appendLedgerKV per-entry hint + getLedgerTailKV orphan prune` `R2 AC:28 50 entries AC:29 orphan AC:30 tests pass`; `R3:18-24` → `138-144` 3 tests via `MemoryStore` (lock busy + dedup + ordering) — grilling narrow respected but ticket `onRun` diverges (RC-3); `R4:18-33` → `162-172` `proof-bundle.ts ≤70 lines` `state/production-harvest-proof.json + KV discovery:proof:bundle single put` `harvest.ts:237` wire `PROOF_BUNDLE_ENABLED=0` `bundleDigest = sha256(JSON.stringify(ledgerTail.entries))` + deferred live `launchctl+deploy` HITL — matches `R4:27-32` blocked→resolved requires live.
- Missing or partial: `R3:30 onRun` not covered by storage dedup test (see RC-3); `R1 TTL` doc drift (RC-2); `fallback delegation` (RC-1). No extra behaviour beyond spec (no `INCR allocator`, no per-cell locks, no vault sync bearing, no GH sync) — scope faithful for 4.

---

## Required changes vs notes

| Finding | Severity | Location | Fix (self-approving amendment) | Blocking dispatch? |
|---|---|---|---|---|
| **RC-1** `createFallbackStore` lacks `setIfAbsent` delegation | **CHANGES_REQUIRED** | `src/lib/persistence/store.ts:130-181` wrapper `136-180` + `plan-HOLD.md:94-96` `work-order-HOLD.json:191-196` `datastore-seam-single-writer` policy | Extend fallback object with `async setIfAbsent(key,val,ttl){ try{ if(kv.setIfAbsent) return await kv.setIfAbsent(key,val,ttl); return false; }catch{ return fallback.setIfAbsent(key,val,ttl); } }` and add `MemoryStore.setIfAbsent` TTL `setTimeout unref` at `28-57` already `AD-26`; add `createFallbackStore:130-181` to `M-R1 touches` note; add acceptance bullet `fallback wrapper delegates setIfAbsent` | **Yes** — silent KV→Memory shadow reintroduces burst race `harvest.ts:140` if not fixed |
| **RC-2** TTL default ambiguity `R1:33 3600s` vs `grilling-HOLD.md:18 120s` vs `plan:92 ttlSec=120` vs `work-order:64 TTL default 120 or 3600 documented` | **CHANGES_REQUIRED** | `R1-cross-instance-harvest-lock.md:33` + `grilling-HOLD.md:18` + `plan-HOLD.md:92` `M-R1 Outputs ttlSec=120` + `work-order:64` + `report-HOLD.md §3.1 chosen 120s` + `harvest.ts:166-172 HARVEST_NODE_POLL_BUDGET` | Pin canonical: `acquireHarvestLock(store?,ttlSec=120,holder?)` default 120s per grilling (+ `HARVEST_NODE_POLL_BUDGET` bound doc), note ticket `3600s` is configurable per-call `acquireHarvestLock(store,3600)` for long jobs; add to interfaces `plan §5 report §5` `ttlSec defaults 120, pass 3600 per R1 AC:33`. One-line doc edit, no code. | **Yes** — prevents 30x sleep mismatch and second harvest sneak-in after 120s vs 3600s expectation |
| **RC-3** R3 `onRun` AC not covered | **CHANGES_REQUIRED (or documented deferral)** | `R3-regression-tests.md:29-31` `One test proves onRun is invoked exactly once under repeated terminal fetches` vs `plan:142-144` `checkDuplicate/claimFingerprints` + `grilling-HOLD.md:49-50 callback dedup` + `R3-regression-tests.md:18-24` `work-order-HOLD.json:117` + `remaining lane M-R7 provider-health-refresh` | Either (a) add `onRun` spy `tests/discovery/hold-r3.test.ts` fourth case mocking `fetchJobById` `lastOnRunJobId` exactly once per `provider-health.tsx` contract — ponytail single extra case ok; OR (b) amend plan acceptance to state `R3:30 onRun dedup covered by remaining lane work-order-remaining M-R7 Refresh parity (provider-health.tsx title/ dedup bypass) — R3 HOLD narrow covers storage-layer dedup only` and close ticket bullet as intentional split with owner ack. Self-approving note, no HOLD HITL. | **Yes if keep storage dedup only without note** — ticket would stay technically incomplete |
| **N-1** `state/production-harvest-proof.json` vs ticket `.autoforge/validation/ops-loop-evidence-live.json` path | **Note** | `R4:22 store under .autoforge/validation/ops-loop-evidence-live.json and mirror to stages/07_validate/output/` vs `plan:171-172 state/production-harvest-proof.json deterministic path AD-29` `work-order:149` `stateProofPath` | Keep `state/production-harvest-proof.json` as primary (machine-canonical per `CHARTER.md:20 vault/CHARTER state/*.json` + staging hygiene `AGENTS.md:21-23 explicit git add`), document secondary `.autoforge/validation/ops-loop-evidence-live.json` as **read-only validation copy** written by `readProofBundle → JSON.stringify` for validator `ops-loop-evidence.json:11-28` compatibility — not committed as source-of-truth (`.autoforge` staging churn `MAP.md:15`). Add one-line mapping doc to `M-R4 Outputs`. | No — state path is more correct; ticket path is observation bundle after 24h window — complementary, not conflicting |
| **N-2** `HARVEST_LOCK_KEY` scoping `harvest:lock` vs future `harvest:lock:{wsHash}` | **Note** | `decisions-HOLD.md AD-26` `HARVEST_LOCK_KEY="harvest:lock" (future scoped harvest:lock:${workspaceHash(ws)} via workspaceHash at store.ts:196)` vs `plan:94` single global key | Global `harvest:lock` 120s is explicit ceiling per `MAP.md:16` + `report §7 368` `ponytail: per-cell locks deferred`; single global suffices for MV where workspace is single-tenant; document that multi-workspace scoping `workspaceHash` lands only if `KV workspaceKey` sharding measured — no code now. | No |
| **N-3** Ledger heal uses `withPersistenceSingleWriter:7` in-process guard not cross-instance | **Note** | `ledger.ts:39` + `single-writer.ts:7-17` `throws persistence write in progress` + `work-order:88` `O(500) scan ceiling` | Correct ceiling — true KV CAS `EVAL` pipeline deferred per `store.ts:64-67` pipeline rejection + `AD-27` `report §3.2` ponytail; `R1 harvest:lock` primary serializes writers so hot window = TTL expiry only. Document that second writer after TTL sees best-effort `catch(()=>{})` merge, healed on next `getLedgerTailKV` read. | No |
| **N-4** `DAG.topological_order` flattened serialization masks P parallelism | **Note** | `work-order-HOLD.json:164` `["M-R1","M-R2","M-R3","M-R4"]` vs `waves:241-274` correctly parallel `P [M-R1-seam M-R2 M-R3 M-R4-module]` | Scheduler must use `waves` + `parallel_safe_groups:275-285` not flat `topological_order` for dispatch; optionally emit `topological_order: [[P-group],[S1],[S2]]` or remove flat field. | No |

**No other CHANGES_REQUIRED** — remaining enumeration / overlap / parallel / ponytail / testability / deferred flags are PASS as above.

---

## Parallel safety re-verification (independent grep)

Cross-checked `rg "harvest:lock"` zero per `report-HOLD.md:30` plan gap check `store.ts:60-126` + `harvest.ts:99-105` confirms lock key new; `rg "INDEX_KEY|discovery:ledger:index"` appears only in `ledger.ts:5` `harvest.ts:281-289` tail derive + `dedupe-persist.ts` not HOLD — `M-R2` is sole writer this lane; `rg "src/discovery/harvest.ts"` appears only in `M-R1+M-R4` hazard_touches → must serialize ✅ flagged `249`; `rg "src/lib/persistence/store.ts"` only `M-R1` extender → `datastore-seam-single-writer` singleton ✅; `rg "state/production-harvest-proof.json"` only `M-R4` ✅ `vault-state+proof-bundle` guards; `rg "acquireHarvestLock"` would hit only `harvest-lock.ts + harvest.ts wire + tests` per plan `94`; `rg "setIfAbsent"` would hit `store.ts + ledger.ts hint + harvest-lock.ts` distinct keys `harvest:lock` vs `discovery:ledger:index-entry:` → no key collision. No hidden caller missed — every caller of `setIfAbsent/put/get/appendLedgerKV/getLedgerTailKV` routes through declared `DataStore` seam `store.ts:16-26,60-126` per `report §1` map-ownership `TRACKER.md:14 MAP.md:9,39`.

---

## Traceability (what was pinned)

- `tracker-index.md:3-4` R1 `BLOCKED` `MAP.md:41-42` + `:7 R2 :42-43` + `:13 R3 :43-44` + `:18 R4 :44-45 hitl:true`
- `grilling-HOLD.md:10-26 R1 MV 6 steps Self-approve Yes :24 Gate HITL if KV gateway not available :25` + `28-41 R2 4 steps Yes :39 Gate orphan risks corruption :40` + `43-54 R3 3 steps Yes :52 Gate KV locks :53` + `56-67 R4 3 steps Yes :65 Gate durable path :65-66` + `69-76 M1–M8 HITL/FOG at :74`
- `report-HOLD.md:1-40` seams `DataStore store.ts:16-26 MemoryStore:28-57 KvRestStore:60-126 getDataStore:183-188 setDataStoreForTests:192-194` + `Job store jobs.ts:41-42 createJob:133-197 getJob:199-215 listJobs:223-274 updateJob:276-318` + `Harvest HARVEST_LOCK:99-105 executeJob:106-256 persistDiscoveryState:258-334 harvest:336-429` + `Ledger ledger.ts:4-40 ENTRY_PREFIX:4 INDEX_KEY:5 appendLedgerKV:8-26 getLedgerTailKV:28-39` + `Dedupe dedupe-persist.ts:12-109 keys.ts:50` + `Single-writer:7` + `HealthAggregate:20-61`; §2 boundaries per HOLD; §3 alternatives design-it-twice; §5 interfaces; §6 locks `blocked_by [] hazard-only`; §7 risks ponytail ceilings `global harvest:lock 120s TTL, slice(-500) O500 hint+lazy heal, dedupe writeQueue, proof single KV, vault worktree`
- `decisions-HOLD.md AD-26 R1 TRUE reversible SET NX EX + harvest-lock.ts` `AD-27 R2 TRUE per-entry hint+lazy heal` `AD-28 R3 TRUE one file MemoryStore` `AD-29 R4 TRUE MV bundle HITL live daemon/deploy` `AD-30 M1–M8 FALSE HITL/FOG` `AD-31 seam depth` `AD-32 locks` `AD-33 risks`
- `MAP.md:9,14-17` local-markdown only ops-residual `no new deps` `explicit accepted ceilings` + `41-45` tickets R1–R4
- Tickets `R1:1-40 [] hitl:false R1 AC:30-35` `R2:1-35 [] false 50-concurrent AC:28 orphan AC:29` `R3:1-37 [] false busy AC:29 onRun AC:30 ordering AC:31` `R4:1-36 [] hitl:true ledgerGrowth.verified daemonVerified productionDeploymentVerified:27-32`

---

## Acceptance criteria for this review task

- [x] covers enumeration **4/4** (R1,R2,R3,R4 BLOCKED at `tracker-index.md:3-20` → `M-R1,M-R2,M-R3,M-R4-bundle` self-approving; `work-order-HOLD.json:7-15` `tracker_index_verbatim` 4; R5–R15,R19 OPEN + R16–R18 FOG + M1–M8 BLOCKED correctly `OUT OF LANE`/`OUT OF SCOPE`/`DEFERRED` per `plan:27-41` `work-order:16-30` `decisions AD-30`)
- [x] touches **overlap guard (harvest-single-writer)** verified — `M-R1-wire ↔ M-R4-wire` share `src/discovery/harvest.ts:99-256` → `harvest-single-writer` sequential `plan:99,174,249` `work-order:177,286`; disjoint `store.ts vs ledger.ts vs tests vs proof-bundle.ts` correctly `parallel_safe` `245-248 275-284`; `vault-state-single-writer` for `state/production-harvest-proof.json` `224/168`; fallback RC-1 flagged
- [x] **parallel safety** verified — waves `P parallel disjoint [M-R1-seam M-R2 M-R3 M-R4-module]` → `S serialize harvest.ts [M-R1-wire → M-R4-wire]` → `V vault-sync --check` → `H M1–M8 HITL` with hazard locks `harvest-single-writer, discovery-ledger-index-single-writer, datastore-seam-single-writer, proof-bundle-single-writer, vault-state-single-writer` `222-232` `168-211`; DAG `edges []` because `blocked_by:[]` roots `R1:8 R2:8 R3:8 R4:8` sequencing hazard-only `208-216` `159-167`; scheduler split `P-seam` vs `S-wire` explicitly `239-241,255-260` `286` module boundary ≠ child-session boundary
- [x] **ponytail** verified — ladder rung cited per module, ceilings named (`harvest:lock 120s TTL global per-cell deferred MAP:30`, `O500 hint+lazy heal on read`, `one test file is ceiling`, `single last-write-wins KV+file is ceiling` `store.ts:4 crypto` `store.ts:30 MemoryStore` `single-writer.ts:7`), no new dep `MAP.md:14`, stdlib `randomUUID` reuse `work-order:323` `plan:55,97,110,133,158,188`
- [x] **testability** verified — per-module `vitest`+`MemoryStore setDataStoreForTests:192`+`VITEST!==true:215` runnable acceptance `plan:100-107 124-130 150-155 178-185` + global `lint+typecheck+test+build+vault-sync --check` `263 296`; `hold-r3.test.ts` 3 cases + `proof-bundle.test.ts` digest repro `ledgerDigest===sha256(JSON.stringify(entries))` ; RC-3 onRun note
- [x] **deferred M1–M8 correctly flagged** — `tracker-index.md:103-140` `M1–M8 BLOCKED` + `grilling-HOLD.md:69-76 HITL/FOG at :74` → `plan:40 DEFERRED — no code per AD-30` `192-202` wave `H — HITL gate blocked until owner HITL ack` `242` + `work-order:16-25 deferred_verbatim` `AD-30 FALSE HITL/FOG` `32,36`; no worker code, `plan-decision-single-writer` only
- [x] **HITL gates** verified — `R4:5 hitl:true` + AD-29 split: MV bundle self-approving `plan:189,65` `work-order:149 PROOF_BUNDLE_ENABLED=0` reversible, live `ledgerGrowth.verified/productionDeploymentVerified/daemonVerified` `R4:27-32` + `launchctl print gui/$(id -u)/com.auditorai.discovery | grep state` `R4:21` stays `HITL for live (hitl:true not auto-flipped)` `grilling:65-66 Gate HITL if no durable path` `decisions AD-29:95` correctly deferred

---

**Artifact path:** `.autoforge/reviews/plan-HOLD-critique.md` (this file)
**Next step:** amend `plan-HOLD.md:92,94-96,142-144`
  `work-order-HOLD.json:60-65,191-196,117` with RC-1 fallback delegation, RC-2 TTL canonical `120 default (3600 configurable)`, RC-3 `onRun` note; then dispatch `node scripts/vault-sync.mjs --check` baseline green → `P1: [M-R1-seam,M-R2,M-R3,M-R4-module]` parallel (disjoint `store.ts+harvest-lock.ts` vs `ledger.ts` vs `tests/**` vs `proof-bundle.ts`) → `S1: M-R1-wire (harvest.ts acquire)` → `S2: M-R4-wire (harvest.ts persistProofBundle)` → `V: lint+typecheck+test+build+vault-sync --check`; `H: M1–M8` wakes only after owner domain-model + ADRs per `decisions-HOLD.md:104-107` `AD-30`.

**Self-approve per R1–R4 (independent):** **M-R1 TRUE** (with RC-1,RC-2) · **M-R2 TRUE** · **M-R3 TRUE** (with RC-3 note) · **M-R4 TRUE for MV bundle / HITL for live daemon+deploy** — 4/4 self-approving MV; **M1–M8 NEEDS REVIEW (HITL/FOG per AD-30)**.

