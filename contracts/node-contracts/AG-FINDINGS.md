# Node Contract: Deterministic Findings Shaping

<!-- The yaml block below is GENERATED by scripts/gen-node-topology.ts from
     src/domain/pipeline/registry.ts DESCRIPTORS (single source of truth).
     Regenerate via `npm run gen:nodes`; human edits belong between the
     prose:start/prose:end markers only. -->

```yaml
node_id: AG-FINDINGS
title: Deterministic Findings Shaping
role: Gates raw rule outcomes into schema-valid draft findings.
impl: src/domain/pipeline/nodes/findings.ts runFindings
producer: domain-engine
determinism_class: deterministic
emits: findings.deterministic
executed_in_batch: true
state:
  reads:
    - rule_results
  writes:
    - rule_results
depends_on:
  - AG-RULES
```

<!-- prose:start -->
### Purpose

Give process/eligibility outcomes their full Finding form: ids, statement, evidence links, assumptions, rationale, source_trace, reviewer_status=draft.

### Context

- **Domain:** Finding anatomy follows CONTEXT.md and finding.schema.json: typed, reviewable, provenance-carrying, uncertainty-explicit.
- **Jurisdiction:** normative_basis_note records applicability at the native stage id.
- **Project:** finding_id embeds rule_id + stage for cross-jurisdiction uniqueness.
- **Relevant decisions:** ADR-0003 finding model
- **Evidence required:** evidence entries use use=defines_requirement (process) or use=context (eligibility).

### Contract

- **Inputs:** rule_results.deterministic_findings (raw outcomes)
- **Outputs:** Finalized Finding[] within rule_results + artifact payload_kind=findings.deterministic
- **Invariants:**
  - All findings validate against finding.schema.json.
  - reviewer_status starts at draft; risk_components all null; recommendation stays null until a human decides.
- **Forbidden actions:**
  - Emitting kind=safety_concern from deterministic rules.
  - Pre-filling recommendations.
- **Acceptance criteria:**
  - Contract tests bind these exact objects to the committed finding schema.
  - source_trace.origin === deterministic_rule on every emitted finding.

### Failure

- **Retry policy:** Deterministic: no retry.
- **Escalation policy:** A schema violation is a build failure, never runtime coercion.

### Verification

- Schema contract tests bind engine output to committed schemas.
<!-- prose:end -->
