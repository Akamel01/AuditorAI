# Node Contract: Input Manifest

<!-- The yaml block below is GENERATED by scripts/gen-node-topology.ts from
     src/domain/pipeline/registry.ts DESCRIPTORS (single source of truth).
     Regenerate via `npm run gen:nodes`; human edits belong between the
     prose:start/prose:end markers only. -->

```yaml
node_id: AG-MANIFEST
title: Input Manifest
role: Input normalization to §14/§27 states for the selected native stage.
impl: src/domain/pipeline/nodes/manifest.ts runManifest
producer: domain-engine
determinism_class: deterministic
emits: manifest.table
executed_in_batch: true
state:
  reads:
    - stage_context
    - project_input
  writes:
    - input_manifest
depends_on:
  - AG-STAGE-SELECT
```

<!-- prose:start -->
### Purpose

Filter the pack's input requirements to those applying to the selected native stage and resolve each stored input value into a manifest state.

### Context

- **Domain:** Requirement levels (required/recommended/optional) drive downstream completeness rules; resolution logic lives in engine resolveState.
- **Jurisdiction:** Requirement levels come from the jurisdiction pack — never harmonized across jurisdictions.
- **Project:** Reads project_input.input_values keyed by input_id.
- **Relevant decisions:** ADR-0002 canonical stage model
- **Evidence required:** Each manifest entry carries evidence_ids justifying the requirement itself.

### Contract

- **Inputs:** stage_context + pack.inputs + project_input.input_values
- **Outputs:** SharedState.input_manifest + artifact payload_kind=manifest.table
- **Invariants:**
  - 'provided' with a blank value downgrades to the level-appropriate missing state (required_missing / recommended_missing / optional_missing / unknown).
  - conditional_on is preserved for later evaluation.
  - Entries exist only for inputs whose stage_ids include the selected stage.
- **Forbidden actions:**
  - Inventing inputs not present in the pack.
  - Marking anything provided without recorded value/evidence.
- **Acceptance criteria:**
  - GF fixtures reproduce manifest tables byte-stably.
  - Downgrade rules unit-tested per requirement level.

### Failure

- **Retry policy:** Deterministic: no retry.
- **Escalation policy:** Malformed packs are caught by schema validation at CI time, not here.

### Verification

- Property test: every rule-referenced input_id exists in manifest, else the rule is inert.
<!-- prose:end -->
