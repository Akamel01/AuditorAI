# Node Contract: Report Assembly

<!-- The yaml block below is GENERATED by scripts/gen-node-topology.ts from
     src/domain/pipeline/registry.ts DESCRIPTORS (single source of truth).
     Regenerate via `npm run gen:nodes`; human edits belong between the
     prose:start/prose:end markers only. -->

```yaml
node_id: AG-REPORT
title: Report Assembly
role: Deterministic report assembly (JSON literal + Markdown rendering).
impl: src/domain/pipeline/nodes/report.ts runReport
producer: report-builder
determinism_class: deterministic
emits: report.bundle
executed_in_batch: true
state:
  reads:
    - project_input
    - stage_context
    - input_manifest
    - rule_results
    - audit_questions
    - adjudication
  writes:
    - report_bundle
depends_on:
  - AG-ADJUDICATION
```

<!-- prose:start -->
### Purpose

Assemble the final AuditResult (adjudicated findings, questions, limitations, disclaimer) and render its Markdown form for download/print.

### Context

- **Domain:** Reports carry the DISCLAIMER verbatim: assistance, not professional judgment; compliance outputs never imply safety.
- **Jurisdiction:** Limitations include framework-specific notes (e.g., GG 119 assigns no severity scores [EV-UK-024]).
- **Project:** audit_id format AUD-<project>-<stage>; ran_at injected by caller.
- **Relevant decisions:** ADR-0003 finding model
- **Evidence required:** Reports cite evidence by id; readers resolve via the registry.

### Contract

- **Inputs:** adjudication.final_findings + all slices
- **Outputs:** SharedState.report_bundle + artifact payload_kind=report.bundle
- **Invariants:**
  - Identical slices produce byte-identical markdown apart from rendered timestamps.
  - Disclaimer string equals engine.DISCLAIMER exactly.
  - Non-authoritative mapping-confidence warnings appear under limitations.
- **Forbidden actions:**
  - Omitting unaccepted findings from the record (they appear with their status).
  - Softening limitation language.
- **Acceptance criteria:**
  - Report API e2e returns both forms; browser-print PDF path works.
  - Snapshot test pins disclaimer + limitations sections.

### Failure

- **Retry policy:** Deterministic: no retry.
- **Escalation policy:** Rendering failure is a plain 500 — no partial reports ship.

### Verification

- Integration flow covers the report leg.
<!-- prose:end -->
