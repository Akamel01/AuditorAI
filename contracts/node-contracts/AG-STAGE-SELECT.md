# Node Contract: Stage Resolution & Eligibility

<!-- The yaml block below is GENERATED by scripts/gen-node-topology.ts from
     src/domain/pipeline/registry.ts DESCRIPTORS (single source of truth).
     Regenerate via `npm run gen:nodes`; human edits belong between the
     prose:start/prose:end markers only. -->

```yaml
node_id: AG-STAGE-SELECT
title: Stage Resolution & Eligibility
role: Jurisdiction→Framework→NativeStage resolution with eligibility checks.
impl: src/domain/pipeline/nodes/stage-select.ts runStageSelect
producer: domain-engine
determinism_class: deterministic
emits: context.snapshot
executed_in_batch: true
state:
  reads:
    - project_input
  writes:
    - stage_context
depends_on:
  - AG-PROJECT
```

<!-- prose:start -->
### Purpose

Validate stage_selection against the jurisdiction pack: native stage exists, is in MVP scope, and carries its canonical-stage mapping plus confidence label.

### Context

- **Domain:** Native Stage vs Canonical Stage is ADR-0002 law; bare stage numbers have NO cross-jurisdiction meaning [EV-CA-006].
- **Jurisdiction:** Exceptions are data, not code: UAE combined Stage 1/2, UK has no Stage 0, US uses named phases.
- **Project:** Uses only project_input.stage_selection.
- **Relevant decisions:** ADR-0002 canonical stage model
- **Evidence required:** Pack exceptions carry evidence_ids surfaced on rejection (max 3; engine requireStage).

### Contract

- **Inputs:** project_input.stage_selection + jurisdiction policy pack
- **Outputs:** SharedState.stage_context + artifact payload_kind=context.snapshot; throws StageNotEligibleError when ineligible
- **Invariants:**
  - canonical_stages subset of {FEASIBILITY_CONCEPT, PRELIMINARY_DESIGN, DETAILED_DESIGN}.
  - mapping_confidence in {authoritative, interpreted, inferred} — never hidden from users.
  - mvp_scope=false stages are ineligible and reported with display_name.
- **Forbidden actions:**
  - Silently substituting a different stage when selection fails.
  - Dropping confidence labels for UI convenience.
- **Acceptance criteria:**
  - Every golden fixture's jurisdiction resolves its declared stage.
  - Unknown stage id raises StageNotEligibleError carrying exception evidence_ids.

### Failure

- **Retry policy:** Deterministic: no retry.
- **Escalation policy:** Ineligibility surfaces as user-facing 422 through serverError mapping.

### Verification

- Jurisdiction exception stages (UAE S0 / combined S12, UK absence of S0) stay covered by tests.
<!-- prose:end -->
