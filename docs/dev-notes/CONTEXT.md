# dev-notes

Working engineering notes that are NOT evidence-registry sources.

- **Reads:** nothing (reference material).
- **Does:** holds one-off findings/investigations (KV image limits, LLM access,
  drawing corpus, stage normalization, standards-conflict analysis).
- **Writes:** nothing downstream; nothing here is compiled into
  `state/evidence-registry.json` (only `docs/research/*-rsa-research.md` is).
- **Human check:** if a note hardens into quotable doctrine, migrate its claims
  into the matching `docs/research/<jur>-rsa-research.md` with provenance.
