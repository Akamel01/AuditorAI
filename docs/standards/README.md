# Standards Library

Local snapshots/extracts of authoritative standards material relied upon by policy
packs: stage definitions, required inputs/outputs, process requirements, with
revision identifiers. Nothing here is invented; every item traces to an evidence
record in `state/evidence-registry.json`. Where a standard's text cannot be
redistributed, store structured summaries + citations, not full text.
