{
  "name": "auditorai",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "eval": "tsx scripts/run-eval.ts",
    "gen:nodes": "tsx scripts/gen-node-topology.ts",
    "readiness": "tsx scripts/readiness-report.ts",
    "render-learning-html": "tsx scripts/render-learning-html.ts",
    "vault": "node scripts/compile-evidence.mjs && node scripts/vault-export.mjs && node scripts/vault-import.mjs",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run build",
    "ci:local": "npm run lint && npm run typecheck && npm run test && npm run build && node scripts/validate-state.mjs && node scripts/compile-evidence.mjs && git diff --exit-code -- state/evidence-registry.json && node scripts/vault-export.mjs && node scripts/vault-import.mjs && git diff --exit-code -- vault/views state/vault-notes.json && node scripts/check-eval-gate-freshness.mjs && node scripts/check-evidence-head.mjs",
    "prepare": "husky install 2>/dev/null || git config core.hooksPath .githooks 2>/dev/null || true"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "next": "^15.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "unpdf": "^1.8.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@tailwindcss/postcss": "^4.1.0",
    "@types/node": "^22.10.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "eslint": "^9.20.0",
    "tailwindcss": "^4.1.0",
    "tslib": "^2.8.0",
    "tsx": "^4.23.12",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.20.0",
    "vitest": "^3.0.0",
    "yaml": "^2.9.0"
  }
}
