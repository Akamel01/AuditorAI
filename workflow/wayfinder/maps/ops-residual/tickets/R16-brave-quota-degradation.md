---
id: R16
title: Brave Search quota degraded — graceful fallback + monitoring
type: task
hitl: false
status: open
assignee:
blocked_by: []
blocks: []
created: 2026-08-31
resolved:
---

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**
- discovery-harvest hourly runs 33400187043 (2026-08-31T14:01) and 33361578079 (2026-08-31T05:44) both failed: `brave-search discovered nothing: HTTP 402 USAGE_LIMIT_EXCEEDED` — monthly quota 5.0/5.0 exceeded.
- 70a519c fixes CI lint (`as any` disable) and makes brave-search return empty on 402 instead of throwing, so pipeline continues with seed-portals — harvest no longer hard-fails but is degraded (0 Brave hits).
- Deploy `70a519c` ci/deploy both green (ci 1m38s, 5 jobs; deploy 10s) at 2026-08-31T19:32.

**What we still need:**
- Quota replenishment or plan upgrade / key rotation.
- Monitoring/alert on 402 so next quota breach is visible without checking Actions logs.
- Fallback ordering: when Brave is empty, ensure Google CSE or seeds still cover gaps; record refusal counts in ledger.

## Agent Brief

**Category:** bug
**Summary:** Harvest degrades gracefully on Brave 402 but ops has no signal and no fallback ranking.

**Current behavior:** `src/discovery/providers/brave-search.ts:58-83` now returns `[]` on 402 and `pipeline.ts` continues; jobs complete via `PERSIST-WARN` path. No metric, no ledger refusal, no alert.

**Desired behavior:** On 402, (1) append a ledger refusal entry `brave:USAGE_LIMIT_EXCEEDED`, (2) emit a structured log `brave quota 402` at `WARN` level, (3) pipeline tries Google CSE before falling back to seeds (existing ordering already does; just ensure not short-circuited). A lightweight monitor (Actions job `harvest-health` or `health` route) surfaces `brave: degraded` when last 2 runs had 0 Brave hits.

**Key interfaces:**
- `BraveSearchProvider.discover()`: existing 402 early-return; add refusal append via `state` patch or `refusals` array.
- `runDiscoveryNode` / `pipeline.ts`: already aggregates refusals; ensure brave 402 refusal propagates.
- `src/app/api/dev/health`: optionally surface `providers.brave.degraded: boolean`.

**Acceptance criteria:**
- [ ] `brave-search.ts` on 402 returns `[]` without throw and records `USAGE_LIMIT_EXCEEDED` in `refusals` so `ops-loop-evidence.json` shows degraded state.
- [ ] Next scheduled `discovery harvest` run succeeds (green) even while quota remains exceeded — jobs marked done via seeds.
- [ ] Health or log contains a searchable `brave quota` token for alerting (no new infra).
- [ ] Test: mock 402 response returns empty hits without throw; mock 429 still retries.

**Out of scope:**
- Upgrading Brave plan or rotating keys (HITL infra task).
- Changing gap-ranking semantics.

