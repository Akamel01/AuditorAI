---
id: R17
title: Evidence bundle reconciliation + HEAD anchoring automation
type: task
hitl: false
status: open
assignee:
blocked_by: []
blocks: []
created: 2026-08-31
resolved:
---

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**
- `HEAD == origin/main == 70a519c` after rebase+push 2026-08-31T19:32; `vault-sync --check` passes; `cmp -s .autoforge/validation/ops-loop-evidence.json stages/07_validate/output/ops-loop-evidence.json` passes; `ci` vault determinism (V2) green.
- Evidence still anchored at commit `61c7475e` in `ops-loop-evidence.json:3-4` (stale `generatedAt 2026-08-30T05:52:52Z`); ledgerGrowth/productionDeploymentVerified/daemonVerified remain `false` by design (residual).
- DIRs `state/discovery-ledger.json` and `state/odd-coverage.json` are committed via bot `data(discovery): harvest [skip ci]` commits; working tree now clean for those files.

**What we still need:**
- Evidence bundle regen when infra proof (KV growth, daemon) becomes available — commit labeling and HEAD anchoring so `ops-loop-evidence.json:commit` always equals deployed `HEAD`.
- CI gate that fails when `ops-loop-evidence.json:commit != HEAD` or `generatedAt` is older than last harvest.

## Agent Brief

**Category:** enhancement
**Summary:** Automate evidence HEAD anchoring and staleness check so validation report never lags behind deployed code.

**Current behavior:** Evidence bundle is manually preserved with `historicalProbe` at old commit; no job enforces `commit == HEAD` or freshness. Reviewer noted `report.md:18-20` stale NOT VERIFIED line.

**Desired behavior:** Add a CI step `evidence-head-check` that (1) asserts `ops-loop-evidence.json.commit == $(git rev-parse HEAD)` and `generatedAt` within 24h of `HEAD` author date, failing with actionable message otherwise; (2) on `workflow_dispatch` with `regen-evidence=true`, re-runs doctor/health capture and overwrites both evidence copies. Evidence copies remain byte-identical by construction.

**Key interfaces:**
- `.autoforge/validation/ops-loop-evidence.json` and `stages/07_validate/output/ops-loop-evidence.json`: existing schema, only `commit`/`generatedAt`/`ledgerGrowth`/`productionDeploymentVerified`/`daemonVerified` change.
- `scripts/validate-state.mjs`: already exists; extend or add `scripts/check-evidence-head.mjs`.
- `.github/workflows/ci.yml`: new job `evidence-head-check` after `vault determinism`.

**Acceptance criteria:**
- [ ] `npm run typecheck && node scripts/check-evidence-head.mjs` exits 0 when `commit == HEAD` and `generatedAt` within 24h.
- [ ] CI fails with `evidence stale: commit 61c7475 != HEAD 70a519c — run: gh workflow run discovery-harvest` when evidence is stale.
- [ ] Regen path updates both copies atomically and `git diff --stat` shows only those two files changed.
- [ ] Existing `report.md` stale line is updated to reflect current dedup guard.

**Out of scope:**
- Changing `ops-loop-evidence.json` schema beyond `commit`/`generatedAt`.
- Auto-pushing regenerated evidence (manual dispatch only).

