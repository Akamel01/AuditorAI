---
id: R18
title: Bento components merge verification — safety, conflicts, functional purpose
type: task
hitl: false
status: open
assignee:
blocked_by: []
blocks: []
created: 2026-08-31
resolved:
---

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**
- Bento/Ethereal Glass components (`src/app/_components/ui/*` 13 files: `alignment-rail.tsx`, `app-shell.tsx`, `button.tsx`, `chips.tsx`, `confirm-dialog.tsx`, `empty-state.tsx`, `evidence-ref.tsx`, `icons.tsx`, `inline-notice.tsx`, `panel.tsx`, `reveal.tsx`, `segmented.tsx`, `theme-toggle.tsx` plus `reset-key.tsx`/`stage-label.tsx`) already on `main` as tracked `.tsx` (see `git ls-files | grep _components/ui` at `056b75f`). Untracked `.jsx`/`.js` duplicates (150+ files, incl `next.config.js`, `scripts/*.js`, `src/**/*.js`, `src/app/**/*.jsx`) were build artifacts cleaned 2026-08-31.
- `feat/mission-control` @ `f6be99c` diff vs `main` shows no delta in `ui/*` — merge is already effectively on `main` via `97dbef7`/`61c7475`/`dce8f08`. `npm run typecheck` pass, `npx eslint` 0 errors, `main` CI green (1m44s).
- `stages/` vs `.autoforge/stages/` divergence noted; not blocking Bento.

**What we still need:**
- Verify after duplicate cleanup that components render correctly in Mission Control (`/dev/mission-control` segmented, odd-matrix, kpi-strip, provider-health) and no style drift.

## Agent Brief

**Category:** enhancement
**Summary:** Verify Bento components on main are safe, non-conflicting, and functional after duplicate cleanup.

**Current behavior:** `main` at `056b75f` has Bento `.tsx` tracked; `.jsx` duplicates removed 2026-08-31. No visual regression check has run post-cleanup.

**Desired behavior:** Manual + automated verification that Bento components (a) have no file conflicts (no `.jsx` shadow), (b) satisfy intended functions: `AppShell` nav, `Button` variants, `Panel` bezel, `Segmented` tabs, `OddMatrix`/`CellCard`/`KpiStrip`/`ReadinessMeters` render with Ethereal Glass tokens, (c) Mission Control page loads with `npm run build` and e2e `flow.spec.ts` passes.

**Key interfaces:**
- `src/app/_components/ui/*`: existing 13 components; no API change.
- `src/app/dev/mission-control/page.tsx` and `_components/*`: existing consumers.
- `next.config.ts` / `tailwind` tokens: existing.

**Acceptance criteria:**
- [ ] `git ls-files --others --exclude-standard | grep -E "\.jsx|\.js$"` shows 0 duplicates shadowing `.tsx`/`.ts` for `ui/*`.
- [ ] `npm run build` succeeds (`/dev/mission-control` 13.9kB check) and `npm run lint` 0 errors on tracked files.
- [ ] Visual check: `/dev/mission-control` renders segmented tabs, odd-matrix 16-cell grid, kpi-strip, no console errors; e2e `flow.spec` passes or is marked skip with reason.
- [ ] No conflict with `feat/mission-control` branch — `git diff main...feat/mission-control -- src/app/_components/ui/` empty.

**Out of scope:**
- Redesigning Bento tokens or adding new variants.
- Deleting `stages/` vs `.autoforge/stages/` divergence (separate ticket).

