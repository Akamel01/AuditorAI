---
id: R19
title: Wayfinder plumbing traceability — ticket index + Mission Control board
type: task
hitl: false
status: open
assignee:
blocked_by: []
blocks: []
created: 2026-09-01
resolved:
---

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**
- Wayfinder local-markdown is the canonical issue tracker for `ops-residual`/`ops-seamless-verify` per `workflow/wayfinder/TRACKER.md` (GitHub `gh issue list` 0 open, `maps/v2-agentic-platform` canonical on GitHub issue #20 but `ops-residual` is local-only). Existing 18 R tickets (`R1-R18`) + 8 F tickets (`M1-M8` via `F1-F4` each) already cover the handoff §8 frontier (`MAP.md:39-57`, `.autoforge/plans/plan.md:204-216`).
- Remaining working-tree drift at `a715ee8` wrap was 5 modified `M` (`package.json`, `src/app/dev/mission-control/page.tsx`, `src/lib/client.ts`, `state/discovery-ledger.json`, `state/odd-coverage.json`, `workflow/wayfinder/TRACKER.md`) + `??` plumbing: `scripts/wayfinder-tickets.ts`, `src/wayfinder/tickets.ts` + `ticket-types.ts`, `src/app/api/dev/tickets/route.ts`, `src/app/dev/mission-control/_components/ticket-board.tsx`, `tests/domain/wayfinder-tickets.test.ts`, `src/app/dev/mission-control/page.tsx` board wiring, `package.json` `tickets` script. These implement ticket compilation for Mission Control (`/dev/mission-control` → Tickets) and `npm run tickets` (`src/wayfinder/tickets.ts` compiles `maps/*/tickets/*.md`).
- Redundancy check (a): searched `src/wayfinder/**`, `scripts/wayfinder*`, `src/app/api/dev/tickets/**` by domain concept "ticket" — found existing implementation at `src/wayfinder/tickets.ts:12-45` (front-matter parse `FRONT_MATTER_RE`, `isTerminalStatus`, `ticketKey`), no prior duplicate ticket for the compilation feature itself. The feature is implemented but not yet ticketed as a traceable decision — this ticket makes it traceable.
- Prior rejection check (b): `.out-of-scope/*.md` does not exist; no prior rejection resembles this plumbing. `workflow/wayfinder/TRACKER.md` explicitly expects this compilation (`Index: src/wayfinder/tickets.ts`).

**What we still need:**
- No reporter info needed — maintainer decision: confirm this plumbing stays as the canonical index (vs GitHub sync) and that `state/*.json` drift remains bot-owned (`data(discovery): harvest [skip ci]`).

## Agent Brief

**Category:** enhancement
**Summary:** Make Wayfinder ticket compilation traceable — the index that powers Mission Control Tickets and `npm run tickets` should be a tracked decision, not untracked drift.

**Current behavior:**
Working tree at `a715ee8` contains untracked plumbing that compiles local-markdown tickets into an index for both the UI and CLI. The compilation exists in the codebase and is referenced in `TRACKER.md`, but there is no decision ticket that records its contract or acceptance. `gh issue list` remains 0 open; `ops-residual` remains local-markdown only.

**Desired behavior:**
The ticket compilation remains the canonical index for `ops-residual`/`ops-seamless-verify`. An agent or human can run a single command to list the frontier: ready without owner, HITL, blocked, and all. Mission Control's Tickets view and the CLI show the same counts. Markdown files remain the source of truth; claim/resolve happens by editing front-matter (`status`, `assignee`). The plumbing is intentionally local-markdown only for `ops-residual` (GitHub remains canonical for `v2-agentic-platform` map #20).

**Key interfaces:**
- Ticket index compilation: the module that reads `maps/*/tickets/*.md` front-matter and exposes `IndexedTicket`/`TicketIndex` with `ready_without_owner`, `hitl`, `frontier`, `blocked` counts
- CLI entry: the script that prints `wayfinder <n> tickets · frontier <n> · ready <n> · hitl <n> · blocked <n>` and supports `--lane`/`--json`
- Mission Control board: the component that renders the ticket lanes (frontier vs HITL vs blocked) from the same index
- API route: the endpoint that serves the index to the board

**Acceptance criteria:**
- [ ] `npm run tickets` (or `npx tsx scripts/wayfinder-tickets.ts --json`) emits valid JSON with `counts.total`, `counts.frontier`, `counts.ready_without_owner`, `counts.hitl_frontier`, `counts.blocked` and `tickets[]` with `key`, `status`, `hitl`, `ready_without_owner`
- [ ] `/dev/mission-control` Tickets lane shows same counts as the CLI for `ready`, `hitl`, `blocked` (manual visual or `flow.spec` check)
- [ ] A new `status: open` ticket front-matter edit (e.g., `assignee: alice`) is reflected in both CLI and board without code change
- [ ] No GitHub sync is introduced for `ops-residual`; `v2-agentic-platform` remains the only GitHub canonical map

**Out of scope:**
- Migrating `ops-residual` to GitHub Issues
- Changing `MAP.md` Destination/Notes/Decisions structure
- Vault or evidence anchoring (`R17`) or Brave monitoring (`R16`)
