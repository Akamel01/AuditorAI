---
id: C4
title: Folder ordering + normalization execution (ICM tree, manifest-gated moves)
type: task
hitl: false
status: closed
assignee: orchestrator
blocked_by: [C2, C3]
blocks: []
created: 2026-09-11
resolved: 2026-09-12
---

## Agent Brief

**Category:** restructure
**Summary:** Execute the ICM ordering of `docs/RSA-Documents/` per icm-architect restructure mode: target tree `jurisdiction/` → `stage/` → `type/` shelves + `_system/` (schema, catalog script, contracts) + `_archive/` (superseded logs, never silently deleted); per-folder `CONTEXT.md` purpose lines; root `CLAUDE.md` routing (<60 lines). Moves are manifest-gated: publish `MOVE_MANIFEST.md` (old → new → role) FIRST, owner approves in-file, then migrate. Corpus is git-untracked so moves have zero repo diff — the gate is about human review, not CI.

**Current behavior:** Harvest-era layout (source-oriented: fhwa-case-studies, state-dots, canadian*) + quarantine piles + empties.

**Desired behavior:** Ordered tree + `corpus-catalog.json` generated by script (rebuildable, never hand-edited — ICM invariant 9) + one-home-per-fact links where old paths were referenced (COLLECTION_INDEX.md paths!). De-duplicate entry files toward the catalog.

**Key interfaces:**
- Inputs: C2 schema, C3 verdicts, C1 inventory, COLLECTION_INDEX.md path refs (leave redirect notes, don't edit history).
- Script: catalog builder (stdlib only) emitting `corpus-catalog.json` from front-matter/sidecars.

**Acceptance criteria:**
- [ ] MOVE_MANIFEST.md published + owner-approved (signed line) BEFORE any move.
- [ ] Post-move walk test passes (cold: where-am-I + stage contracts + status-by-scanning).
- [ ] `corpus-catalog.json` rebuilds byte-identical via script; COLLECTION_INDEX.md refs resolvable.
- [ ] `_archive/` holds superseded run logs (`.rsa_*.log`, pid files), nothing deleted silently.

**Out of scope:**
- Text extraction (C6), labeling content (uses C2, applied here mechanically), license gating (C10 runs parallel).

## Resolution

Manifest-gated close (GATE-1 upheld — ZERO moves executed): `MOVE_MANIFEST.md` PROPOSED (dir table + rule + _archive payload + fha-delete proposal + COLLECTION_INDEX 60/64 redirect, approval line UNSIGNED) + `corpus-catalog.json` via `scripts/build-catalog.py` (327 records, 1:1 with C5 index, byte-identical rebuild `7d9be769…`, 327/327 single-shelf dry-run) + walk test PASS pre-move. Review fix applied: dotfiles/.DS_Store/.autoforge/scripts/registers excluded from catalog universe (homes elsewhere), full sha recorded. Post-approval migration (moves, CONTEXT.md/CLAUDE.md, post-move walk) = follow-up trigger on owner signature. CLOSED 2026-09-12
